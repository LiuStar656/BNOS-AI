# node_rust_grok_hands

> 节点类型：Rust 工具执行器 | 语言：Rust | 状态：[PLAN]

## 目录

1. [节点定位](#一节点定位)
2. [来源](#二来源)
3. [职责](#三职责)
4. [算力回路](#四算力回路)
5. [端口定义](#五端口定义)
6. [node_config.json](#六node_configjson)
7. [处理逻辑伪代码](#七处理逻辑伪代码)
8. [依赖](#八依赖)
9. [与原项目的差异](#九与原项目的差异)
10. [测试计划](#十测试计划)

---

## 一、节点定位

纯工具执行器。**不调用任何 API、不持有任何 LLM、不做任何推理判断**。算力统一由 `llm_infer` 节点提供。

## 二、来源

封装 Grok Build 的 MCP 客户端。

## 三、职责

- 接收 AAA 发来的工具调用请求（`data_type: "tool_call"`）
- 通过 MCP 协议执行外部工具（web_search、code_exec、file_read/write 等）
- 返回原始工具执行结果给 AAA（`data_type: "json"`）
- AAA 收到结果后写 DB -> 构建含工具结果的新 prompt -> 发给 `llm_infer` 推理

## 四、算力回路

```
AAA 检测 tool_call -> Grok 执行工具
                         │
                         ▼ 返回原始结果
                       AAA 写 DB + 重建 prompt
                         │
                         ▼
                    llm_infer 推理（唯一算力提供者）
                         │
                         ▼
                       AAA 解析 + 最终输出
```

Grok 仅执行，不吃算力。如果工具结果需要理解和总结，由 AAA 拼接回 prompt，`llm_infer` 统一推理。

## 五、端口定义

### 输入端口

| 端口名 | filter (data_type) | 说明 |
|--------|-------------------|------|
| `tool_call` | `tool_call` | 工具调用指令 `{tool_name, args}` |

### 输出端口

| 端口名 | data_type | 目标 |
|--------|-----------|------|
| `default` | `json` | AAA tool_result 端口 |

## 六、node_config.json

```jsonc
{
  "node_name": "grok_hands",
  "language": "rust",
  "parameters": [
    {"name": "mcp_server_list", "type": "text", "label": "MCP 服务器列表 (JSON)", "default": "[]"},
    {"name": "max_exec_time", "type": "int", "label": "最大执行时间(秒)", "default": 30}
  ],
  "input_ports": [
    {"name": "tool_call", "label": "工具调用", "type": "json", "required": true, "source": "node"}
  ],
  "filter": {
    "tool_call": {"data_type": "tool_call"}
  },
  "output_ports": [
    {"name": "default", "label": "工具结果", "type": "default"}
  ]
}
```

## 七、处理逻辑伪代码

遵循 BNOS 节点开发规范：`main.rs` 从 stdin 读 JSON、stdout 写 JSON、不依赖任何 BNOS 模块、多端口用 `filter` 路由不用 if-else 分发。

```rust
use std::io::{self, Read, Write};
use serde_json::Value;

fn main() {
    let mut input = String::new();
    io::stdin().read_to_string(&mut input).unwrap();
    let data: Value = serde_json::from_str(&input).unwrap();
    
    let tool_name = data["content"]["tool_name"].as_str().unwrap();
    let args = &data["content"]["args"];
    
    // 根据 tool_name 路由到对应 MCP 工具
    let result = match tool_name {
        "web_search" => execute_web_search(args),
        "code_exec" => execute_code(args),
        "file_read" => read_file(args),
        "file_write" => write_file(args),
        _ => Ok(Value::String("unknown tool".to_string())),
    };
    
    let output = serde_json::json!({
        "data_type": "json",
        "content": result.unwrap_or_else(|e| Value::String(format!("error: {}", e)))
    });
    
    io::stdout().write_all(output.to_string().as_bytes()).unwrap();
}
```

## 八、依赖

- `serde`, `serde_json`
- `reqwest`（HTTP 调用 MCP 服务器）
- `tokio`（异步运行时）
- 标准库：`std::io`, `std::process`

## 九、与原项目的差异

| Grok Build | 新设计 |
|------------|--------|
| 内嵌 LLM 推理能力 | 去除（算力归 `llm_infer`） |
| 支持 ACP 协议 | 去除（仅保留 MCP） |
| 自主决策调用工具 | 去除（由 AAA 决定调用什么工具） |
| MCP 客户端核心 | 保留 |
| 工具执行和结果返回 | 保留 |

## 九、参考项目

本节点的设计参考了 `BNOS_AI_project` 根目录下的以下项目：

| 项目 | 路径 | 参考内容 |
|------|------|---------|
| Grok Build | `../../referencees/grok-build-main/` | MCP 客户端核心实现、工具执行逻辑、Rust 项目结构 |

### 关键文件索引

```
grok-build-main/
├── src/
│   ├── main.rs                        # 主入口（MCP 客户端初始化）
│   ├── mcp_client.rs                  # MCP 客户端实现（协议/连接管理）
│   ├── tools/
│   │   ├── web_search.rs              # 联网搜索工具
│   │   ├── code_exec.rs               # 代码执行工具
│   │   ├── file_read.rs               # 文件读取工具
│   │   └── file_write.rs              # 文件写入工具
│   └── types.rs                       # 数据类型定义
├── Cargo.toml                         # 依赖配置
└── mcp_protocol.md                    # MCP 协议文档
```

## 十、测试计划

### 单元测试

- `tool_name` 路由 -> 正确工具执行

### 集成测试

- mock MCP 服务器 -> 发送 `tool_call` -> 验证返回结果

### 边界测试

- 超时测试：`max_exec_time` 到期 -> 返回超时错误
- 错误处理：未知工具名 -> 返回 error
