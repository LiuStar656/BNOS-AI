# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# 节点代码入库（历史决策：节点纳入主仓库管理）
# 排除：废弃节点、运行时文件、官方子仓库（harness 由 node_dsh/.gitignore 排除）
nodes/shared/
nodes/node_rust_grok_hands/
voice/
audio/
models/
live2d/

# Reference projects
references/
_archive/
# OS
Thumbs.db
.DS_Store

# Logs
*.log
logs/

# Runtime artifacts — 引擎/节点/GUI 每次运行的产物，不入库
*.pid
bnos_status.json
conversation_history.json
output_*.json

# Environment
.env
.env.*

# Large binary artifacts
*.npz
*.wav
*.pt
*.pth

# Logseq user data
logseq_pages/

# Experiment / temp artifacts (not for public mirror)
_tmp_*/
tmp_*.py
_tmp_*.py
_dbg/
runs/
*.db
*.sqlite
*.jsonl
_tmp_deep_analysis.py

# schemanet research data & run outputs (kept local)
schemanet/runs/
schemanet/data/

# Runtime artifacts — 引擎/节点/GUI 每次运行的产物，不入库
bnos_status.json
conversation_history.json
output_*.json

# Paper assets — 论文初稿/大纲与生成物仅本地保留，不入公开镜像
docs/cogevo/\[WIP\]*
docs/cogevo/\[EXP\]*
docs/cogevo/\[PLAN\]*
docs/cogevo/\[REPORT\]*
docs/cogevo/paper.html
docs/cogevo/convert_paper.py
docs/cogevo/figs/

# 复现包内的实验样本放行（覆盖上方 *.jsonl）
!docs/cogevo/paper_repro/**/*.jsonl

# schemanet 子项目 — 含论文草稿/终结宣告/答辩材料等敏感资产，仅本地保留
schemanet/

# large git bundle backup (kept outside the repo)
_backup_prefilter.bundle
