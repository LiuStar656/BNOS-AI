# BNOS 节点开发规范

> 本文档定义 BNOS 节点的开发标准，包括目录结构、配置文件、监听器、启动脚本等。
> 如需开发**新语言的节点生成器**，请参阅 [节点生成器开发准则](节点生成器开发准则.md)。
> 如需了解 `node_config.json` 输入端口与参数控件的详细配置，请参阅 [config_json 开发规范](config_json_开发规范.md)。

## 目录

- [一、节点目录结构](#一节点目录结构)
- [二、节点设计原则：避免过度颗粒化](#二节点设计原则避免过度颗粒化)
  - [2.1 一个节点应该独立的判断标准](#21-一个节点应该独立的判断标准)
  - [2.2 典型反例：纯数据搬运节点](#22-典型反例纯数据搬运节点)
  - [2.3 判别速查](#23-判别速查)
  - [2.4 正确的抽象粒度](#24-正确的抽象粒度)
- [三、node_config.json 基础规范](#三node_configjson-基础规范)
- [四、监听器（listener）开发规范](#四监听器listener开发规范)
  - [输入源解析优先级（resolve_input_sources）](#输入源解析优先级resolve_input_sources)
  - [路径规范化去重（Windows）](#路径规范化去重windows)
  - [进程生命周期管理](#进程生命周期管理)
  - [多文件轮询主循环](#多文件轮询主循环)
  - [数据格式解包（unwrap_data）](#数据格式解包unwrap_data)
  - [注意力过滤实现（is_my_data）](#注意力过滤实现is_my_data)
  - [子进程超时保护](#子进程超时保护)
  - [venv 自愈机制（pyvenv.cfg 重建）](#venv-自愈机制pyvenvcfg-重建)
  - [并发处理架构（线程池多源并行）](#并发处理架构线程池多源并行)
  - [Node.js / JavaScript 并发处理](#nodejs--javascript-并发处理)
  - [Python 监听器伪代码](#python-监听器伪代码含多源监听--port_mappings--venv-自愈)
  - [Node.js / JavaScript 监听器伪代码](#nodejs--javascript-监听器伪代码多源并发版)
- [五、处理逻辑（main）开发规范](#五处理逻辑main开发规范)
  - [data_type 路由与 filter 必须同步](#data_type-路由与-filter-必须同步)
- [六、启动脚本规范](#六启动脚本规范)
  - [编码与缓冲设置](#编码与缓冲设置)
  - [引擎级进程清理（run.bat）](#引擎级进程清理runbat)
- [七、节点间通信协议](#七节点间通信协议)
  - [防重复处理](#防重复处理)
  - [port_mappings 数据路由](#port_mappings-数据路由自定义接口)
  - [上游输出协议定义](#上游输出协议定义)
- [八、开发检查清单](#八开发检查清单)
- [九、相关文档](#九相关文档)

---

## 一、节点目录结构

每个节点在 `项目/nodes/` 下必须有如下结构：

```
nodes/
└── node_{lang}_{name}/
    ├── node_config.json      # 节点配置（必需，见下节）
    ├── start.bat            # Windows 启动脚本（必需）
    ├── start.sh             # Linux/macOS 启动脚本（必需）
    ├── listener.{ext}       # 监听器入口（可选，持续运行）
    ├── main.{ext}           # 处理逻辑入口（可选，核心业务代码）
    ├── output.json          # 节点输出文件（运行时生成）
    ├── requirements.txt     # 依赖声明（Python 等语言必需）
    ├── .pid                 # 进程 PID 文件（启动后自动生成）
    ├── .gitignore           # 忽略编译产物/日志
    └── logs/                # 运行日志目录（自动创建）
        └── listener.log
```

### Python 节点示例

```
nodes/
└── node_python_demo/
    ├── node_config.json
    ├── start.bat
    ├── start.sh
    ├── listener.py
    ├── main.py
    ├── packet.py            # 数据包结构定义（可选）
    ├── output.json
    ├── requirements.txt
    ├── venv/                # 独立虚拟环境（首次启动自动创建）
    ├── .gitignore
    └── logs/
        └── listener.log
```

### Rust 节点示例

```
nodes/
└── node_rust_demo/
    ├── node_config.json
    ├── start.bat
    ├── start.sh
    ├── Cargo.toml           # Rust 项目配置
    ├── output.json
    ├── src/
    │   ├── main.rs          # 主处理逻辑
    │   ├── listener.rs      # 监听器
    │   └── packet.rs        # 数据包结构（可选）
    ├── .gitignore
    └── logs/
        └── listener.log
```

---

## 二、节点设计原则：避免过度颗粒化

### 2.1 一个节点应该独立的判断标准

将一个功能拆分为独立节点会带来进程隔离、独立 venv、独立崩溃域等好处，但**每个节点也意味着一次额外文件 I/O、一个额外进程、一份额外心智负担**。设计节点时，用以下标准判断是否值得独立：

| 标准 | 应该独立 | 不该独立 |
|------|---------|---------|
| **不同技术栈** | 一个 Python，一个 Rust / JS → 独立 | 都是 Python，依赖相同 → 考虑合并 |
| **不同资源需求** | 一个要 GPU / 大内存，另一个轻量 → 独立 | 资源需求相似 → 考虑合并 |
| **不同崩溃域** | 一个挂了不影响另一个的可用性 → 独立 | 一个挂了另一个也失去意义 → 合并 |
| **独立业务逻辑** | 有完整的状态机、独立的数据处理流程 → 独立 | 只是格式转换、加标签、路由转发 → 合并 |
| **可独立开发测试** | 不依赖其他节点也能运行和验证 → 独立 | 必须依赖上游/下游才能运行 → 合并或重组 |

### 2.2 典型反例：纯数据搬运节点

```
GUI → gui_adapter → user_input → AAA → LLM → AAA → gui_adapter → GUI
```

`gui_adapter` 只做格式转换，`user_input` 只加一个 `source` 标签。两者加一起不到 20 行有效逻辑，但多了：
  - 2 个进程启动时间
  - 2 次额外的文件 I/O（读 + 写）
  - 2 份日志需要追踪
  - 2 份 `node_config.json` 需要维护

**正确做法**：将格式转换和标签标记合并到 AAA 作为内部逻辑，AAA 直接监听 `gui_input.json`。

### 2.3 判别速查

```
这个功能要不要独立成节点？
│
├─ 需要不同语言 / 框架（Python vs JS vs Rust）？ → 独立
├─ 需要独占 GPU / 硬件资源？                   → 独立
├─ 崩溃不应影响核心对话链？                     → 独立
├─ 只是数据格式转换 / 加字段 / 路由转发？        → 合并
├─ 逻辑 < 30 行有效代码且不依赖特殊资源？        → 合并
└─ 必须凑齐全链路节点才能测试？                  → 合并
```

### 2.4 正确的抽象粒度

一个节点应该是**一个独立的业务能力单元**，而不是：
  - ❌ 一个函数 → 独立成节点（过度拆分）
  - ❌ 一个数据格式转换 → 独立成节点（过度拆分）
  - ❌ 一个 "以后可能需要独立" 的功能 → 独立成节点（过早优化）
  - ✅ 一个有状态机/状态管理的完整流程 → 独立节点
  - ✅ 一个需要不同运行时环境的逻辑块 → 独立节点
  - ✅ 一个崩溃不影响其他功能的关键模块 → 独立节点

> **原则**：先合后拆。先在一个节点里实现完整的业务流程，当确实出现以上独立需求时再拆分为独立节点。不要为了"未来可能的扩展"提前拆分。

---

## 三、node_config.json 基础规范

每个节点必须包含 `node_config.json`，字段如下：

```json
{
  "node_name": "node_python_1",
  "listen_upper_file": "../data/upper_data.json",
  "output_file": "./output.json",
  "output_type": "",
  "filter": {},
  "out_connections": {},
  "port_mappings": {},
  "parameters": [],
  "input_ports": [],
  "output_ports": []
}
```

### 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `node_name` | string | **必须与节点目录名一致** |
| `listen_upper_file` | string | 监听的上游数据文件路径（**可选**，使用 `port_mappings` 或 `pipeline.json` 时可留空） |
| `output_file` | string | 本节点输出文件路径 |
| `filter` | object | **v2.1+ 支持多端口过滤**：外层为端口名，内层为该端口的过滤条件。空 `{}` 则处理所有任务，见下方详解 |
| `output_type` | string | 输出数据类型标识 |
| `output_ports` | array | **输出端口列表**：每个端口可指定独立的 `output_file`，实现多端口到不同文件的映射（详情见 [config_json 开发规范](config_json_开发规范.md)） |
| `out_connections` | object | **输出端口连接映射**：`"本节点输出端口名": "下游节点名|下游输入端口名"`，记录画布连线的源端口→目标路由（由 Canvas 引擎自动维护） |
| `port_mappings` | object | **自定义接口映射**：定义输入端口到上游文件路径的映射，实现多源数据路由（见通信协议章节） |

### 注意力过滤机制（v2.1+ 多端口过滤）

`filter` 字段支持**多端口过滤**格式：

```json
{
  "filter": {
    "input_port_a": { "data_type": "chat", "source": "text" },
    "input_port_b": { "data_type": "command", "source": "gui" }
  }
}
```

- `filter` 为空 `{}`：节点处理**所有**上游任务（广泛注意力），此时在 listener 的 `is_my_data()` 中直接返回 `true`
- `filter` 有键值对时：
  - 外层 Key 为端口名称（仅供阅读标记，不作为匹配依据）
  - 内层 Value 为该端口的过滤条件（`{key: value}`）
  - 匹配规则：数据的字段**满足任意一个端口的全部条件**即可通过（**或关系**），而非要求同时满足所有条件

> **注意**：旧版 `filter: { "type": "data_task" }` 单层格式已废弃。如使用单层格式，listener 会将其作为单端口处理，数据必须完全匹配所有键值对。

### 自定义接口映射（port_mappings）

`port_mappings` 字段定义节点间的**自定义数据路由**，与 Canvas 引擎配合实现端口级别的数据分发：

```json
{
  "port_mappings": {
    "input_port_name": "../other_node/output.json"
  }
}
```

- Key 为当前节点的输入端口名称
- Value 为上游节点输出文件的路径（相对路径，相对于本节点目录）
- listener 在启动时自动解析 `port_mappings`，将映射的文件路径加入监听列表
- **无需手动配置 `listen_upper_file`**：Canvas 引擎启动时会根据 `pipeline.json` 的边关系和 `port_mappings` 自动填充数据路由

> **详细配置**（`parameters` / `input_ports` / `output_ports` 11 种控件类型）见：[config_json 开发规范](config_json_开发规范.md)

---

## 四、监听器（listener）开发规范

监听器是节点的核心入口，**必须持续运行**，不能主动退出。

### 最低功能要求

1. 读取本节点 `node_config.json` 获取监听配置
2. 自动解析输入源：通过 `resolve_input_sources()` 整合三类来源（见下文）
3. 同时监听**多个输入源文件**的变化（多文件轮询）
4. 根据 `filter` 规则判断是否处理该任务（注意力过滤，**任一端口匹配即可**）
5. 检查 `_processed_<node_name>` 标记，避免重复处理
6. 调用 `main` 处理逻辑，传入任务数据
7. 将处理结果写入本节点 `output.json`
8. 在上游文件中添加 `_processed_<node_name>` 处理标记
9. 日志写入 `logs/listener.log`
10. 数据格式解包：自动处理包装格式（`{"code": 0, "data": {...}}`）

### 自愈机制

- 输出文件路径不存在时自动创建目录
- 上游文件不存在时等待并重试
- 异常时不崩溃，记录日志后继续监听
- 检测运行环境是否完整，缺失则自动修复

### 多源监听与 port_mappings 适配

listener 必须支持**同时监听多个输入源**，而非仅依赖 `listen_upper_file`。

#### 输入源解析优先级（resolve_input_sources）

listener 启动时调用 `resolve_input_sources()` 自动收集所有输入源：

1. **`listen_upper_file`** — 如果配置了该字段，优先使用
2. **`port_mappings` 字符串值** — 遍历配置中所有值为字符串的映射条目，解析为绝对路径
3. **`pipeline.json` edges** — 查找所有指向本节点的上游节点，追踪其 `output_file` 路径

```python
def resolve_input_sources():
    """自动解析输入源文件列表。"""
    sources = []

    # 1. listen_upper_file
    if UPPER_FILE:
        sources.append(UPPER_FILE)

    # 2. port_mappings 字符串值（输入端口 → 文件路径）
    for port_name, mapping in cfg.get("port_mappings", {}).items():
        if isinstance(mapping, str):
            path = mapping if os.path.isabs(mapping) else os.path.abspath(os.path.join(NODE_DIR, mapping))
            sources.append(path)

    # 3. pipeline.json edges
    project_root = find_project_root()
    pipeline_path = os.path.join(project_root, "pipeline.json")
    nodes_dir = os.path.join(project_root, "nodes")
    if os.path.exists(pipeline_path):
        pipeline = json.load(open(pipeline_path, "r", encoding="utf-8"))
        for edge in pipeline.get("edges", []):
            if edge["to"] == NODE_NAME:
                src_node = edge["from"]
                # ★ 获取上游端口名（pipeline 中 source_port 字段）
                src_port = edge.get("source_port", "default")
                src_cfg = json.load(open(os.path.join(nodes_dir, src_node, "node_config.json")))
                # ★ 优先从 output_ports 中按端口名匹配，获取实际输出文件路径
                #    ⚠ 不能直接用顶层 output_file，多端口输出下顶层 output_file 可能
                #      与实际端口输出文件不一致（如 default 端口输出到 output_default.json
                #      但顶层 output_file 仍为 ./output.json），导致 listener 监听错误文件。
                src_output_file = src_cfg.get("output_file", "./output.json")
                for port in src_cfg.get("output_ports", []):
                    if isinstance(port, dict) and port.get("name") == src_port and port.get("output_file"):
                        src_output_file = port["output_file"]
                        break
                src_path = os.path.abspath(os.path.join(nodes_dir, src_node, src_output_file))
                if src_path not in sources:
                    sources.append(src_path)

    # 4. 大小写规范化去重（Windows 上 E:\ 和 e:\ 是同一路径）
    seen = set()
    deduped = []
    for p in sources:
        key = os.path.normcase(p)
        if key not in seen:
            seen.add(key)
            deduped.append(p)

    return deduped
```

#### 路径规范化去重（Windows）★

**问题**：在 Windows 系统中，`E:\project\nodes\a\output.json` 和 `e:\project\nodes\a\output.json` 指向同一文件，但 Python 字符串比较认为它们是不同的路径。如果 `listen_upper_file` 使用大写盘符，而 `port_mappings` 或 `pipeline.json` 解析出小写盘符路径，同一文件会被添加两次，导致数据被重复处理。

**解决方案**：在 `resolve_input_sources()` 的最后用 `os.path.normcase()` 统一规范化路径后再去重：

```python
# 大小写规范化去重
seen = set()
deduped = []
for p in sources:
    key = os.path.normcase(p)  # Windows: E:\... → e:\...
    if key not in seen:
        seen.add(key)
        deduped.append(p)
INPUT_SOURCES = deduped
```

> **注意**：`os.path.normcase()` 在 Windows 上将路径转换为小写，在 Linux/macOS 上不改变大小写。因此这段代码在所有平台都安全，不会破坏 Linux 的大小写敏感路径。

#### 多端口输出场景的监听陷阱 ★

**问题**：当上游节点配置了 `output_ports` 多端口输出时，`resolve_input_sources()` 从 `pipeline.json` 解析输入源时如果只取上游节点的顶层 `output_file` 字段，可能监听到错误的文件。

**典型场景**：
```
上游节点 (AAA) 配置了 output_ports：
  - name: "default", output_file: "./output_default.json"
  - name: "prompt",  output_file: "./output_prompt.json"

顶层 output_file 仍为 "./output.json"（兼容旧版默认值）

pipeline.json edge：{ "from": "AAA", "to": "GUI", "source_port": "default" }

resolve_input_sources() 误听：
  ❌  E:\...\aaa_cognition\output.json        ← 实际不存在的旧文件
  ✅  应监听 E:\...\aaa_cognition\output_default.json  ← default 端口实际输出
```

**后果**：listener 监听了一个不存在的文件或过时的文件，当上游节点写入实际输出时 listener 无任何反应，数据链中断。

**解决方案**（已在上面伪代码中修正）：
在 `resolve_input_sources()` 解析 `pipeline.json` 时，必须读取 `edge["source_port"]` 获取上游端口名，然后从上游节点的 `output_ports` 中匹配对应端口的 `output_file`，而非直接使用顶层的 `output_file`：

```python
src_port = edge.get("source_port", "default")
src_output_file = src_cfg.get("output_file", "./output.json")
for port in src_cfg.get("output_ports", []):
    if isinstance(port, dict) and port.get("name") == src_port and port.get("output_file"):
        src_output_file = port["output_file"]  # 匹配到正确的输出文件
        break
src_path = os.path.abspath(os.path.join(nodes_dir, src_node, src_output_file))
```

> **注意**：此问题也存在于 `resolve_input_sources()` 中 `port_mappings` 直接配置的路径中。如果 `port_mappings` 值是手动配置的绝对路径（如 Canvas 引擎自动写入的），它已经是正确的路径，不需要修改。但 `pipeline.json` 的自动解析必须做端口匹配。

#### 进程生命周期管理

listener 作为长时间运行的事件循环进程，必须管理好自己的生命周期，并与引擎的 `ProcessManager` 配合。

##### PID 文件管理

启动时写入 PID 文件，进程退出时清理：

```python
import atexit

PID_FILE = os.path.join(NODE_DIR, f"{NODE_NAME}.pid")

def cleanup():
    if os.path.exists(PID_FILE):
        os.remove(PID_FILE)

atexit.register(cleanup)

# 启动时写入 PID
with open(PID_FILE, "w") as f:
    f.write(str(os.getpid()))
```

##### 信号处理器

每个 listener 都应注册 `SIGINT`/`SIGTERM` 处理器，实现优雅退出：

```python
RUNNING = True

def signal_handler(signum, frame):
    RUNNING = False

signal.signal(signal.SIGINT, signal_handler)
signal.signal(signal.SIGTERM, signal_handler)
```

主循环使用 `while RUNNING:` 而非 `while True:`，收到信号后自然退出。

##### 启动时杀旧进程

引擎在启动每个节点前先杀死同名旧实例，防止残留进程导致重复处理：

```python
def _kill_existing_instance(node_path: Path) -> None:
    """根据 PID 文件杀掉该节点的旧进程。"""
    pid_file = node_path / f"{node_path.name}.pid"
    if pid_file.exists():
        try:
            old_pid = int(pid_file.read_text().strip())
            if os.name == "nt":
                subprocess.run(["taskkill", "/F", "/PID", str(old_pid)], capture_output=True, timeout=5)
            else:
                os.kill(old_pid, 9)
        except Exception:
            pass
        pid_file.unlink(missing_ok=True)
```

##### 非阻塞启动（与引擎 ProcessManager 配合）

长时间运行的 listener 必须提供非阻塞启动接口，由引擎统一管理生命周期：

```python
# StandaloneRunner.start() — 非阻塞启动
def start(self) -> tuple[str, subprocess.Popen]:
    """非阻塞启动节点进程，立即返回 (node_id, proc)。"""
    node_path = self.project_root / self.defn.path
    self._kill_existing_instance(node_path)
    proc = subprocess.Popen(
        [str(python_exe), entry],
        cwd=str(node_path),
        stdout=None, stderr=None,  # 继承引擎 stdout/stderr（不捕获，防管道阻塞）
        env={**os.environ, "BNOS_RUNTIME": "1"},
    )
    return self.node_id, proc
```

| 方法 | 用途 | 说明 |
|------|------|------|
| `run()` | 短任务节点 | 阻塞等待完成，返回 `NodeResult`（旧接口） |
| `start()` | 长时间运行节点 | 非阻塞返回 `(node_id, proc)`，由引擎 `ProcessManager` 管理生命周期 |

> **引擎侧流程**：启动所有节点 → 注册到 ProcessManager → 等待关闭信号（Ctrl+C/SIGTERM）→ ProcessManager.shutdown() 统一 kill 所有子进程。

#### 多文件轮询主循环

listener 的主循环必须同时轮询所有输入源文件：

```python
# 轮询所有输入源
for src_path in INPUT_SOURCES:
    if not os.path.exists(src_path):
        continue
    current_mtime = os.path.getmtime(src_path)
    if current_mtime <= last_mtimes.get(src_path, 0):
        continue

    with open(src_path, "r", encoding="utf-8") as f:
        raw_data = json.load(f)

    # 去重：用内容哈希检测是否真的变了
    content_str = json.dumps(raw_data, ensure_ascii=False)
    content_hash = hashlib.md5(content_str.encode()).hexdigest()
    if content_hash == content_hashes.get(src_path):
        last_mtimes[src_path] = current_mtime
        continue

    # 更新缓存
    last_mtimes[src_path] = current_mtime
    content_hashes[src_path] = content_hash

    # 处理数据...
```

#### 数据格式解包（unwrap_data）

上游数据可能以包装格式传入（`{"code": 0, "data": {...}}`），listener 需要自动解包：

```python
def unwrap_data(data):
    """如果数据是包装格式 {'code': 0, 'data': {...}}，提取内层 data"""
    if isinstance(data, dict) and "code" in data and "data" in data:
        inner = data["data"]
        if isinstance(inner, dict):
            return inner
    return data
```

#### 注意力过滤实现（is_my_data）

filter 采用**或关系**匹配，满足任意端口条件即可：

```python
def is_my_data(data):
    if not MY_FILTER:
        return True
    # 匹配任一端口条件即可通过（或关系）
    for _port_name, criteria in MY_FILTER.items():
        if all(data.get(k) == v for k, v in criteria.items()):
            return True
    return False
```

### 子进程超时保护 ★

listener 通过 `subprocess.run()` 执行 `main.py` 时，如果不设置 `timeout` 参数，`main.py` 的进程可能永久卡死，导致整个 listener 的处理线程被占用无法释放。

#### 典型场景

LLM 推理节点的 `main.py` 调用本地模型 API 时，如果模型服务挂了或响应超时，`requests.post()` 的默认超时是 `None`（永远等待）。没有 `timeout` 保护的情况下，listener 的工作线程被永久阻塞，不再处理其他输入源的数据。

#### 检查清单

- [ ] `subprocess.run()` 是否设置了 `timeout` 参数（建议 180-300 秒）？
- [ ] `main.py` 内部对 API / 子进程的调用是否设置了超时？
- [ ] 超时后 listener 是否有日志记录和错误恢复逻辑？

```python
# ✅ 正确：subprocess.run 设置 timeout
result = subprocess.run(
    [python_exe, "main.py", json.dumps(input_data)],
    capture_output=True, text=True,
    timeout=180,                          # 必填
)

# ❌ 禁止：永不超时
result = subprocess.run(
    [python_exe, "main.py", json.dumps(input_data)],
    capture_output=True, text=True,       # 可能永久阻塞
)

# ✅ 错误恢复：捕获超时异常并记录
except subprocess.TimeoutExpired:
    log(f"处理超时 [{src_path}]: 子进程超过 {timeout} 秒未返回", "ERROR")
except Exception as e:
    log(f"处理异常 [{src_path}]: {e}", "ERROR")
```

> **特别注意**：`timeout` 应略大于 `main.py` 的正常最长处理时间（如 LLM 推理 120 秒，设 180 秒）。过短会导致正常任务被中断，过长则失去保护意义。

### 多端口输出路由

每个节点可以有多个输出端口，每个端口可输出到**独立的 JSON 文件**而非共用 `output.json`。

#### 配置方式

在 `node_config.json` 的 `output_ports` 中为每个端口指定 `output_file`：

```json
{
  "output_file": "./output.json",
  "output_ports": [
    {
      "label": "默认输出",
      "name": "default",
      "output_file": "./output.json",
      "type": "default"
    },
    {
      "label": "状态反馈",
      "name": "status",
      "output_file": "./output_status.json",
      "type": "json"
    }
  ]
}
```

- `output_file` 为空或缺失时，自动推导为 `./output_{port_name}.json`
- 顶层 `output_file` 作为回退路径：当某端口的输出类型不匹配任何已注册端口时，写入顶层 `output_file`

#### Listener 路由逻辑

listener 启动时从 `output_ports` 构建端口→文件路径映射：

```python
OUTPUT_FILE = os.path.abspath(os.path.join(NODE_DIR, cfg["output_file"]))
OUTPUT_PORTS = {}
for port in cfg.get("output_ports", []):
    if isinstance(port, dict) and port.get("name"):
        fpath = port.get("output_file", "")
        if fpath:
            OUTPUT_PORTS[port["name"]] = os.path.abspath(os.path.join(NODE_DIR, fpath))
```

处理完成后根据 `main.py` 返回结果的 `type` 字段路由输出：

```python
port_name = result.get("type", "default") if isinstance(result, dict) else "default"
out_path = OUTPUT_PORTS.get(port_name, OUTPUT_FILE)
```

- `main.py` 返回 `{"type": "gui_text", "data_type": "text", "content": "..."}` → 路由到 `OUTPUT_PORTS["gui_text"]`
- `main.py` 返回 `{"data_type": "reply", ...}`（无 `type`）→ 路由到 `OUTPUT_FILE`（顶层回退）

#### main.py 输出规范

为了让 listener 正确路由，`main.py` 的返回结果应包含 `type` 字段，值对应 `output_ports[*].name`：

```python
def process(data):
    if is_gui_input(data):
        return {
            "type": "gui_text",       # ← 对应 output_ports 中 name="gui_text" 的端口
            "data_type": "text",
            "content": data.get("content", ""),
        }
    if is_reply(data):
        return {
            # 无 type → 路由到 OUTPUT_FILE（顶层回退）
            "data_type": "reply",
            "content": data.get("content", ""),
        }
```

#### 写锁保护

多端口输出时，每个端口文件独立写入，但统一通过 `WRITE_LOCK` 保护，防止并发写错乱：

```python
with WRITE_LOCK:
    os.makedirs(os.path.dirname(out_path), exist_ok=True)
    with open(out_path, "w", encoding="utf-8") as f:
        f.write(output)
    log(f"写入 {out_path}")
```

### venv 自愈机制（pyvenv.cfg 重建）

虚拟环境的 `pyvenv.cfg` 文件可能因 git 操作、压缩解压等原因丢失，导致 Python 无法识别 venv。listener 必须包含自动检测与重建逻辑。

#### 检测逻辑

```python
def _repair_pyvenv_cfg(venv_path):
    """重建缺失的 pyvenv.cfg"""
    import sys
    cfg_content = f"""home = {os.path.dirname(sys.executable)}
implementation = CPython
version_info = {sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}
include-system-site-packages = false
base-prefix = {sys.prefix}
base-exec-prefix = {sys.prefix}
base-executable = {sys.executable}
"""
    cfg_path = os.path.join(venv_path, "pyvenv.cfg")
    with open(cfg_path, "w", encoding="utf-8") as f:
        f.write(cfg_content)
```

#### 自愈流程

```python
def check_and_repair_environment():
    venv_path = os.path.join(NODE_DIR, "venv")
    python_exe = os.path.join(venv_path, "Scripts", "python.exe")  # Windows
    pyvenv_cfg = os.path.join(venv_path, "pyvenv.cfg")

    # 检测：python.exe 存在但 pyvenv.cfg 缺失
    if os.path.exists(python_exe) and not os.path.exists(pyvenv_cfg):
        _repair_pyvenv_cfg(venv_path)
        if os.path.exists(pyvenv_cfg):
            return True

    # venv 完全损坏：自动删除并重建
    if not os.path.exists(python_exe):
        # 清理损坏的 venv，调用创建脚本重建
        ...
```

> **注意**：此机制仅适用于开发模式（非 exe 打包）。exe 模式下跳过 venv 检测。

---

### 并发处理架构（线程池多源并行）

当节点有多个输入源（多个上游节点通过 `port_mappings` 连接）时，listener 必须支持**并发处理**，防止某个上游的处理阻塞其他上游的数据接收。

#### 设计原则

```
主循环（单线程，只做快速检查）
    │
    ├── mtime 检查 → 哈希去重 → filter 匹配 → processed 标记检查
    │
    ├── 通过后 → executor.submit(_process_one, src_path, raw_data)
    │               ↓
    │             线程池（ThreadPoolExecutor）
    │               ├── thread-1: main.py 处理 user_input 的数据
    │               ├── thread-2: main.py 处理 LLM 返回的数据
    │               └── ...
    │
    └── 主循环继续轮询下一个输入源（不等待线程完成）
```

- **主循环**：只做文件检测（mtime、哈希、filter），不阻塞
- **线程池**：`_process_one()` 在独立线程中执行 `main.py`，不影响主循环轮询
- **写锁**：`WRITE_LOCK` 保护 `output.json` 的写入和上游文件的 `_processed_` 标记，防止多个线程同时写同一个文件
- **线程数**：`max_workers = min(len(INPUT_SOURCES), 8)`，每个输入源最多占用一个工作线程

#### 关键实现

```python
import threading
from concurrent.futures import ThreadPoolExecutor

WRITE_LOCK = threading.Lock()
MAX_WORKERS = min(len(INPUT_SOURCES) or 1, 8)


def _process_one(src_path, raw_data):
    """在独立线程中处理单条数据"""
    try:
        input_data = unwrap_data(raw_data)

        # 执行 main.py（耗时操作，不阻塞主循环）
        res = subprocess.run(
            [resolve_python(), "main.py", json.dumps(input_data)],
            capture_output=True, text=True, timeout=300,
        )
        output = res.stdout.strip()

        # 写 output.json 加锁，防止并发写错乱
        with WRITE_LOCK:
            with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                f.write(output)

        # 标记已处理（同样加锁）
        raw_data[PROCESS_FLAG] = True
        with WRITE_LOCK:
            with open(src_path, "w", encoding="utf-8") as f:
                json.dump(raw_data, f, indent=2, ensure_ascii=False)

    except Exception as e:
        log(f"处理异常 [{src_path}]: {e}", "ERROR")


# ── 主循环 ──
with ThreadPoolExecutor(max_workers=MAX_WORKERS) as executor:
    while RUNNING:
        for src_path in INPUT_SOURCES:
            # ... 快速检查 mtime / hash / filter ...
            if not is_my_data(inner_data):
                continue
            # 提交到线程池，不阻塞
            executor.submit(_process_one, src_path, raw_data)
        time.sleep(0.1)
```

#### 线程安全约束

| 约束 | 说明 |
|------|------|
| **`process()` 必须是无状态的** | `main.py` 的 `process(data)` 不能依赖全局变量或模块级缓存，每次调用应是纯函数。如果必须保留状态（如对话历史），使用 `data` 参数传入 |
| **日志必须线程安全** | 使用 `threading.Lock` 保护日志写入，或使用 Python logging 模块的 `logging.Handler` 内置锁 |
| **写文件统一加锁** | 所有 `output.json` 写入和上游文件 `_processed_` 标记必须通过 `WRITE_LOCK` |
| **避免死锁** | `_process_one()` 内部不要再启动新线程或等待其他锁 |
| **Python GIL 问题** | I/O 密集型任务（API 调用、文件读写）不受 GIL 影响。CPU 密集任务（如本地 LLM 推理）可考虑 `ProcessPoolExecutor` |

#### 何时需要并发

| 场景 | 是否需要 | 说明 |
|------|---------|------|
| 只有 1 个输入源 | **不需要** | 单源串行即可，线程池增加开销 |
| 2-3 个输入源，处理很快 (<1s) | **可选** | 串行延迟可接受 |
| 2-3 个输入源，处理需 3-10s | **推荐** | 如 AAA 同时处理用户输入和 LLM 返回 |
| 3+ 个输入源（如 gui_bridge 收集各节点状态） | **必须** | 避免一个节点超时阻塞其他节点 |
| 单个 `main.py` 耗时长 (30s+) | **必须** | 如大模型推理、文件处理 |

> **注意**：并发模式下，`_processed_<node_name>` 标记和 `output.json` 写入必须加锁。同时在 `is_my_data()` 前快速过滤掉不需要处理的数据，减少不必要的线程创建。

### Node.js / JavaScript 并发处理

JS 节点（Node.js）由于**单线程事件循环**的特性，无需使用线程池——异步 I/O 本身不阻塞主循环。但需要额外的机制来防止同一数据被重复处理：

| Python (线程池) | JavaScript (异步) |
|----------------|-------------------|
| `ThreadPoolExecutor` | 直接 `async function` 调用，不 `await` |
| `WRITE_LOCK` 保护写文件 | Node.js 单线程，写文件本身是同步的 |
| `_trackers mtime` | 同 `_trackers mtime` |
| `_content_hashes` 哈希去重 | 同 `_content_hashes` |
| 无并发重复问题 | 需 `_inFlight` Set 跟踪正在处理的源 |

#### 核心差异

```javascript
// Python 版：主循环提交到线程池
executor.submit(_process_one, src_path, raw_data)

// JS 版：直接调用异步函数，不 await（不阻塞主循环）
processOne(srcPath, rawData);
// 注意：没有 await！主循环继续轮询下一个源
```

#### 防重复处理（_inFlight Set）

JS 版使用 `_inFlight` Set 跟踪正在处理中的输入源，防止主循环在异步处理完成前再次提交同一个源的数据：

```javascript
const _inFlight = new Set();

async function processOne(srcPath, rawData) {
    if (_inFlight.has(srcPath)) return; // 跳过
    _inFlight.add(srcPath);
    try {
        // ... 异步处理 ...
    } finally {
        _inFlight.delete(srcPath); // 处理完成后释放
    }
}
```

#### 注意点

- **不 `await` 处理结果**：主循环调用 `processOne()` 时不加 `await`，否则会阻塞轮询
- **单线程写文件无需锁**：Node.js 主线程是单线程的，`writeFileSync` 天然互斥
- **`execFile` 子进程不受影响**：虽然 Node.js 是单线程，但 `execFile` 启动的子进程在系统层面并行执行
- **错误隔离**：`processOne()` 内部必须用 `try/catch` 捕获所有异常，崩溃不影响主循环

### Python 监听器伪代码（含多源监听 + port_mappings + venv 自愈）

```python
import json, time, os, sys, subprocess, hashlib, shutil
from datetime import datetime

# ═══════════════════════════════════════════
#  NODE_DIR 解析
# ═══════════════════════════════════════════
NODE_DIR = os.path.dirname(os.path.abspath(__file__))
CONFIG_PATH = os.path.join(NODE_DIR, "node_config.json")
LOG_DIR = os.path.join(NODE_DIR, "logs")
os.makedirs(LOG_DIR, exist_ok=True)


def log(msg, level="INFO"):
    now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    line = f"[{now}] [{level}] {msg}"
    print(line)
    with open(os.path.join(LOG_DIR, "listener.log"), "a", encoding="utf-8") as f:
        f.write(line + "\n")


# ═══════════════════════════════════════════
#  配置加载
# ═══════════════════════════════════════════
try:
    with open(CONFIG_PATH, "r", encoding="utf-8") as f:
        cfg = json.load(f)
except Exception as e:
    log(f"配置加载失败: {e}", "ERROR")
    sys.exit(1)

UPPER_FILE = os.path.abspath(os.path.join(NODE_DIR, cfg["listen_upper_file"])) if cfg.get("listen_upper_file") else ""
OUTPUT_FILE = os.path.abspath(os.path.join(NODE_DIR, cfg["output_file"])) if cfg.get("output_file") else ""
NODE_NAME = cfg["node_name"]
MY_FILTER = cfg.get("filter", {})
PROCESS_FLAG = f"_processed_{NODE_NAME}"


# ═══════════════════════════════════════════
#  venv 自愈
# ═══════════════════════════════════════════

def _repair_pyvenv_cfg(venv_path):
    """重建缺失的 pyvenv.cfg"""
    cfg_content = f"""home = {os.path.dirname(sys.executable)}
implementation = CPython
version_info = {sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}
include-system-site-packages = false
base-prefix = {sys.prefix}
base-exec-prefix = {sys.prefix}
base-executable = {sys.executable}
"""
    with open(os.path.join(venv_path, "pyvenv.cfg"), "w", encoding="utf-8") as f:
        f.write(cfg_content)


def check_and_repair_environment():
    """检测并修复虚拟环境"""
    venv_path = os.path.join(NODE_DIR, "venv")
    if os.name == "nt":
        python_exe = os.path.join(venv_path, "Scripts", "python.exe")
    else:
        python_exe = os.path.join(venv_path, "bin", "python")
    pyvenv_cfg = os.path.join(venv_path, "pyvenv.cfg")

    # 检测 python.exe 存在但 pyvenv.cfg 缺失
    if os.path.exists(python_exe) and not os.path.exists(pyvenv_cfg):
        log("检测到 pyvenv.cfg 缺失，尝试自动修复...", "WARNING")
        try:
            _repair_pyvenv_cfg(venv_path)
            if os.path.exists(pyvenv_cfg):
                log("pyvenv.cfg 已重建")
                return True
        except Exception as e:
            log(f"重建 pyvenv.cfg 失败: {e}", "ERROR")

    # venv 完全损坏：自动重建
    if not os.path.exists(python_exe):
        log("检测到虚拟环境异常，尝试自动修复...", "WARNING")
        if os.path.exists(venv_path):
            shutil.rmtree(venv_path, ignore_errors=True)
        # 调用节点创建脚本重建
        ...

    return True


# 启动时执行环境检测
check_and_repair_environment()


# ═══════════════════════════════════════════
#  多源监听
# ═══════════════════════════════════════════

def find_project_root():
    """向上查找项目根目录（包含 pipeline.json）"""
    d = NODE_DIR
    for _ in range(5):
        if os.path.isfile(os.path.join(d, "pipeline.json")):
            return d
        d = os.path.dirname(d)
    return os.path.dirname(os.path.dirname(NODE_DIR))


def resolve_input_sources():
    """自动解析输入源文件列表。
    
    优先级：
    1. listen_upper_file
    2. port_mappings 字符串值
    3. pipeline.json edges
    """
    sources = []
    if UPPER_FILE:
        sources.append(UPPER_FILE)
    for port_name, mapping in cfg.get("port_mappings", {}).items():
        if isinstance(mapping, str):
            path = mapping if os.path.isabs(mapping) else os.path.abspath(os.path.join(NODE_DIR, mapping))
            sources.append(path)
    project_root = find_project_root()
    pipeline_path = os.path.join(project_root, "pipeline.json")
    if os.path.exists(pipeline_path):
        with open(pipeline_path, "r", encoding="utf-8") as f:
            pipeline = json.load(f)
        for edge in pipeline.get("edges", []):
            if edge["to"] == NODE_NAME:
                src_node = edge["from"]
                src_cfg = json.load(open(os.path.join(project_root, "nodes", src_node, "node_config.json")))
                src_path = os.path.abspath(os.path.join(project_root, "nodes", src_node, src_cfg.get("output_file", "./output.json")))
                if src_path not in sources:
                    sources.append(src_path)
    return sources


def unwrap_data(data):
    """解包包装格式数据"""
    if isinstance(data, dict) and "code" in data and "data" in data:
        inner = data["data"]
        if isinstance(inner, dict):
            return inner
    return data


def is_my_data(data):
    """注意力过滤（或关系匹配）"""
    if not MY_FILTER:
        return True
    for _port_name, criteria in MY_FILTER.items():
        if all(data.get(k) == v for k, v in criteria.items()):
            return True
    return False


def listen():
    INPUT_SOURCES = resolve_input_sources()
    last_mtimes = {s: 0 for s in INPUT_SOURCES}
    # 内容哈希缓存，避免 mtime 精度不够导致重复处理
    content_hashes = {}

    log(f"监听器启动，输入源 ({len(INPUT_SOURCES)} 个):")
    for s in INPUT_SOURCES:
        log(f"  - {s}")

    while True:
        try:
            for src_path in INPUT_SOURCES:
                if not os.path.exists(src_path):
                    continue

                current_mtime = os.path.getmtime(src_path)
                if current_mtime <= last_mtimes.get(src_path, 0):
                    continue

                with open(src_path, "r", encoding="utf-8") as f:
                    raw_data = json.load(f)

                # 内容哈希去重
                content_str = json.dumps(raw_data, ensure_ascii=False)
                content_hash = hashlib.md5(content_str.encode()).hexdigest()
                if content_hash == content_hashes.get(src_path):
                    last_mtimes[src_path] = current_mtime
                    continue

                last_mtimes[src_path] = current_mtime
                content_hashes[src_path] = content_hash

                # 注意力过滤（顶层 filter）
                if not is_my_data(raw_data):
                    continue

                # 防重复处理
                if raw_data.get(PROCESS_FLAG):
                    continue

                # 数据解包
                input_data = unwrap_data(raw_data)

                log(f"检测到匹配任务，开始处理 (source: {src_path})")

                # 调用 main.py 处理
                result = subprocess.run(
                    [sys.executable, "main.py", json.dumps(input_data)],
                    capture_output=True, text=True, timeout=300
                )

                if result.returncode == 0:
                    output = json.loads(result.stdout)
                    os.makedirs(os.path.dirname(OUTPUT_FILE), exist_ok=True)
                    with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                        json.dump(output, f, ensure_ascii=False, indent=2)

                    # 标记已处理
                    raw_data[PROCESS_FLAG] = True
                    with open(src_path, "w", encoding="utf-8") as f:
                        json.dump(raw_data, f, ensure_ascii=False, indent=2)

                    log("处理完成")
                else:
                    log(f"处理失败: {result.stderr}", "ERROR")

        except Exception as e:
            log(f"异常: {e}", "ERROR")

        time.sleep(1)


if __name__ == "__main__":
    listen()
```

### Node.js / JavaScript 监听器伪代码（多源并发版）

JS 监听器核心职责同 Python 版，但利用 Node.js 异步特性实现并发：

```javascript
const fs = require('fs');
const path = require('path');
const crypto = require('crypto');
const { execFile } = require('child_process');

const NODE_DIR = __dirname;
const cfg = JSON.parse(fs.readFileSync(path.join(NODE_DIR, 'node_config.json'), 'utf-8'));

const UPPER_FILE = cfg.listen_upper_file ? path.resolve(NODE_DIR, cfg.listen_upper_file) : '';
const OUTPUT_FILE = cfg.output_file ? path.resolve(NODE_DIR, cfg.output_file) : '';
const NODE_NAME = cfg.node_name;
const MY_FILTER = cfg.filter || {};
const PORT_MAPPINGS = cfg.port_mappings || {};
const PROCESS_FLAG = `_processed_${NODE_NAME}`;
const POLL_INTERVAL = 200;

// ═══════════════════════════════════════════
//  多源解析
// ═══════════════════════════════════════════

function findProjectRoot() {
    let d = NODE_DIR;
    for (let i = 0; i < 5; i++) {
        if (fs.existsSync(path.join(d, 'pipeline.json'))) return d;
        d = path.dirname(d);
    }
    return path.dirname(path.dirname(NODE_DIR));
}

function resolveInputSources() {
    const sources = [];
    if (UPPER_FILE) sources.push(UPPER_FILE);
    for (const [, mapping] of Object.entries(PORT_MAPPINGS)) {
        if (typeof mapping === 'string') {
            const p = path.isAbsolute(mapping) ? mapping : path.resolve(NODE_DIR, mapping);
            if (!sources.includes(p)) sources.push(p);
        }
    }
    // pipeline.json edges
    const projectRoot = findProjectRoot();
    const pipelinePath = path.join(projectRoot, 'pipeline.json');
    if (fs.existsSync(pipelinePath)) {
        const pipeline = JSON.parse(fs.readFileSync(pipelinePath, 'utf-8'));
        for (const edge of (pipeline.edges || [])) {
            if (edge.to === NODE_NAME) {
                const srcPort = edge.source_port || 'default';  // ★ 获取上游端口名
                const srcCfg = JSON.parse(
                    fs.readFileSync(path.join(projectRoot, 'nodes', edge.from, 'node_config.json'), 'utf-8')
                );
                // ★ 优先从 output_ports 中按端口名匹配实际输出文件
                let srcOutputFile = srcCfg.output_file || './output.json';
                for (const port of (srcCfg.output_ports || [])) {
                    if (port && port.name === srcPort && port.output_file) {
                        srcOutputFile = port.output_file;
                        break;
                    }
                }
                const srcPath = path.resolve(projectRoot, 'nodes', edge.from, srcOutputFile);
                if (!sources.includes(srcPath)) sources.push(srcPath);
            }
        }
    }
    return sources;
}

function unwrapData(data) {
    if (data && typeof data === 'object' && 'code' in data && 'data' in data) {
        const inner = data.data;
        if (inner && typeof inner === 'object') return inner;
    }
    return data;
}

function isMyData(data) {
    const keys = Object.keys(MY_FILTER);
    if (keys.length === 0) return true;
    for (const key of keys) {
        const criteria = MY_FILTER[key];
        if (!criteria) continue;
        if (Object.entries(criteria).every(([k, v]) => data[k] === v)) return true;
    }
    return false;
}

// ═══════════════════════════════════════════
//  输入源 / 状态
// ═══════════════════════════════════════════

const INPUT_SOURCES = resolveInputSources();
const _trackers = {};      // srcPath → lastMtime
const _contentHashes = {}; // srcPath → md5
const _inFlight = new Set(); // srcPath 正在处理中
for (const s of INPUT_SOURCES) _trackers[s] = 0;

// ═══════════════════════════════════════════
//  单源处理（异步并发，不阻塞主循环）
// ═══════════════════════════════════════════

async function processOne(srcPath, rawData) {
    if (_inFlight.has(srcPath)) return;
    _inFlight.add(srcPath);
    try {
        const inputData = unwrapData(rawData);
        // 调用 main.js（子进程）
        const result = await new Promise((resolve, reject) => {
            execFile(process.execPath,
                [path.join(NODE_DIR, 'main.js'), JSON.stringify(inputData)],
                { cwd: NODE_DIR, encoding: 'utf-8', timeout: 30000 },
                (err, stdout) => err ? reject(err) : resolve(JSON.parse(stdout.trim()))
            );
        });
        if (!result) return;
        fs.writeFileSync(OUTPUT_FILE, JSON.stringify(result, null, 2), 'utf-8');
        rawData[PROCESS_FLAG] = true;
        fs.writeFileSync(srcPath, JSON.stringify(rawData, null, 2), 'utf-8');
    } catch (e) {
        log(`处理异常 [${srcPath}]: ${e.message}`, 'ERROR');
    } finally {
        _inFlight.delete(srcPath);
    }
}

// ═══════════════════════════════════════════
//  主循环
// ═══════════════════════════════════════════

async function mainLoop() {
    while (RUNNING) {
        for (const srcPath of INPUT_SOURCES) {
            if (!fs.existsSync(srcPath) || _inFlight.has(srcPath)) continue;
            const mtime = fs.statSync(srcPath).mtimeMs;
            if (mtime <= (_trackers[srcPath] || 0)) continue;
            let rawData;
            try { rawData = JSON.parse(fs.readFileSync(srcPath, 'utf-8')); } catch (_) {
                _trackers[srcPath] = mtime; continue;
            }
            // 内容哈希去重
            const hash = crypto.createHash('md5').update(JSON.stringify(rawData)).digest('hex');
            if (hash === _contentHashes[srcPath]) { _trackers[srcPath] = mtime; continue; }
            _trackers[srcPath] = mtime;
            _contentHashes[srcPath] = hash;
            const inner = unwrapData(rawData);
            if (inner && typeof inner === 'object' && inner[PROCESS_FLAG]) continue;
            if (!isMyData(inner)) continue;
            processOne(srcPath, rawData); // 不 await → 非阻塞
        }
        await new Promise(r => setTimeout(r, POLL_INTERVAL));
    }
}

mainLoop();
```

---

## 五、处理逻辑（main）开发规范

`main` 是节点的业务逻辑入口，负责**单次**处理任务并返回结果。

### 接口规范

- **输入**：接收 JSON 字符串（从命令行参数传入）或从 stdin 读取
- **输出**：输出 JSON 字符串到 stdout
- **核心要求**：业务逻辑封装在 `MyNode` 类的 `process()` 方法中，顶层 `process(data)` 函数作为桥接。`process(data)` 必须是纯函数，**不依赖进程环境**（复合节点兼容）

### 复合节点兼容约束 ★

> `main.py` 在复合节点模式下会被 `importlib.import_module()` 加载到编排进程内直接调用，而非通过 `subprocess` 启动子进程。因此必须遵守以下约束：

#### 1. 所有文件路径必须以 `__file__` 为基准（强制）

```python
# ✅ 正确
import os
NODE_DIR = os.path.dirname(os.path.abspath(__file__))
config_path = os.path.join(NODE_DIR, "node_config.json")
model_path = os.path.join(NODE_DIR, "models", "qwen.gguf")

# ❌ 禁止 —— 依赖 os.getcwd()，复合节点模式下 cwd 是项目根目录
config_path = "node_config.json"
model_path = "./models/qwen.gguf"
log_dir = os.path.join(os.getcwd(), "logs")
```

| 语言 | 正确写法 | 禁止写法 |
|------|----------|----------|
| Python | `os.path.dirname(os.path.abspath(__file__))` | `os.getcwd()` / `"./xxx"` / `"xxx.json"` |
| Rust | `env!("CARGO_MANIFEST_DIR")` → 编译期，或 `std::env::current_exe()` → 运行时 | `std::env::current_dir()` |

#### 2. `process()` 函数必须可直接 import 调用

```python
# ✅ 正确：process 是顶层函数，可直接导入
def process(data: dict) -> dict:
    ...

# ❌ 禁止：process 依赖 sys.argv
def process():
    data = json.loads(sys.argv[1])   # 复合节点不传命令行参数
```

#### 3. 模块导入时不得有副作用

```python
# ❌ 禁止：import 时执行阻塞 I/O 或依赖 cwd
with open("node_config.json") as f:    # 依赖 cwd，import 时报错
    CONFIG = json.load(f)

# ✅ 正确：惰性加载
_config = None
def get_config():
    global _config
    if _config is None:
        cfg_path = os.path.join(os.path.dirname(__file__), "node_config.json")
        with open(cfg_path) as f:
            _config = json.load(f)
    return _config
```

#### 4. 不得在模块级别持有可变全局状态

```python
# ❌ 禁止：跨节点共享状态
SESSION_CACHE = {}   # 多个节点的 main 共享同一进程，会互相污染

# ✅ 正确：每次 process() 自包含
def process(data: dict) -> dict:
    cache = {}   # 局部变量，每次调用独立
    ...
```

#### 4.1 并发安全：`process()` 必须是纯函数 ★

> 当 listener 启用**线程池并发处理**时，`main.py` 的 `process()` 可能被多个线程同时调用。

```python
# ❌ 禁止：使用模块级可变状态（线程不安全）
_counter = 0
def process(data: dict) -> dict:
    global _counter
    _counter += 1                # 多线程下 race condition
    data["seq"] = _counter
    return data

# ❌ 禁止：在 process() 内修改共享文件路径（线程不安全）
def process(data: dict) -> dict:
    result = do_something(data)
    with open("shared_cache.json", "r+") as f:  # 多线程同时读写同一文件
        cache = json.load(f)
        cache[data["id"]] = result
        json.dump(cache, f)
    return result

# ✅ 正确：纯函数，不依赖外部状态
def process(data: dict) -> dict:
    # 所有状态通过 data 传入，结果通过 return 传出
    return {"processed": True, "input": data}
```

**并发安全检查清单**：
- `process()` 是否使用了全局变量？→ 改用 `data` 参数传入
- `process()` 是否修改了共享文件？→ 由 listener 的 `_process_one()` 统一处理（加锁）
- `process()` 是否调用了非线程安全的库？→ 在该库调用处加锁，或使用线程局部存储

#### 5. EXE 打包兼容约束 ★

> 节点通过 BNOS 编译为独立 exe 后，listener 会以 `import _bnos_main` 方式加载 `main.py` 模块并直接调用 `process(data)`，而非 `subprocess` 启动子进程。`__name__ == "__main__"` 代码块在 exe 模式下**永远不会执行**。

##### 5.1 `process()` 是唯一的调用入口

listener 只调用 `process(data)`，不传命令行参数、不读 stdin/stdout。因此所有业务逻辑必须在 `process()` 内完成或由 `process()` 调用。

##### 5.2 模块导入必须零副作用（强制）

`import _bnos_main` 时不应触发任何文件 I/O、配置读取或路径解析。这些操作一律放入 `if __name__ == "__main__"` 块内。

```python
# ✅ 正确：模块级只定义函数和常量，无副作用
import json

def process(data: dict) -> dict:
    return data.get("data")

if __name__ == "__main__":
    # 配置读取、路径解析仅在此处执行（直接运行 main.py 时）
    import os
    NODE_DIR = os.path.dirname(os.path.abspath(__file__))
    with open(os.path.join(NODE_DIR, "node_config.json")) as f:
        cfg = json.load(f)
    ...

# ❌ 禁止：模块级读取 node_config.json
NODE_DIR = os.path.dirname(os.path.abspath(__file__))
with open(os.path.join(NODE_DIR, "node_config.json")) as f:
    cfg = json.load(f)   # exe 模式下 Nuitka 解压路径异常，必定报错
```

##### 5.3 不要依赖 NODE_DIR / os.getcwd() / __file__

exe 模式下这些路径均不可靠（指向临时解压目录或不确定的 cwd）。若 `process()` 需要访问节点目录下的文件（如模型、资源），应由 listener 传入路径参数，而非在 `main.py` 中自行解析。

> **总结**：保持 `main.py` 模块级代码干净——只有 import、常量定义和函数定义。业务逻辑封装在 `MyNode` 类中，配置/路径/I/O 全部放在 `if __name__ == "__main__"` 里。这样无论直接运行、复合节点 import、还是 exe 编译，都能正常工作。

### Python 模板（推荐）

节点业务逻辑封装在 `MyNode` 类中，其他代码为框架基础设施，开发者不要修改。

```python
"""
节点名称 - 简短描述
"""
import sys
import json
import os


# ════════════════════════════════════════════════════════════════
#  ★ 开发者在此类中编写所有业务逻辑 — 其他代码不要修改 ★
# ════════════════════════════════════════════════════════════════

class MyNode:
    """
    节点的业务逻辑处理器。

    process() 是框架入口，由桥接函数 process(data) 自动调用。
    开发者只需在此类中填充业务逻辑，不要修改框架代码。
    """

    def __init__(self):
        """初始化业务状态（可选覆盖）"""
        pass

    def process(self, data: dict) -> dict:
        """
        框架入口。

        参数:
            data: 上游传入的 JSON dict

        返回:
            dict: 处理结果，会被包装为 {"code": 0, "type": ..., "data": ...}
        """
        # ═══ 在此实现你的业务逻辑 ═══
        result = data.get("data", {})
        # ══════════════════════════════
        return result


# ════════════════════════════════════════════════════════════════
#  框架桥接（开发者不要修改）
# ════════════════════════════════════════════════════════════════

NODE_DIR = os.path.dirname(os.path.abspath(__file__))
_node = MyNode()


def process(data: dict) -> dict:
    """框架入口，由 listener.py 或 __main__ 调用。"""
    return _node.process(data)


# ════════════════════════════════════════════════════════════════
#  __main__ 入口（仅直接运行 python main.py 时执行）
# ════════════════════════════════════════════════════════════════
if __name__ == "__main__":
    # 读取输入（支持 argv 和 stdin 两种方式）
    input_data = {}
    if len(sys.argv) >= 2:
        try:
            input_data = json.loads(sys.argv[1])
        except Exception:
            pass
    if not input_data:
        try:
            s = sys.stdin.read().strip()
            if s:
                input_data = json.loads(s)
        except Exception:
            pass
    if not input_data:
        print(json.dumps({"code": -1, "error": "no input"}, ensure_ascii=False))
        sys.exit(1)

    # 加载配置
    config_path = os.path.join(NODE_DIR, "node_config.json")
    with open(config_path, "r", encoding="utf-8") as f:
        cfg = json.load(f)

    # 执行业务 + 输出
    result = process(input_data)
    print(json.dumps({
        "code": 0,
        "type": cfg.get("output_type", "default"),
        "data": result,
    }, ensure_ascii=False))
```


### Rust 示例

```rust
use serde_json::Value;

fn process(data: &Value) -> Option<Value> {
    let input = data.get("data")?;
    
    // ═══ 在此实现你的业务逻辑 ═══
    let result = input.clone();
    // ══════════════════════════
    
    Some(serde_json::json!({
        "code": 0,
        "data": result
    }))
}

fn main() {
    let args: Vec<String> = std::env::args().collect();
    let data: Value = if args.len() > 1 {
        serde_json::from_str(&args[1]).unwrap()
    } else {
        serde_json::from_reader(std::io::stdin()).unwrap()
    };
    
    if let Some(result) = process(&data) {
        println!("{}", serde_json::to_string(&result).unwrap());
    }
}
```

### 输出格式

```json
{
  "code": 0,
  "type": "output_type",
  "data": "<处理结果>"
}
```

- `code`: `0` 表示成功，非 `0` 表示失败
- `type`: 从 `node_config.json` 的 `output_type` 字段读取，标识数据类型
- `data`: 处理结果，可以是任意 JSON 类型

### data_type 路由与 filter 必须同步 ★

这是节点开发中最容易被忽略的陷阱：**`node_config.json` 的 `filter` 通过 `data_type` 放行了数据，但 `process()` 的路由判断使用了不同的 `data_type` 来分流**，导致数据被 filter 接收后走错处理分支或直接丢弃。

#### 典型问题

一种常见错误是，上游节点（如 LLM）输出的 `data_type` 从 `"text"` 变更为 `"parsed"`，但节点只更新了 `node_config.json` 的 `filter`，忘记了 `main.py` 中 `process()` 的条件判断：

```python
# node_config.json filter（已更新 → 数据被正确接收）
"llm_response": { "data_type": "parsed", "source": "llm" }

# main.py process()（忘记更新 → 数据被丢弃 ❌）
if data_type == "text" and source == "llm":   # 永远不会匹配 "parsed"
```

后果：filter 通过了数据，但 `process()` 的 if/elif 链没有一个分支命中，最后返回 `{"status": "noop"}` 甚至 `None`。

#### 解决方案

```python
def process(self, data):
    data_type = data.get("data_type", "")
    source = data.get("source", "")

    # 确保 data_type 值与 filter 完全一致
    if data_type in ("text", "parsed") and source == "llm":  # ✅ 兼容新旧值
        return self._on_parsed(data)

    if data_type == "text":
        return self._on_text(data)

    # 未匹配到任何路由 → 记录警告
    return {"_port": "default", "status": "noop"}
```

#### 开发检查清单

- [ ] `node_config.json` 的 `filter` 中，该端口的 `data_type` 值是多少？
- [ ] 上游节点实际输出中，`data_type` 字段的值是多少？**以实际输出为准，不要靠猜**
- [ ] `main.py` 的 `process()` 中，路由到正确处理函数（`_on_parsed` / `_on_text` / `_on_tool_result`）的条件判断是否使用了**同一个 `data_type`**？
- [ ] 如果修改了上游节点的 `data_type` 输出格式，是否同步更新了下游节点的 filter 和 `process()`？

---

## 六、启动脚本规范

### start.bat（Windows）

```bat
@echo off
setlocal enabledelayedexpansion
if not "%1"=="--no-pause" (
    cls
    chcp 65001 >nul
    echo ======================================
    echo        BNOS Node Starter
    echo ======================================
    echo.
)
cd /d "%~dp0"

REM === 环境自愈（按需实现） ===
REM 检测运行时依赖，缺失则自动修复

REM === 后台启动 ===
if not "%1"=="--no-pause" (
    echo 后台启动监听程序...
    start /b "" <启动命令>
) else (
    start /b "" <启动命令> >nul 2>&1
)

REM === 写入 PID ===
timeout /t 1 /nobreak >nul
REM 用 tasklist 获取进程 PID
for /f "tokens=2" %%i in ('tasklist /fi "imagename eq <进程名>" /nh 2^>nul') do echo %%i > .pid

if not "%1"=="--no-pause" (
    echo 监听程序已在后台运行
    pause
)
```

### start.sh（Linux/macOS）

```bash
#!/bin/bash
cd "$(dirname "$0")"
NO_PAUSE=false
[ "$1" = "--no-pause" ] && NO_PAUSE=true

# === 环境自愈（按需实现） ===

# === 后台启动 + PID ===
nohup <启动命令> > /dev/null 2>&1 &
echo $! > .pid
[ "$NO_PAUSE" = false ] && echo "监听程序已在后台运行 (PID: $(cat .pid))" && read -p "按回车键退出..."
```

### 启动脚本关键规范

| 规范 | 说明 |
|------|------|
| 支持 `--no-pause` | GUI 调用时必须静默运行，不 `pause`/`read` |
| 环境自愈 | 检测依赖缺失时自动修复（安装 venv、编译等） |
| 后台启动 | Windows 用 `start /b`，Linux/macOS 用 `nohup &` |
| PID 记录 | 启动后写入 `.pid` 文件，内容为进程 PID（一行一个数字） |
| `.pid` 格式 | 文件只写一行一个数字，如 `12345` |

### 编码与缓冲设置

`start.bat`（以及 `run.bat`）**必须**设置以下环境变量，否则 Python 输出中文日志可能报 `UnicodeEncodeError`，或在文件重定向时日志不刷新：

```bat
set PYTHONIOENCODING=utf-8
set PYTHONUNBUFFERED=1
```

| 变量 | 作用 |
|------|------|
| `PYTHONIOENCODING=utf-8` | 解决 `chcp 65001` 下 print 中文的 `UnicodeEncodeError` |
| `PYTHONUNBUFFERED=1` | 禁用 print 缓冲，确保重定向到文件时日志实时刷新 |

> 如果不设置 `PYTHONUNBUFFERED=1`，`start /B python xxx.py > engine.log 2>&1` 时日志可能延迟数小时才写入文件。

### 引擎级进程清理（run.bat）

BNOS 使用引擎（`engine.py`）统一管理所有 node 子进程。引擎内部包含 `ProcessManager` 组件：

```python
class ProcessManager:
    """子进程生命周期管理器 — 注册所有 node 子进程，退出时统一清理。"""

    def register(self, node_id: str, proc: subprocess.Popen) -> None:
        self._procs[node_id] = proc

    def request_shutdown(self) -> None:
        """请求优雅关闭（由信号处理器调用）。"""
        self._shutdown_requested = True

    def shutdown(self) -> None:
        """杀死所有已注册的子进程。"""
        for node_id, proc in self._procs.items():
            try:
                proc.kill()
                proc.wait(timeout=5)
            except Exception:
                pass
```

`run.bat` 作为一键启动脚本，需要确保引擎退出后**所有 listener 进程也被清理**。推荐使用 PowerShell 命令（比 `wmic` 更可靠）：

```bat
REM PowerShell 匹配所有引擎 + listener 进程并强制杀死
powershell -NoProfile -Command "Get-CimInstance Win32_Process ^| Where-Object { $_.Name -like '*python*' -and ( $_.CommandLine -match 'bnos_runtime.engine' -or ( $_.CommandLine -match 'listener' -and $_.CommandLine -match 'node_' ) ) } ^| ForEach-Object { Stop-Process -Id $_.ProcessId -Force }" >nul 2>&1
```

引擎启动前也应杀死所有残留 listener，防止重复处理：

```python
def _kill_orphan_listeners(project_root: Path) -> None:
    """用 PowerShell 杀死项目中所有残留的 listener 进程。"""
    nodes_dir = project_root / "nodes"
    node_names = [d.name for d in nodes_dir.iterdir() if d.is_dir() and d.name.startswith("node_")]
    name_conditions = " -or ".join(f"($_.CommandLine -match '{n}')" for n in node_names)
    ps_cmd = (
        "Get-CimInstance Win32_Process | Where-Object {{ "
        "$_.Name -like '*python*' -and $_.CommandLine -match 'listener' -and ({0}) "
        "}} | ForEach-Object {{ Stop-Process -Id $_.ProcessId -Force }}"
    ).format(name_conditions)
    subprocess.run(["powershell", "-NoProfile", "-Command", ps_cmd], capture_output=True, timeout=15)
```

---

## 七、节点间通信协议

### 任务数据格式

上游节点通过监听文件（`listen_upper_file` 指向的 JSON）传递任务：

```json
{
  "type": "data_task",
  "data": { "key": "value" },
  "_processed_node_python_1": true
}
```

### 输出数据格式

节点处理完成后写入 `output_file`：

```json
{
  "code": 0,
  "type": "data_result",
  "data": { "processed_key": "processed_value" }
}
```

### 防重复处理

每个节点处理完任务后，在上游文件中写入 `_processed_<node_name>` 标记：

```json
"_processed_node_python_1": true
```

监听器在每次处理前检查此标记，若已存在则跳过。

#### 重要：防重标记检查对象必须为 `raw_data` ★

listener 在处理数据时，**防重标记必须检查 `raw_data`（读取的完整文件内容），而非 `unwrap_data(raw_data)` 解包后的内层数据**。

```python
# ✅ 正确：检查原始数据
if isinstance(raw_data, dict) and raw_data.get(PROCESS_FLAG):
    continue

# ❌ 错误：检查解包后的内层数据
inner_data = unwrap_data(raw_data)
if isinstance(inner_data, dict) and inner_data.get(PROCESS_FLAG):
    continue  # _processed_ 标记在外层，永远检测不到，导致重复处理
```

> **原因**：`_processed_<node_name>` 标记被写入在上游文件的顶层，而非数据的 `data` 内层。如果对 `unwrap_data()` 的结果进行检查，标记永远不会被找到，导致同一数据被无限重复处理。

### 多节点链式处理

```
upper_data.json
    ↓ [node_python_1] 过滤 type=data_task → 处理 → output.json
    ↓ [node_python_2] 监听 node_1 的 output → 处理 → output.json
    ↓ [node_python_3] 监听 node_2 的 output → 处理 → output.json
```

### port_mappings 数据路由（自定义接口）

BNOS Canvas 引擎通过 `pipeline.json` 的边关系 + `port_mappings` 配置实现节点间的**端口级别数据自动分发**。

#### 工作原理

```
[Canvas 引擎启动时]
    │
    ├── 读取 pipeline.json edges
    │   └── { "from": "node_A", "to": "node_B" }
    │
    ├── 查找 node_B 的 port_mappings
    │   └── { "input_port": "../node_A/output.json" }
    │
    └── 自动将 node_B 的 listener 指向 node_A 的输出文件
         ↓
    node_B listener 通过 resolve_input_sources() 自动监听
```

#### 配置方式

在 `pipeline.json` 中定义边关系：

```json
{
  "nodes": ["node_python_user_input", "node_python_aaa_cognition", "node_python_llm_infer"],
  "edges": [
    { "from": "node_python_user_input", "to": "node_python_aaa_cognition" },
    { "from": "node_python_llm_infer", "to": "node_python_aaa_cognition" }
  ]
}
```

在目标节点的 `node_config.json` 中配置 `port_mappings`：

```json
{
  "port_mappings": {
    "user_text": "../node_python_user_input/output.json",
    "llm_reply": "../node_python_llm_infer/output.json"
  }
}
```

#### 核心原则

| 原则 | 说明 |
|------|------|
| **无需手动配置 `listen_upper_file`** | Canvas 引擎启动时根据 `pipeline.json` 自动填充数据路由，`listen_upper_file` 可保持空字符串 |
| **listener 自动解析多源** | `resolve_input_sources()` 自动从 `port_mappings` 和 `pipeline.json` 收集所有输入源 |
| **端口名称仅做标记** | `port_mappings` 的 key 在 listener 层面不做过滤，所有源文件的数据统一处理 |
| **filter 负责数据筛选** | 多源数据统一进入 listener 后，由 `filter` + `is_my_data()` 决定哪些数据需要处理 |

#### 常见误解

```
❌ 错误理解：port_mappings 配置了，还需要再配 listen_upper_file
   → 不需要。port_mappings 已定义数据路由，Canvas 引擎会自动处理

❌ 错误理解：同一个文件被多个节点监听会冲突
   → 不会。每个节点有自己的 _processed_<node_name> 标记，互不干扰

❌ 错误理解：port_mappings 需要 listener 手动读取配置
   → 不需要。resolve_input_sources() 自动从配置中解析，listener 无需额外编码
```

#### 数据流示例

```
[user_input] 输出 output.json
    │  { "data_type": "chat", "source": "text", "content": "你好" }
    │
    ▼  port_mappings: { "user_text": "../node_python_user_input/output.json" }
[AAA 节点] listener 检测到文件变化
    │  is_my_data() 匹配 filter → 调用 main.py 处理
    │  输出 output.json: { "code": 0, "data": { "data_type": "reply", "content": "收到" } }
    │
    ▼  port_mappings: { "aaa_output": "../node_python_aaa_cognition/output.json" }
[LLM 节点] listener 检测到文件变化
    │  is_my_data() 匹配 filter → 调用 main.py 处理
    │  输出 output.json: { "code": 0, "data": { "data_type": "parsed", "content": "..." } }
    │
    ▼  port_mappings: { "llm_reply": "../node_python_llm_infer/output.json" }
[AAA 节点] listener 再次检测到新数据
    │  is_my_data() 匹配 filter → 继续处理...
```

### 上游输出协议定义 ★

当节点 A 的输出被节点 B 监听时，节点 A 的**输出格式**（`data_type` / `source` 字段的值）就是节点 B 的输入协议。这个协议必须明确定义，两端保持一致。

#### 协议定义

每个公共输出节点应明确约定其输出格式：

```python
# LLM 推理节点的输出协议
{
    "code": 0,
    "type": "default",
    "data": {
        "data_type": "parsed",     # 固定值：parsed（节标记文本）
        "content": "...",          # LLM 返回的原始节标记文本
        "source": "llm",           # 固定值：llm
    }
}

# 下游（AAA）按此协议配置
# node_config.json filter:
"llm_response": { "data_type": "parsed", "source": "llm" }

# main.py process() 路由:
if data_type in ("text", "parsed") and source == "llm":
    return self._on_parsed(data)
```

#### 约定规则

| 规则 | 说明 |
|------|------|
| **上游定义，下游适配** | 上游节点决定输出格式（`data_type` / `source`），下游按此开发 filter 和 process 路由 |
| **书面约定** | 节点 `README` 或注释中明确写出输出协议格式 |
| **变更通知** | 修改上游输出格式时，必须同步更新所有下游节点的 filter 和 process 路由 |
| **兼容过渡** | 允许 process() 中兼容新旧值（如 `data_type in ("text", "parsed")`），但应在 dev 日志中警告旧值已被弃用 |

#### 开发流程

```
开发节点 B（下游）时：
1. 查看上游节点 A 的 node_config.json → 确认 A 的 output_ports
2. 查看 A 的 main.py 返回结果 → 确认 data_type / source 实际值
3. 用这些实际值配置 B 的 filter
4. 用同样的 data_type 值写 B 的 process() 路由
5. 验证端到端数据流
```

---

## 八、开发检查清单

新建节点时请逐项确认：

### 基础配置

- [ ] 目录结构完整（node_config.json / start.bat / start.sh）
- [ ] `node_config.json` 中 `node_name` 与目录名一致
- [ ] `listen_upper_file` 路径正确（如使用 `port_mappings` 可留空）
- [ ] `port_mappings` 配置正确，路径指向上游节点的 `output_file`
- [ ] `filter` 规则配置合理（空 `{}` 或按多端口格式配置）
- [ ] `output_file` 路径存在写入权限
- [ ] **多端口输出**：`output_ports` 中每个端口正确配置了 `output_file`
- [ ] **main.py 返回 `type` 字段**：当需要路由到非 default 端口时，返回结果包含 `type: "<port_name>"`
- [ ] **listener 写路由**：已实现 `OUTPUT_PORTS` 映射 + 按 `type` 字段路由写入逻辑

### 监听器（listener）

- [ ] 监听器持续运行，不主动退出
- [ ] 实现 `resolve_input_sources()` 自动解析三类输入源
- [ ] ★ `resolve_input_sources()` 中实现路径规范化去重（`os.path.normcase()` 去重，防 Windows 大小写路径重复）
- [ ] ★ `resolve_input_sources()` 从 `pipeline.json` 解析上游路径时，必须根据 `edge["source_port"]` 匹配上游节点的 `output_ports`，获取正确的 `output_file`，而非直接使用顶层 `output_file`（否则多端口输出场景下会监听错误的文件，详见 **多端口输出场景的监听陷阱** 章节）
- [ ] 支持**多文件轮询**（同时监听多个输入源文件）
- [ ] 内容哈希去重（`hashlib.md5` 避免 mtime 精度问题导致重复处理）
- [ ] 实现 `unwrap_data()` 自动解包 `{"code":0, "data":...}` 格式
- [ ] 实现 `is_my_data()` 注意力过滤（或多关系匹配，注意 JS 版需修复为 AND-within-port 逻辑）
- [ ] ★ 实现 `_processed_<node_name>` 防重复处理标记，**必须检查 `raw_data` 而非 `unwrap_data(raw_data)`**
- [ ] 监听器异常不崩溃，记录日志后继续
- [ ] 实现 `find_project_root()` 向上查找 `pipeline.json`
- [ ] 日志写入 `logs/listener.log`
- [ ] 启动时写入 PID 文件（`node_name.pid`），注册 `atexit` 清理
- [ ] 注册 `SIGINT`/`SIGTERM` 信号处理器，设置 `RUNNING=False` 优雅退出

#### Python 监听器额外项

- [ ] 多输入源时实现**线程池并发处理**（`ThreadPoolExecutor`）
- [ ] 实现 `WRITE_LOCK` 保护 `output.json` 和 `_processed_` 标记的写入
- [ ] 主循环只做快速检查，耗时处理提交到线程池（不阻塞轮询）
- [ ] 线程数合理设置：`min(len(INPUT_SOURCES), 4)`（推荐 4，避免过多线程）
- [ ] 主循环 `while RUNNING` 使用全局标志，信号处理器中设为 `False`
- [ ] 同时支持 `subprocess` 调 `main.py` 和 exe 模式 `import _bnos_main` 直接调用

#### Node.js / JavaScript 监听器额外项

- [ ] 实现 `_inFlight` Set 防止同一源重复提交（主循环调用 `processOne()` 不加 `await`）
- [ ] 异步函数 `processOne()` 内部用 `try/catch` 隔离错误，崩溃不影响主循环
- [ ] `is_my_data()` 使用 `Object.entries().every()` 实现 AND-within-port 逻辑（而非 OR-within-port）

### venv 自愈

- [ ] 启动时检测 `pyvenv.cfg` 是否存在
- [ ] 实现 `_repair_pyvenv_cfg()` 重建缺失的 pyvenv.cfg
- [ ] venv 完全损坏时自动清理并重建
- [ ] exe 模式下跳过 venv 检测

### 处理逻辑（main）

- [ ] `main` 接收 JSON 输入，输出 JSON 到 stdout
- [ ] 输出格式符合 `{"code": 0, "type": "...", "data": ...}` 规范
- [ ] ★ 所有文件路径以 `__file__` 为基准（禁止 `os.getcwd()` / `./xxx` 相对路径）
- [ ] ★ 业务逻辑封装在 `MyNode` 类中，`process(data)` 为顶层桥接函数
- [ ] ★ 模块 `import` 时无阻塞 I/O、无 cwd 依赖副作用
- [ ] ★ 无模块级可变全局状态（每次 `process()` 自包含）
- [ ] ★ 启用并发时，`process()` 必须是纯函数（无全局变量、无共享文件修改）
- [ ] ★ 并发模式下，文件读写由 listener 的 `_process_one()` 统一加锁处理
- [ ] ★ EXE 打包兼容：模块级代码无副作用，配置读取只在 `__main__` 块内
- [ ] ★ `process()` 不依赖 `NODE_DIR` / `os.getcwd()` / `__file__` 解析路径

### 启动脚本

- [ ] start.bat 支持 `--no-pause` 参数
- [ ] start.bat 后台启动 + 写入 `.pid`
- [ ] start.sh 后台启动 + `echo $! > .pid`
- [ ] `.pid` 文件内容为一行一个数字
- [ ] start.bat 设置 `PYTHONIOENCODING=utf-8` 和 `PYTHONUNBUFFERED=1` 解决编码/缓冲问题
- [ ] run.bat 中引擎退出后使用 PowerShell 杀死所有引擎和 listener 进程

### 进程清理（run.bat）

- [ ] GUI 关闭后，使用 PowerShell 命令杀死所有匹配 `bnos_runtime.engine` 和 `listener` 的 Python 进程
- [ ] 引擎启动前杀死所有残留的 listener 进程（防止重复处理）
- [ ] 启动脚本使用 `Get-CimInstance`（比 `wmic` 更可靠，支持 Python 3.14+）

### 版本控制

- [ ] `.gitignore` 排除 `venv/`、`target/`、`logs/`、`.pid`、`output.json`
- [ ] `node_config.json` 中 `entry` 字段指向有效脚本文件

---

## 九、相关文档

| 文档 | 用途 |
|------|------|
| [config_json 开发规范](config_json_开发规范.md) | `node_config.json` 参数控件/输入输出端口的详细配置 |
| [节点生成器开发准则](节点生成器开发准则.md) | 开发新语言节点生成器的规范（Go/Java/C++等） |
| [Node_Generator_Guidelines_EN.md](Node_Generator_Guidelines_EN.md) | 节点生成器准则英文版 |
