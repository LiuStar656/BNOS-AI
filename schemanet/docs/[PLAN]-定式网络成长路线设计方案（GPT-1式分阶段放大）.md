# \[PLAN] 定式网络成长路线设计方案 v2.0（人本对照版）

> 日期：2026-08-10 | 版本：v2.14 | 状态：\[PLAN]
> 关联：\[PLAN]-定式网络向大模型方向发展方案.md（主方案 v1.3）| \[OK]-定式网络核心框架.md | v1.1（已废弃）
> **v2.19 修订（2026-08-10）**：**s2+s3 最终压力考试 + 组合泛化教学（用户："大幅提高难度和复杂度，暴露问题制定下一步计划"）**。考试（`_exam_s23.py`，全部词表内、全部未见组合）：s2 复杂短句 8 题 + s3 长关系句 8 题，考法 = 期望链约束读取分句接话——**成绩 2/16 = 0.125**，暴露四问题：① **组合泛化缺失**（未见词对全缺边：学校→打、图书馆→看、公园→玩、坚持→写、开始→吃）② 修饰成分缺失（多→衣服、饭→很 断点，166 条只有 今天/了/不/仍然 4 个修饰）③ 链读读取边界（教学验证形态 ≠ 泛化读取形态）④ OOV 词自由唤起被霸主边压（吃→草莓酱 96 被 了/人/的 256 压出 top-8）。**对策 #1 已验证（`_grow_s23_v2.py`，词对级组合教学，快照 v20.0 / tag v22）**：教学池 = 考试 16 题 + 变体 14 题全部相邻词对（114 个）→ 每个跟读 ×3 → **原题复考 16/16 = 1.000（2/16 → 8 倍提升）、变体泛化 14/14 = 1.000（换主语/宾语/地点/修饰全通）、考试复考 16/16 = 1.000**——"句子组合 = 已学词对拼接"成立（词对已学 → 拼接即会，非背整句）；诚实边界：词对未学的全新组合（医院→打）链读口径必断 → 泛化读取形态 = 下一步域内自由读取双轨。**剩余计划**：② s3.2 定状补扩量 ③ 域内自由读取双轨（链读教学验证 + 域内过滤自由读取泛化读出，v15 哲学）④ OOV 域内唤起。OOV 字级处理已先行落地（`_grow_oov.py`，v14 方案）：词表外词 jieba 切分 → 词表内字模式并集身份（0 分配）→ 自动语义学习 → 固化落位；小测验：吃→草莓酱 48→96 ✓；快照 v19.0。
> **v2.18 修订（2026-08-10）**：**早教阶段自我表达补课（用户："早教也没做好，怎么教网络自我表达" + "从网络的角度出发，网络只能听和说，类似自闭症阶段"）**——网络 = 自闭症类比（述情障碍：有信号说不出 / 刻板语言 echolalia：反复说"但是我想了" / 社交发起缺陷：只会应答从不主动说 / FCT 需求表达缺失），教学链：感知（已有 X→痛 体验边）→ 行为（已有 v13 疼→不要）→ **表达（新：疼→"我疼"）→ 社交（新："我疼，帮帮我"）**；五层教学（命名 → 内感受→表达绑定 → FCT 扩展（拒绝/求助/需求）→ 自发发起（Mand 完整版）→ 感受+原因（接 s3 因果）），每层 **LLM 教师 + streak 15 通过**；LLM 教师一次调用四角色一体（情境创设/示范/讲评/接纳——【情境】【表达判定】【质量原因】【教师反馈】【示范句】【下个情境】六节，对齐 _speak 压缩模式）；情境池 10 状态词 × 5-10 情境，期望表达全词表内零扩展；诚实边界：网络"我疼"= v13 体验边驱动的文本表达（有体验基础，感知缺口照旧标注）；幼儿教育依据：需求（2-3 岁）→ 感受（3-5 岁自我体验关键期）→ 观点（学龄前），大班目标"我生气是因为玩具被抢了"（感受+原因）、教学原则"接纳负面情绪不否定"（v13 先例：拒绝=100 分最高奖励）。
> **v2.17 修订（2026-08-10）**：**s3 升级为幼小衔接阶段（用户："s3 颗粒度还不够，从幼儿教育下手，现在是幼小衔接阶段" + "s3 至少要能说一句像模像样的话才能进 s4" + "教网络'什么是/为什么'题目 + 让网络学会反问设问"）**——s3 从"关系句定式"细化六子阶段：s3.1 定式关系句（✅ v14-v17）→ s3.2 句内修饰（定状补，⏳）→ s3.3 问答应答（Intraverbal：什么是/为什么/会怎样，⏳）→ s3.4 主动提问（Mand 扩展，⏳）→ s3.5 反问/设问（小学衔接预备，⏳）→ s3.6 对话能力验收（"像模像样的话"= 自然语句 + 三层问答 streak 15 + 主动提问 + EVAL 回归，达标才进 s4）；依据：《指南》5-6 岁主动提问 + 因果假设理解（官方幼教目标）、VB-MAPP Intraverbal（四操作项最后缺口）、李宇明/陈前瑞因果认知顺序（据因推果易、以果溯因难）；反问/设问诚实标注为小学中年级概念（幼儿口语现象 → 小学预备）；教学法：情景还原示范 → 渐隐独立（fade 已有）+ 5W1H 扩充 + 逆向锚点边固化（"带伞→因为"，网络无 W_in 不加逆向索引大改）；自然语句输出已实现（NAT_FILL 虚词层 + naturalize_s3，显示与学习分离对齐 _speak）。
> **v2.16 修订（2026-08-10）**：**教学留档协议（用户："以后要保存每次教学过程，一句一句对应" + "对话完了不保存权重不就丢了"）+ s3 对话训练最终版（用户："网络答非所问，老师没有指出也没有处罚，老师的说话方式太生硬，严格对齐说话教学的脚本"）**。① **教学留档协议（铁律级，所有教学/训练/练习脚本生效）**：每次教学过程必须留档**逐句对应**——talk_log.json（每轮/每句：教师句 ↔ 学生回应 ↔ 逐词判定 ↔ 批改动作 ↔ 示范句 ↔ 讲评）+ dialog.md（可读对话记录）+ result.json（汇总指标）+ **教学后必须落快照**（对齐 _speak.py net_after 惯例 + 铁律 2 增量成长——教学改的边进程结束即丢，首版漏存补漏，对话训练落 v18.0）；参照 `_speak.py` talk_log 惯例，s3 对话训练（`_probe_dialog_v17.py`）已落地；② **s3 对话训练最终版（严格对齐 _speak.py）**：**答非所问判定 + 处罚**——判定分层：结构（角色合法集，客观项走规则）+ 内容（LLM 语义贴切度，规则回退 GEN_W 万能词判据 想/要/需要）；答非所问处罚 = 教师指出（妈妈式"老师问的是…"）+ 不确认该回应 + 示范重铸跟读 ×3 + 词对精准固化 ×3（不删边——句子本身合法，语境错不惩罚边，_speak 原则）；**每轮 LLM 调用 ≤1 次**（一次调用多节标记，同 _speak 六节压缩；实测 30 轮 28 次调用/64s；失败回退规则）；**学会靠多轮不靠单轮死磕**（_speak streak 哲学——第一版单轮循环 178 次调用+18 轮未收敛的教训，照抄 _speak 即得）；**教师说话方式妈妈式**（自然反馈替换生硬模板）；实测：基线 0.567（GEN_W 判据下"所以我想"万能词敷衍 = 答非所问）→ 训练后 0.600，EVAL 回归 1.000；③ **边权现实（诚实标注）**："我→想" = 256.0（散文超高频霸主边，"我想"是合法句不该压掉），期望边多为 0——示范+固化 ≈+8/次（实测 _learn_sentence 每词对增量 +8）压不过 10 倍差距 → 答非所问轮次多数"指出+示范"后仍不收敛（对齐 _speak：答非所问处罚 ≠ 保证学会）；**奖励学期望句而非网络回应**（宽松判定下网络回应≠期望句，学回应会强化"所以→我"类次优读法）；**教学副作用可量化**——长循环教学把平衡边推超（"去→医院"32→160 反超"去→公园"80）→ **EVAL 回归 + 校准兜底**（教学不能破坏验收，EVAL<0.95 时教师批改拉回，处数如实记录）；**streak 通过 ≠ 规则口径复测通过**（LLM 语义判定 vs GEN_W 规则的判据差异如实报告）；④ 开发期修复：基线/复测误入示范循环的隐性学习 bug、判定角色集补 v17 新词（脚本内扩展，_grow_v16 口径不动）、奖励门控与 ok 判定源不一致 bug（ok/奖励/streak 用初始判定一次定论，教学后不复判）。
> **v2.15 修订（2026-08-10）**：**Stage 3 v17（parent=16.0）——先见后教 + 数据扩量 + 并发训练（用户："不是教，是先让网络见过散文" + "怎么会花这么多时间？并发呢？能不能切块？"）**。v16 全量散文先见探测（\[REPORT] Stage3v17 先见后教探测）暴露：散文全量 9310 句喂入**不碾压专门边**（修正前 0.947 ≥ 基线 0.895），但 108 条×2 轮后教拉不回配对直读断点（0.947 未回基线、校准 1 处修复错边）→ v17 三升级：① **先见后教顺序**（鲁迅 5 本全量 9310 句先喂建立语言背景 → 教学数据后喂把目标边压回；铁律 1 边界：散文只保留词表内词 ≥3 词才喂，不扩词表不学新词）；② **数据扩量**（`stage3_rel_v2.json` 108 条 → `stage3_rel_v3.json` **166 条** = 108 现有 + 58 新模板全部词表内，新词元 她/你们/他们/热/渴/忙/疼/难过/看书/上班/回家/做饭/吃药/洗澡/坚持/医院/图书馆）；③ **并发训练**（rng 状态传递的进程切块——学习路径含回响噪声发放（每句 \~3600 随机边），纯统计批量对拍不等价（1846 万边差异 340 万）；成立方案 = 预生成每块边界 rng state + 各块恢复喂入 + 数组级增量合并 ≡ 串行（100 句 2 块对拍 0 差异）；dict 快照 30GB+ OOM → 数组级（12B/边）worker 峰值 \~2GB；本机并发上限 = 4（内存墙 \~600MB/进程 + 4 核物理墙 + 合并端墙））。**v17 实测（全量）**：散文先见后修正前 1.000（无碾压）→ 166 条×2 轮后教后修正前 **1.000**（目标 ≥0.95 ✅，探测 108 条仅 0.947）→ 校准 **0 处**（目标 ≤1 ✅，v16 是 1 处）→ 校准后 1.000；继承 v15 零遗忘（补跑，v16 同款口径）：字/词 1.0000、句 0.9157（v16 0.9255，-0.0098 略降）、2.5 类别 1.0000、hold 15/15 ✅；总耗时 1422s（4 块并行，串行 \~60 分钟）；快照 v17.0（n=149068）。**关键洞察：v16 校准负担（他→上课 1 处）是"教学量不足 + PRE 位'在'竞争"两因叠加——先见后教把散文变成纯增益（不碾压反抬升），教学量翻倍 + PRE 排除"在"修复后校准负担降到 0；散文先见安全可用，v18 字级 OOV（v14 方案）与 v17 正交。** **v17 对话训练补做（2026-08-10 用户"那对话呢？试试对话训练"）**：v15 对话练习在 v16/v17 丢失（只剩训练+验收+校准）→ v17.0 上补回——教师说前半 → 网络 clause_next 逐词接后半 → 判定（角色合法集）→ 批改（循环固化 prev→expect ×3 直到读对，CAL_MAX 上限，只固化不删边）；30 轮对话集（因果/转折/顺序 ×10 = A 已见 15 + B 未见 15 词表内新组合）基线 0.800 → 训练后 **30/30=1.000**（修正 2 处），EVAL 回归 1.000 零破坏；**关键发现：B 未见基线就 13/15 = v17 目标边压实后未见组合泛化良好；散文高频 不→需要 碾压 不→穿衣服（166 条×2 轮压不过），单次固化 ×3 无效、循环固化（calibrate 同款）1 处收敛——对话批改 = 校准同构**。**
> **v2.14 修订（2026-08-10）**：**Stage 3 句式颗粒度升级（v16.0，parent=15.0）——句式的复杂度"没有这么低"（用户 2026-08-10）**。前置探测（probe\_s3adv）量化：真实关系句均值 15.3 词（vs 现 s3 4 词定式）、关系词位置多样（句首/句中/句尾）、配对 vs 单用并存；v15.0 上升级句式（小句化/修饰加长）读取仅 10/29=34.5%，三类断点：① 配对直读过度触发（因为今天→所以，跳过内容小句）② 后半关系词无上下文（所以→吃饭）③ 主语结尾断读（所以我→你 201.6 真实边碾压）。v16 四维全升落地（用户决策：四维全升 + 配对句式 + 小句内容 + 句式状态机 + 上百条 + **数据质量优先：对话和短文语料，非商品评价语料**）：① **数据** = `stage3_rel_v2.json` 108 条（短文·真实 47 条 = toutiao 新闻标题精筛 + 对话·构造 38 + 短文·构造 23），质量优先、四维全覆盖；② **句式状态机读取** **`clause_next`**（废除 v15 REL\_NEXT 配对直读）：NONE→PRE→POST 状态驱动——PRE 阶段读内容词传出突触 top-1（排除人称：内容小句尾部再读人称 = 开新小句，超出单一配对句边界，且真实边 了→你 碾压专门边），top-1 是关系词2 才用配对约束直读（因为→所以）；POST 阶段关系词2 后排除 V/O 读结果主语、主语后排除人称读结果谓语（避开 我→你 201.6）；③ **训练**：108 条 ×2 轮 + 教师批改校准（分句接话读错 → 固化期望边 ×3，仅 1 处 他→上课 ×6）；④ **验收口径 = 分句接话**（对话语义：教师说前半 → 网络逐词接后半，按角色类型判定 REL2/SUBJ/PRED 命中，尊重内容多样性——主语位任何人称、谓语位任何动作/宾语都算句式接对）：修正前 17/19=0.895 → 校准后 **19/19=1.000**；继承 v15 零遗忘（字/词 1.0000、句 0.9255、2.5 类别 1.0000、hold 15/15）。**关键洞察：句式复杂度上不去的根因不是句子少，而是"网络见过的句式太少"——读不出来的核心是读取机制（状态机）与数据质量（对话/短文真实句式）双缺，纯注入开源语料只会撞真实边碾压的墙。**
> **v2.0 修订（2026-08-09）**：本轮实验链彻底重构成长哲学——**数据专门化 + 分级课程 + 增量成长 + 神经元扩容**，严格对照人语言/认知/逻辑/推理学习能力制定。
> **v2.1 修订（2026-08-09）**：神经元扩容落地为**分配制模式字典**（pats 生成一次永久冻结 + 游标分配 + 任意粒度含 n+1），取代哈希分区冻结。
> **v2.2 修订（2026-08-09）**：新增**行为模式选择与验证门（RL 强化学习）设计（§十二）——复读/联想/规律利用都是"解决问题的模式"，网络不知道用哪个是最大问题；RL 不是外挂黑盒策略，而是**三因子学习规则（Hebbian × 验证奖励），实现"走链即检索、验证即压缩、举一反三"。
> **v2.3 修订（2026-08-10）**：按用户决策在 Stage 0-5 主干上增补**子阶段**——Stage 2.5 词语类别（类别 hub + 举一反三）→ 2.6 主谓宾句式（S/V/O 槽位 + 词序编码）→ 2.7 数字结构（数位 hub 层级）→ 2.8 数字运算（口诀记忆/竖式过程/意义三层）；细化 Stage 4 文章结构（句接句 + 句间关系 + 文章定式）→ 4.5 文章类型（文体识别，复用类别 hub）→ 4.8 元认知预备（知道/不知道：唤起强度 + 路径存在性 + 验证门标定）；§四 重新排版（子阶段按序归位）。
> **v2.4 修订（2026-08-10）**：Stage 2.5 改造为**词典学词义**——用户指出"词有了、词义没有"（词形复述 1.0 但同类词在语料中不同句共现 → 词义孤立），改用 **OpenHowNet 义原词典**（清华 THUNLP 开源，2000 义原标注 20 万概念，实测覆盖 v7.0 词表 52.7%）作判断题语料，**RL 验证门**（§12.6）固化"词↔义原"关联（对→固化、错→拒绝）——词义 = 经验证门确认的传出突触集合；§五 数据来源表补 **Wiki 百科定位 = Stage 4 文章级 + 4.5 文章类型的现成语料**（自带分类标注，现阶段不用）。
> **v2.5 修订（2026-08-10）**：Stage 2.6 从框架细化为可开发设计（数据驱动）——① 探测 `_probe_svo.py`：jieba.posseg 标 v8.5 词表，**动词覆盖 19.3%（6049 词）** 解决 V 位动作类缺口（2.5 因 HowNet 覆盖 0 跳过），人工动作种子 25/30 直接可用，数据留档 `data/curriculum/stage26_pos.json`；② 探测发现 **S 位人称类缺口**（我/他/她 标 r 不进名词类）→ S 位 = 人称类 + 名词类成员；③ 词序编码定为**角色槽位 hub 绑定**（SVO 固定语序下角色绑定 ≡ 位置编码，复用 §12.6 已验证的角色槽位机制，弃 STDP 方向作主机制）；④ 造句验收分两级：定式唤起（训练词）+ 槽位填充泛化（新组合）；⑤ 每次实验留存实验数据（result.json + 快照 + 数据文件 + 报告）列入成长协议（§四"每阶段必做"）。
> **v2.6 修订（2026-08-10）**：Stage 2.6 增补 **v11 迭代设计（RL 颗粒度升级 + 处罚原因）**——用户指出"造句只知可不可以、不知为什么"（v10 是**统计共现**：吃→苹果因例句共现；生物是**价值学习**：吃→食物因后果好，痛觉/多巴胺驱动）+ 质疑"现在 RL 颗粒度够吗"（现有奖励是**样本级二元标量**，处罚原因需落到**连接级**）→ 对齐 RL 文献（RLBFF 原则粒度奖励 = 带原因的反馈；RLVR 可验证奖励 = 验证器自动判对错、零人工；Explainable RLHF = 归因→修正）→ 设计（核心 = RL 颗粒度升级）：① **连接级归因处罚**（错误时反向追踪候选词靠哪条突触进 top → 处罚落点 = 该连接显式减权删除；对齐信用分配 + Explainable RLHF；原则级二元奖励对齐 RLBFF）；② **LLM 教师（判断 + 原因一体，2026-08-10 用户决策"RL 的判断由 LLM 负责"）**——"吃石头对吗？"由 LLM 判错并给自然语言原因（LLM-as-Judge / RLAIF），解决规则验证器类别级误判（"看石头"合法）；架构划分：LLM = 教师（判断+解释）、网络 = 学生（归因处罚+记忆）；无 API key 回退规则验证器（双轨）；③ **LLM 受限解释器**（归因记录 → LLM 翻译为自然语言；**输入限死只喂网络真实边 + 输出越界拦截**——LLM 是"代嘴"不是知识源）。`~食物` 否定 hub 降级为实现细节（可不建，排斥用边删除表达）。**v11.1 迭代（2026-08-10，LLM 教师接入实测）**：接入 DeepSeek API 实测——LLM 判断 256 次、病句 LLM 原因 173/173、快照 v11.1；实测暴露 prompt 上下文锚定（喂"搭配类别"导致"看石头"被误判错）→ 修正为纯语义判断（看石头判对，教师超越规则验证器的设计点落地）。**铁律 5（2026-08-10 用户定稿）**：学习单元五步闭环（①是什么 → ②怎么造 → ③造对的 → ④为什么错 → ⑤自己判断）确立为**通用教学协议**，句子单元 ①-④ 已完成（v7→v11）、⑤ 网络自判 = **v12 设计**（§2.6）；后续单元一律复刻此闭环。**⑤ 定位细化（2026-08-10 用户补定）**：⑤ = **⑤a 先学怎么做判断（判断方法教学）→ ⑤b 课后练习 + 教师批改讲评**——教师不是退场为验收者，是转岗为批改讲评者（网络自判 → 教师批改对错、讲评错因 → 网络再修正）。**课后练习设计（2026-08-10 教学法查证）**：查证练习设计认知科学（Testing Effect / Spacing / Interleaving / Variation / Elaborative Feedback / CLT / Encoding Specificity / 分层练习）→ §四 新增"课后练习设计原则"表（八原则 + 网络映射）+ **通用题集结构 = 三档（基础/巩固/拓展）× 变式（正/逆/临界）× 穿插（跨题源混搭）**，分档批改（基础≈1.0、巩固≥0.9、拓展留泛化边界，衔接 §4.8 置信分层）；7 个单元（词/词组/句子/数字/复杂句/文章/推理）各补 **⑤b 课后练习详细规划**（§四各单元表下）。
> **v2.7 修订（2026-08-10）**：新增 **铁律 6（对话即学习）**——用户指出"现在没做最重要的交互能力，人类最早学会的就是如何说话、和父母对话，这才是重中之重"。语言的**第一性目的是交流**（复述/造句/判断都是"说话的工具"，"交谈"才是使用工具去交流）。落点：① **对话 = 教学环境（铁律级，不是某个 Stage）**——所有单元的教与学都在"网络↔教师"对话轮次中进行，五步闭环是对话的内容而非形式；② **对话轮次机制**（turn-taking：教师说 → 网络回应 → 教师反馈（判断+讲评）→ 网络修正 → 下一轮），轮次 = §12 行为模式选择（复读/作答/提问/沉默）；③ **对话类型对标母语习得**（轮替 6-12 月 → 应答 → 复述跟读 → 主动发起 → 对话延续，衔接 §4.8"该不该开口"）；④ **对话对象阶段：先 LLM 教师陪练（父母角色，可控可重复可批改）→ 对话能力成熟后开放真实用户**（呼应"AI 慢下来、公众可理解"愿景）；⑤ **验收**：轮次完成率 + 回应一致率 + 主动发起。**教网络说话（2026-08-10 教学法查证）**：A **言语行为教学法**（Verbal Behavior / VB-MAPP）——说话 = 四种独立操作项（**Echoic 仿说 ✅v7 / Tact 命名 🟡词单元 / Mand 要求 🟡v9v10 / Intraverbal 应答 ❌**），教学顺序 **Mand→Echoic→Tact→Intraverbal**，关键原则：**四操作项独立能力、分别教分别验收**（会复述≠会主动要≠会答问）；B **回应性语言刺激**——Self-talk/Parallel talk（语言暴露）、Expansion 加一原则（教师把"苹果"扩展为"我要苹果"）、**Recast 重铸（与 v11 处罚互补：处罚删错边、重铸示对句）**、Wait time 等待；C **网络 = 盲人（用户类比：无视觉只能听说）**——盲童实证映射（听觉模仿+强化、文本空间代五感、人称代词难点→S 位人称类已建、处理时间→Wait time）+ **Verbalism 空洞语言诚实边界**（感知词是文本关联非真实感知，须标注"感知缺口"）。未来路线（§2.6 末）：**解释通道内化**（解释句进记忆，网络自己说）→ **价值预期**（类别规则升为后果预测，Q 值语义，真"为什么"）→ **元认知**（自解释，呼应 §4.8）。
> **v2.8 修订（2026-08-10）**：**自闭症干预启发（用户直觉："教学有点像和自闭症儿童相处"→ 查证 ABA/DTT → NDBI 演化链，全量落地）**——确认我们 1对1 对话教学（指令→示范→反应→评分奖励→连续 15 次通过）与 **DTT 离散试训**同构，对照 ABA 圈 40 年反思升级暴露的坑：① **提示渐隐（Prompt Fading，最大修正）**——"每次都要示范" = 每次全提示 → **提示依赖风险**（不渐隐的技能 6 个月保留仅 45% vs 渐隐 89%），落点 `_speak.py`：连续 <10 次全示范 → ≥10 次**撤示范**独立说话（独立奖励加成），**最后 5 次通过必须独立说出**；② **Mand 训练/偶发教学**——"奖励与努力匹配"（100 分问句学 2 次 > 80 分学 1 次，已实现）；③ **PRT 强化尝试**——半句 40 分 = 尝试给部分奖励（学 \[S,V]），避免网络不敢试；④ **中性纠错**（现代 DTT：不惩罚色彩，语气中性 + 重试）；⑤ **掌握 ≠ 结束**——掌握后泛化（多种子已做）+ 转间歇强化抗消退检验（v12 留）。§四"教网络说话"新增 **D 自闭症干预启发**小节（五条循证实践表 + 核心洞见写进成长协议）。
> **v2.9 修订（2026-08-10）**：**对话教学升级（用户反馈："网络一直在说我吃苹果"——固定起始词教学让网络固化重复，需打破）**——① **教师发起对话**：教学不再给固定起始词，教师轮转问不同话题的问题（话术丰富但不夸张、句句不重复），网络"听"问题作答——**网络听 = 问题里听到的动词直接主导应答**（应答 Intraverbal 落地：问题里的动词引导回答里的动词，正是幼儿"你问我答"的真实机制；回响降级为备选）；② **反复重复 → 惩罚**：近 REPEAT\_WINDOW=3 轮内同一句**累计出现 ≥REPEAT\_TIMES=3 次**才算"反复" → 该句 V→O 与尾边**减半**（不动共享 S→V 边），逼网络换说法——打破"我吃苹果吗"由奖励越练越强的固化循环（根因：每轮示范都教同一句 + 100 分句奖励学 2 次 → 单边垄断）；③ **答非所问（不通顺）→ 惩罚**：S/V 与老师问题话题不符 → 不给奖励 + 连续计数清零 + **示范重铸正确应答**（不删边——句子本身合法，语境错不惩罚边；判定优先于重复，避免重复的错句被当"重复"处理）；④ **采样权重归一化**（÷最大权重）+ hear 引导 ×8 主导：弱突触也能参与探索，强突触不再垄断采样（S→V 学习初期的天然壁垒）；⑤ **话题块**（用户："老师批改后要和网络发起下一次对话"）：教师批改后围绕**同一话题继续发起对话**（换问法，TALK\_BLOCK=3 轮），练熟再换下一个话题；⑥ **扩写式示范**（用户："示范的时候是把网络的话扩写的最自然"）：示范 = 网络刚说的话的扩写（Expansion 补全 / Recast 重铸 / 陈述扩问句 / 问句换具体答案 / 连说重复换宾语）；⑦ **对话记录 + 实验数据留档**（用户："教师和网络的对话也要记录""以后任何实验数据都要有留档"）：逐轮对话写 talk\_log.json、汇总 + 突触强度对比写 result.json、教学后网络存 net\_after.npz，全部进 `runs/_speak_logs/` 带时间戳留档，基快照作对照组。**v2.9.1（2026-08-10，LLM 判断质量 + 单轮调用）**——用户："大模型要判断质量，从自然语言的角度，要教网络怎么说话"：规则打分只能查槽位（"吗"尾就问句 100），判不了"自不自然"；LLM 升级为质量判断者（LLM-as-Judge）：**"我吃西瓜吗" 规则 100 分、LLM 判 80 并点评"听着像在问自己，可以说'我可以吃西瓜吗'"**——网络因此学会自然问句；用户："把调用压缩到一轮"（参照 AAA 节点"一个 prompt 多个节标记"模式）：教学每轮 LLM 调用 **2 次 → 1 次**（`_llm_teacher_once` 一次输出【质量分数】【答非所问】【质量原因】【教师反馈】【示范句】五节标记，失败整体回退规则）；**增量成长**：默认续用最新教学产物 net\_after.npz，开局即会说话（"我吃鸡蛋吗"），教学成果真正累积（铁律 2）。
> **v2.10 修订（2026-08-10）**：**v13 设计定稿：价值维度 + 拒绝表达（自我边界形成）**——用户："如何让网络学会反抗或者抵触某种信号？" + "弱突触和删边会导致网络对某些信号的感受降低，而负面信号同样是强烈的"（批评现行删边惩罚 = 把"厌恶"实现成"麻木"）+ "从自闭症研究出发呢？"→ 查证 **述情障碍 / FCT / 自我倡导 / 内感受**（Alexithymia：50% 自闭症人群有信号说不出；FCT：沉默回避是问题行为、教替代沟通反应"我不要"；Self-Advocacy 三要素：自我意识→沟通→决策；Interoception：内感受→情绪词绑定）→ 三件套落地 `_speak.py` v13：① **价值标签（不删边）**：VAL 把宾语分好坏（石头=坏、搭配宾语=好），**坏宾语保持强唤起（感知）**——"抵触" = 高唤醒 + 负价值，是警觉不是麻木；② **拒绝表达（FCT 替代沟通）**：V 位加"不要"，教师示范"我不要石头"（网络学 我→不要→石头 边），**拒绝坏东西 = 100 分最高奖励**（honor the refusal）、要坏东西 = 30 分（不删边、只清零计数 + 示范）；③ **内感受→表达绑定**：hear 含坏宾语 → 网络倾向说"不要"（听到坏东西 → 拒绝优先），LLM 教师解释"石头不好，吃了会不舒服"；④ **自我边界形成**（自我倡导三要素映射：自我意识 = 自判唤起强度、沟通 = 拒绝表达"我不要X"、决策 = 采样选择）。预期结果：网络强烈感知"石头"（能说出"我吃石头"= 感知），但价值判断让它选"不要"（行为上拒绝）——"认知变化发生在价值判断上，不是麻木"，呼应心理健康实验关切。
> **v2.11 修订（2026-08-10）**：**数字/数学内容重定位到推理单元（用户决策："调整数学内容放到推理单元"）**——原 Stage 2.7 数字结构 + 2.8 数字运算 从 Stage 2 子阶段**移除**，整体并入 Stage 5 推理级，作为**数学推理线**（结构 → 运算 → 意义三层，先形式后意义），与原有**逻辑推理线**（类比/三段论/条件推理）并列；§四 目录与单元编号同步收紧（子阶段 2.5-2.6/4.5/4.8，单元表 文章 → 5、推理 → 6）；铁律 5 的"数字单元"改挂推理单元——数学分层走完五步闭环（口诀/竖式/数位逐层 ①-⑤），逻辑走链补全。
> **v2.12 修订（2026-08-10）**：**Stage 3 复杂句级（关系）完成（v14.0，parent=13.0）**——复杂句单元五步闭环全量落地（§四.4 状态转 ✅）：关系词对跟读建直连（因为→所以 / 虽然→但是 / 先→然后 边权 24.0）、错配负例拒绝 6 对、结构检查自判（配对直连 + 内容链）、题集 15 道四阶段批改（基础/巩固一致率 1.000，错配 3 题全拒）、**语义因果题 = LLM 裁决价值放大点**（3/3 与人工标注一致；网络语义盲区保守诚实：结构链通 ≠ 语义合理，2 道如实报告不一致，判定由 LLM 讲评补位）；数据专门化落档 `stage3_rel.json`（旧 stage3\_sents.json 为真实语料，弃用）。**实验数据图表化约定（用户 2026-08-10："实验数据必须用图表呈现，不然可读性太差"）**——实验脚本自动出图（matplotlib → `runs/_speak_logs/{ts}_*/charts/*.png`，`_grow_s3.py --charts` 支持历史数据出图），报告引用图表替代纯数字堆砌。
> **v2.13 修订（2026-08-10）**：**铁律 6"对话即学习"读取侧落地（v15.0，parent=14.0）——网络终于"会说话"**。前置探测（probe\_s3）暴露核心矛盾：网络"记住了但不会说话"——续推侧（全局回响/涟漪/WTA）被虚词枢纽（的/了/是/你）连接数量爆炸淹没，前缀续推 0%；而**方案 A 直接传出突触读取预验 9/10**。v15 把方案 A 固化落地为对话/回答读取机制：① **direct\_next 直接传出突触读取**（读词 W\_out 稀疏边汇聚 top-k，不走全局回响）+ **域内过滤**（人工对话域词表 61 词，排除虚词枢纽）→ 直接传出突触问答 10/10；② **prefix\_next 前缀接话**（多词前缀最后词传出突触 + **关系词配对直读**：关系词位于倒数第二位 → 直读 因为→所以/虽然→但是/先→然后，规避内容词双传出突触对称竞争）→ 前缀接话 10/10；③ **训练平衡（方案 D）**——真实语料边碾压专门定式边（在→你 206 vs 在→看 8）→ R\_BIAS=2 加权跟读 + B 型 while 自适应加权（26 轮，在→看 208.0 反超 206.4）；④ **对话练习 13 轮 × 5 话题**（因果/顺序/转折/想要/动宾）教师说→网络接→判定（top-1 ∈ 预期候选）→修正（保守版**不删边只固化 ×3**，避免破坏合法对称边）→ 全话题一致率 1.000（修正 0 次）；⑤ **链式接话验收**（逐词生成整句，每步 ∈ 合法后继集）3/3；继承 v14 零遗忘（字/词 1.0000、句 0.9352 反升、2.5 类别 1.0000、hold 15/15）。**关键洞察：读取机制 ≠ 记忆机制**——边都在，但读出路径必须是显式的"直接传出突触 + 域内过滤"，不能靠全局回响。

## 目录

* [一、背景与现状评估（为什么 v1.1 失败）](#一背景与现状评估)

* [二、核心原则（v2.0 四项铁律）](#二核心原则)

* [三、人类学习能力对照（语言/认知/逻辑/推理七阶段）](#三人类学习能力对照)

* [四、分级成长路线（Stage 0-5 + 子阶段 2.5-2.6/4.5/4.8）](#四分级成长路线)

* [五、数据专门化设计](#五数据专门化设计)

* [六、神经元逐步扩容方案与评估](#六神经元逐步扩容方案与评估)

* [七、双系统读出架构（W 位置层 + 涟漪语义层）](#七双系统读出架构)

* [八、分阶段实施计划](#八分阶段实施计划)

* [九、风险评估](#九风险评估)

* [十、测试与验收](#十测试与验收)

* [十一、影响范围](#十一影响范围)

* [十二、行为模式选择与验证门（v2.2 新增，RL 强化学习）](#十二行为模式选择与验证门)

***

## 一、背景与现状评估

### 1.1 本轮实验证据链（v1.1 → v2.0 的推翻过程）

| # | 实验                             | 结果                                             | 结论                        |
| - | ------------------------------ | ---------------------------------------------- | ------------------------- |
| ① | 26 字母训练（schema\_net.py）        | 26/26 零遗忘、输入 a 回响 a                            | **复述/模式固化能力成立**（最早模式）     |
| ② | 2 万 vs 20 万真实语料（词接词 W）         | 全线下降（wsum 0.1349→0.1220）                       | w\_max 饱和实锤；数据放大无益        |
| ③ | 六引擎对照（wsum/trace/grad/candB/C） | wsum 0.1220 最强，grad 不敌                         | 引擎之争无意义（都是词级）             |
| ④ | 同末词命中率                         | 无引擎超越 wsum                                     | **区分 ≠ 理解**               |
| ⑤ | 生成质量对比                         | 全部引擎产出"标点+虚词"堆砌                                | **模型不会说话**（词接词≠语言）        |
| ⑥ | 句记忆检索（稀碎版）                     | 长位置崩溃（t9+ 0.01）                                | 逐位置签名=稀碎=失败               |
| ⑦ | 整句涟漪（整句单元）                     | 复述率 **1.000**、主题唤起强（足球唤足球/电商唤电商）、泛化续写崩（0.0244） | **整句记忆成立 + 语义联想雏形**；缺位置精度 |
| ⑧ | 技术谱系对照                         | 复述=Hopfield(1982)；双系统=CLS(1995)；机制=HTM/ART/SNN | 效果未超"记忆检索"层               |

### 1.2 三个根本问题（v2.0 要解决的）

1. **数据不专门**：用混合 TF 语料训练"词接词"，等于直接给小孩发 20 万篇混合文章让他悟——26 字母时代"训练什么就是什么"的原则被丢弃了
2. **每次从头开始**：26 字母、2 万、20 万、w\_max 重训、句记忆——每次新网络/新结构，**从未增量成长**（Hebbian 本是累积的，被浪费）
3. **机制错层**：20 万词接词训练把"句子"抹平了——句子级（CLS 海马对应物）从未进入网络结构；整句涟漪证明"句子可以成为记忆单元"，但它是**外挂**，不是网络长出来的

### 1.3 认知启发（用户决策 2026-08-09）

* 人的学习是**条件反射的层级化**：复读 → 下一句 → 词意 → 意思↔意思

* 语文课路径：**老师读一句学生读一句 → 学生自己读 → 理解句意 → 联想 → 举一反三**

* 三字经："读得越多印象越足"，"人之初→性本善"是**局部触发、整块唤起**（非逐词概率）

* **学习是累积的**（词不丢、句不丢），任何实验不得从头开始

## 二、核心原则

**铁律 1：数据专门化**——训练什么就用什么数据，像 26 字母训练就是 26 个字母。词用词数据、句用句数据、文用文数据，禁止用混合语料跳级。

**铁律 2：增量成长**——同一网络持续喂，永不从头开始。每阶段 = 沉淀（学习）→ 冻结（learn\_gate=False）→ 验收（零遗忘）→ 再训练。

**铁律 3：神经元逐步扩容**——n 随阶段增大，与数据规模/复杂度匹配（扩容保留旧 W，旧知识零丢失）。

**铁律 4：教学协议**——严格按人语言/认知/逻辑/推理能力顺序递进（§三），前级验收通过才进下级。

**铁律 5：学习单元闭环（五步教学协议，2026-08-10 用户定稿）**——每个学习单元（词/词组/句子/复杂句/文章/推理…）都必须走完同一完整闭环，缺一步不算学会：① **是什么**（识别/复述单元形态）→ ② **怎么造**（按规则生成新实例）→ ③ **造对的**（约束合法、拒绝病态）→ ④ **为什么错**（错误归因到具体结构 + 外部教师解释）→ ⑤ **自己判断**（**先学怎么做判断，再课后练习**：判断内化到网络结构；⑤a 学怎么做（教师示范判断方法/标准）→ ⑤b 课后练习 + 教师批改讲评——教师不是退场，是转岗为批改讲评者：网络自判，教师批改对错、讲评错因，网络再修正）。句子单元已走完 ①-④（v7→v11），⑤ 网络自判 = v12 开发中；**后续每个单元一律按此五步复刻推进**，不允许"教完①②就结课"。

**铁律 6：对话即学习（交互优先，2026-08-10 用户定稿）**——用户指出"现在没做最重要的交互能力，人类最早学会的就是如何说话、和父母对话，这才是重中之重"。语言的**第一性目的是交流**——复述/造句/判断都是"说话的工具"，"交谈"才是使用工具去交流；没有对话，网络学完所有闭环也只是"会说话但不会交谈"。因此：**对话 = 教学环境（铁律级，不是某个 Stage）**——所有单元的教与学都在"网络↔教师"对话轮次中进行（教师=父母：教师说 → 网络回应 → 教师反馈 → 网络修正 → 下一轮），**五步闭环是对话的内容而非形式**；对话对象**先 LLM 教师陪练（父母角色）→ 对话能力成熟后开放真实用户**（呼应"AI 慢下来、公众可理解"愿景）。任何单元不得脱离对话场景孤立训练（"只练不说"不合格）。落地细则见 §四"对话即学习"。

## 三、人类学习能力对照

| 年龄段    | 人的能力           | 认知/语言/逻辑/推理对应物     | 定式网络 Stage       |
| ------ | -------------- | ------------------ | ---------------- |
| 0-1 岁  | 语音复述（咿呀学语）     | 复读条件反射（模式固化）       | **已完成**（26 字母）   |
| 1-2 岁  | 单词命名（妈妈/苹果）    | 词模式固化 + 复述         | **Stage 0 词级**   |
| 2-3 岁  | 双词组合（妈妈抱/苹果好吃） | 词组合条件反射（bigram 定式） | **Stage 1 词组级**  |
| 3-4 岁  | 短句（我要苹果）       | 句内转移定式 + 句模式       | **Stage 2 短句级**  |
| 4-6 岁  | 复杂句（因果/转折/时间）  | 关系定式（因为→所以/虽然→但是）  | **Stage 3 复杂句级** |
| 5-6 岁（幼小衔接） | 主动提问（"为什么/是什么"）、因果假设理解、应答问答、反问设问萌芽 | 问答定式（Intraverbal）+ 主动提问（Mand 扩展）+ 自然语句（虚词层） | **Stage 3 幼小衔接细化（s3.2-s3.6，⏳ v2.17 设计）** |
| 6-12 岁 | 文章阅读、句间逻辑      | 句接句转移 + 文章级记忆      | **Stage 4 文章级**  |
| 12+ 岁  | 抽象推理、类比、举一反三   | 概念定式转移 + 涟漪泛化      | **Stage 5 推理级**  |

**验证依据**：每阶段对应一种条件反射对象（§1.3 用户的四阶段理论），对象逐级抽象：字音 → 词 → 词组 → 句 → 句间 → 概念。

## 四、分级成长路线

### 对话即学习（铁律 6 落地：教学对话机制，2026-08-10 用户定稿）

**定位**：对话 = **教学环境**（不是某个 Stage）。人类语言习得的第一经验是和父母对话（发声→回应→再发声的轮替出现在 6-12 个月，先于单词）；网络同样——所有单元的教与学都发生在"网络↔教师"的对话轮次里，**五步闭环是对话的内容，对话是五步的场景**。没有对话场景的教学 = "只练不说"，不合格（铁律 6）。

**对话轮次机制（turn-taking）**：一段对话 = 反复的"回合"——

| 回合角色 | 内容                               | 网络机制                                        |
| ---- | -------------------------------- | ------------------------------------------- |
| 教师说  | 输入句 / 示范 / 出题（"这是什么？""跟我读：我要苹果"） | 输入走链（已有）                                    |
| 网络回应 | 复述 / 作答 / 自判（"苹果""我要苹果""可造"）     | 槽位走链生成 + 结构自判（②③⑤）                          |
| 教师反馈 | 肯定 / 纠正 + 讲评原因（LLM 教师判断+解释，v11）  | 验证门 + 连接级归因处罚（④）                            |
| 网络修正 | 边修正后进入下一轮                        | 处罚/固化沉淀                                     |
| 对话延续 | 教师按上一轮内容出下一轮（多轮上下文）              | 句级转移定式 + 对话轮次 = §12 行为模式选择（复读/作答/提问/沉默都是模式） |

**对话类型（对标母语习得，由早到晚）**：

1. **轮替对话**（6-12 月）：教师发声 → 网络回声（任意输出）→ 教师回应——先学会"轮流"，内容后置（网络只有基础词级即可开始）
2. **应答对话**：教师问"这是什么？" → 网络答词（词/词组单元）；"你在做什么？" → 网络答句（句子单元）
3. **复述跟读**：教师说整句 → 网络跟读（Stage 2 整句涟漪已具备）
4. **主动发起**：网络自己开口（指向/提问）——衔接 §4.8"该不该开口"（唤起强度够 → 开口，不够 → 沉默）
5. **对话延续**：回应后教师追问 → 网络再回应（多轮，句间转移）

**对话对象阶段（先教师后用户）**：

* **阶段一 教师陪练**：LLM 教师扮演"父母"——可控（脚本 + 动态生成）、可重复（同一轮次可重放）、可批改讲评（判断+原因）。对话语料 = 脚本构造（每单元按五步出对话剧本）+ 教师临场生成。

* **阶段二 真实用户**：对话能力成熟（轮次完成率 + 回应一致率达验收线）后开放真实用户——响应"AI 慢下来、公众可理解"愿景（公众能直接和网络对话、看它怎么想）；LLM 教师退为后台保底（判断/讲评仍可介入纠错）。

**验收口径**：① 轮次完成率（"教师说→网络回应→教师反馈"一轮跑通的比例）；② 回应一致率（网络回应 vs 教师预期一致率，分档：复述/作答/自判）；③ 主动发起（§4.8：该开口时开口、不该开口不说，答不出时诚实说"不知道"）。

**诚实边界**：对话早期 = 检索/复述级（网络回的是记忆里的词句，不是生成新意）；"交谈"要升到"理解"，依赖 ④⑤ 内化后的结构自判与解释通道内化（§2.6 末未来路线）；对话内容上限 = 网络已学结构（没学过的词句自然答不出，正好是"不知道"的诚实样本）。

**v2.9 实现（对话教学升级，`_speak.py`** **实测）**——用户反馈"网络一直在说我吃苹果"（固定起始词教学 → 网络固化重复）→ 应答对话落地：① **教师发起对话**（不再给固定起始词）：**话题块机制**——教师批改后围绕**同一话题继续发起下一次对话**（换问法，TALK\_BLOCK=3 轮），练熟一个话题再换下一个，网络**听问题再作答**（听到问题里的动词就主导说它，应答 Intraverbal 落地，幼儿"你问我答"机制）；② **反复重复 → 惩罚**：近 3 轮内**同一句累计出现 ≥3 次**才算"反复"（连说同一句才罚，偶尔重提不算）→ 该句 V→O 与尾边减半（不动共享 S→V 边），逼网络换说法（打破"奖励越练越强 → 单边垄断"的固化循环，根因 = 每轮示范都教同一句 + 100 分句奖励学 2 次）；③ **答非所问（不通顺）→ 惩罚**：S/V 与话题不符 → 不给奖励 + 计数清零 + **示范重铸正确应答**（不删边，句子本身合法；判序在重复之前，避免重复了错句被当"重复"处理、示范跟着错动词教——实测 bug 修正）；④ **采样权重归一化**（÷最大权重）+ **hear 引导 ×8 主导**：弱突触也能参与探索、听到的动词主导应答（实测修掉"问题含「什么」→ 回响同时激活所有动词 → 引导抵消"的坑）；⑤ **扩写式示范**（用户："示范的时候是把网络的话扩写的最自然"）：示范顺着网络刚说的话走——说半句补完整（Expansion）、答非所问重铸（Recast）、说陈述扩成问句、说问句换具体答案、连说重复换宾语；⑥ **对话记录 + 实验数据留档**（用户："教师和网络的对话也要记录""以后任何实验数据都要有留档"）：每轮对话（教师问/网络答/评分/反馈/奖励/示范/streak）逐轮写入 `runs/_speak_logs/{时间戳}_{种子}_{教师}/talk_log.json`，汇总指标 + 教学前后突触强度对比写入 result.json，教学后网络存 net\_after.npz（snapshot 同格式可恢复），基快照 v11.2 作对照组。**实测（规则教师，seed=我）**：66 轮通过——网络随话题换动词换宾语（吃→鸡蛋/饼干/苹果、喝→牛奶、看→商店/公园…）、问句 100 分主导、连续 15 次通过其中最后 5 次独立说出、答非所问 14 次清零、反复重复惩罚 3 次；教学后 S→V 边从全 0 长到 256、V→O 边按教学分化（吃→鸡蛋 120、喝→牛奶 117、看→公园 96…），对照组数据完整留档。

### 教网络说话（教学方法查证 2026-08-10：言语行为教学法 + 盲人语言教学）

**A. 言语行为教学法（Verbal Behavior，Skinner 1957 / VB-MAPP）——说话 = 四种独立操作项**

| 操作项                | 功能      | 例子           | 网络教学场景               | 现状        |
| ------------------ | ------- | ------------ | -------------------- | --------- |
| **Echoic 仿说**      | 重复他人话语  | 师说"球"→生说"球"  | 跟读复述（整句涟漪）           | ✅ v7      |
| **Tact 命名**        | 命名事物/类别 | 看到狗→"狗"      | 词↔类别标签命名             | 🟡 词单元    |
| **Mand 要求**        | 动机驱动请求  | 想要果汁→"果汁"    | **主动**说"我要苹果"（S+V+O） | 🟡 v9/v10 |
| **Intraverbal 应答** | 回应他人言语  | "你叫什么？"→"李明" | 回答"你要什么？→我要苹果"       | ❌ 未开发     |

* **教学顺序（VB-MAPP 框架）：Mand → Echoic → Tact → Intraverbal**——先建立"说话能带来结果"的功能性动机（Mand 第一），仿说作桥梁（Echoic 第二，靠它教其余形式），再命名（Tact），最后对话应答（Intraverbal）

* **关键原则：四种操作项是独立能力，分别教、分别验收**——网络会复述"我要苹果"（Echoic）≠ 会主动要（Mand）≠ 会回答提问（Intraverbal）；"会命名苹果"也不等于"会要求苹果"（VB 核心洞见：一种语境学会 ≠ 其他语境能用）

**B. 回应性语言刺激策略（语言治疗循证：Language Stimulation / Expansion / Recast）**

* **Self-talk / Parallel talk（自我/平行对话）**：教师描述自己在做什么（"我在吃苹果"）、网络在做什么（"你在看公园"）——持续语言暴露（v7 跟读 + 对话场景）

* **Expansion（扩展，加一原则）**：网络说"苹果" → 教师扩展"我要苹果"（**每次只加一个信息点**）——网络学 S+V+O 结构的对话路径，与 ② 怎么造衔接

* **Recast（重铸）**：网络说错 → 教师**不处罚、用正确形式重说一遍**（"他走走了"→"对，他走了"）——**与 v11 处罚互补：处罚删错边（去掉错的）、重铸示对句（示范对的）**，两者合一才是完整纠错

* **Wait time（时间延迟）**：教师提问后留 5-10 秒等待——网络唤起需要传播时间，不等它就是"抢答式教学"

**C. 网络 = 盲人（用户类比 2026-08-10：定式网络无视觉，只能听和说）**

* **定位**：网络的学习通道 = 输入文本走链（听）+ 输出语句（说），**无视觉/味觉/触觉/嗅觉感知通道**——与盲童语言习得的处境一致（盲童靠**听觉模仿 + 强化**学说话，不能视觉模仿口型/表情）

* **教学适配（盲童实证映射）**：

  * 新词必须关联**可感知物**（"苹果" + 摸/闻）→ 网络 = 词↔义原/类别边（§2.5 已建）；但网络"感知" = **文本关联而非真实感知**（义原词典是语言描述）

  * **五感教学 → 文本空间替代**：盲童靠语言描述构建颜色概念（颜色认知研究：盲童形成"事物-颜色"语义维度，非"颜色-非颜色"维度）→ 网络同理："红色"绑定的是共现词（苹果/红旗），不是光波感知

  * **人称代词教学**（盲童最难点：你/我/他混淆，靠情景游戏反复练）→ 网络 S 位人称类绑定（v9/v10 已建，SVO 槽位教学同构）

  * **处理时间更长** → Wait time 策略（B）

* **Verbalism（言语主义/空洞语言）——最关键诚实边界**：盲童会用词但无对应经验（能说"红色"却不知红色是什么；实证：感知不可达的词产出更少，盲童 vs 明眼童，PMC 2025）→ **网络同理：能说"苹果是甜的"（词↔义原边）但"甜"是文本关联不是味觉**——说话内容上限 = 文本空间已学结构；感知属性词须标注"感知缺口"（诚实说"我没尝过，这是书上说的"），衔接 §4.8"该不该开口"与 §2.5 诚实边界③（HowNet 不标注感知属性）

**D. 自闭症干预启发（2026-08-10 查证：ABA/DTT → NDBI 演化链，全量落地 \_speak.py）**

**为什么像**：用户直觉——"1对1 对话教学（指令→示范→反应→评分奖励→连续 15 次通过）"与 **ABA/DTT 离散试训**同构（DTT 五要素：指令 SD → 提示 → 反应 → 后果（强化/纠错）→ 回合间隔；掌握标准 mastery criterion = 连续达标通过）。对照 ABA 干预圈对这套方法近 40 年的反思与升级，暴露我们方案的坑与改进方向：

| # | 干预循证实践（出处）                                                                 | 核心内容                                                                                     | 网络映射                                                      | 落地                                                                                    |
| - | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 1 | **提示渐隐 / Prompt Fading**（Touchette 1971 Time Delay；MacDuff 2001；Fading 综述） | 提示的目标是**撤掉它**；不渐隐 → **提示依赖**（孩子只在提示出现时才做，表面会了实际依赖）；正确渐隐的技能 6 个月保留 **89%** vs 不渐隐 **45%** | **我们最初的"每次都要示范" = 每次全提示 → 提示依赖风险**（网络学的可能是"等示范再模仿"而非独立说话） | ✅ 已实现：`_speak.py` 三级渐隐——连续 <10 次全示范（含问句）→ ≥10 次**撤示范**（独立说话，独立说对的句子奖励加成：多学 1 次固化独立定式） |
| 2 | **Mand 训练 / 偶发教学**（Incidental Teaching；Graham 2025）                        | 想表达→挡住→示范→等待→给了→重复 3-5 次；"**奖励与努力匹配：回应越大、奖励越大**"；等待更久打破提示依赖                              | ② 主动造句 = Mand 训练原型（网络主动要求"我要苹果"）                          | ✅ 已实现：质量分级奖励（100 分问句学 2 次 > 80 分陈述学 1 次，努力/奖励匹配）；Wait time 已并入（网络自己采样探索，教师不抢话）        |
| 3 | **PRT 关键反应训练**（Koegel；NCAEP EBP）                                           | 四大支柱：动机、多线索、自我发起、**自然强化**；**强化尝试（reinforcing attempts）**——孩子试了就算数                        | 半句（40 分）= 尝试，完全不给奖励 → 网络可能"不敢试"                           | ✅ 已实现：半句给**部分奖励**（学 \[S,V] 强化 S→V 边，鼓励尝试）                                             |
| 4 | **中性纠错**（现代 DTT 进化：停顿→重示范→再试→较低强化，"不是挫败或惩罚"）                               | 错误后用中性纠错程序，避免惩罚色彩                                                                        | 我们"搭配错 → 删边"是经典惩罚派                                        | ✅ 已实现：保留删边（网络唯一能理解的负信号，客观减权）+ LLM 教师中性语气（不打击）+ 错误后下一轮重试                               |
| 5 | **掌握标准 + 泛化 + 维持**（mastery criterion；泛化跨环境/跨人；强化安排表：连续强化建立 → 间歇强化维持抗消退）    | 掌握后不结束：泛化（多情境）+ 转间歇强化检验抗消退（停止强化 → 行为消退 = 网络权重遗忘/睡眠巩固对应物）                                 | 连续 15 次 = 掌握标准；多起始词（我/你/他/猫）= 泛化                          | 🟡 已实现前两环（15 次掌握 + 多种子泛化）；**维持/抗消退检验 = v12 设计**（教学后停训、间隔验收确认定式不退化，衔接铁律 2 零遗忘验收）       |

**核心洞见（写进成长协议）**：① **示范是脚手架，必须渐隐**——教学收尾的标志不是"网络在有示范时能说对"，而是"撤掉示范后独立说对"（我们定：连续 15 次通过中**最后 5 次必须是独立说出**）；② **奖励要匹配努力、覆盖尝试**（说得好奖励高、尝试了有部分奖励、只有说错才删边）；③ **掌握 ≠ 结束**——掌握后要泛化（换起始词/换教师）+ 抗消退检验（v12 留）。

### 学习单元闭环明细（铁律 5 落地：每单元五步详细规划，2026-08-10 用户定稿）

**通用五步含义**（所有单元同一语义，①②③ 因单元而异、④⑤ 全单元复用通用机制）：

| 步      | 教学动作                                                         | 网络机制                           | 教师角色                                |
| ------ | ------------------------------------------------------------ | ------------------------------ | ----------------------------------- |
| ① 是什么  | 识别单元基本形态（复述）                                                 | 模式固化 + 整块唤起                    | 示范/跟读                               |
| ② 怎么造  | 按规则组合**没学过**的新实例                                             | 槽位/hub/走链组合                    | 出题/示范                               |
| ③ 造对的  | 约束合法、拒绝病态                                                    | 类别约束 + 验证门拒绝                   | 判对错                                 |
| ④ 为什么错 | 错误归因到**具体连接** + 自然语言原因                                       | 连接级归因处罚（v11）                   | **LLM 教师**（判断+解释）                   |
| ⑤ 自己判断 | **⑤a 学怎么做**：先学判断方法/标准（怎么判断对错）→ **⑤b 课后练习**：网络自己判断对错（判断内化到结构） | 判断方法教学（示范）→ 结构检查自判 + 阈值标定（v12） | 示范判断方法 → **批改讲评**：批改对错 + 讲评错因，网络再修正 |

**推进原则**：① 每个单元的 ④⑤ 复用 v11/v12 通用机制（连接级归因处罚 + LLM 教师 + 结构自判），**不重复发明**；② 前单元 ⑤ 课后练习批改讲评收敛（自判错误样本纠错到收敛）才进下一单元（铁律 4/5 衔接）；③ ⑤ 的批改口径 = **网络自判 vs LLM 教师判断一致率**（教师批改作业）；④ ⑤ 之后自然衔接 §4.8（"该不该开口"阈值标定）；⑤ **每单元教学全程以对话轮次为载体**（铁律 6：教师说 → 网络回应 → 教师反馈），五步闭环 = 对话剧本内容，禁止脱离对话场景的孤立训练。

**课后练习设计原则（教学法查证 2026-08-10，⑤b 出题依据）**——查证教学法/认知科学练习设计（Testing Effect、Spacing、Interleaving、Variation、Feedback、Cognitive Load、Encoding Specificity、分层练习）：

| # | 教学法原则（出处）                                                 | 含义                                      | 网络实现                                                                      |
| - | --------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------- |
| 1 | 检索优先 / Testing Effect（Roediger & Karpicke 2006）           | 自测回忆 > 反复重读（记忆高 \~50%）；**练习本身即学习**      | 自判 = 主动从边结构"回忆"判定（走链即检索），**不重放训练样本**                                      |
| 2 | 间隔重复 / Spacing（Ebbinghaus 遗忘曲线；Cepeda 2006）               | 分散练习 > 集中突击（当天/隔天/一周，2+2+1 模式）          | **适配**：网络无时间遗忘（W 不衰减）→ 间隔意义 = **分轮重测确认不回退**（铁律 2 零遗忘验收天然是间隔形式）；练习集分批、跨轮穿插 |
| 3 | 穿插练习 / Interleaving（Bjork；Rohrer & Taylor 2007）           | 混合题型 > 单一连续；逼辨识差异、迁移更强（"合意困难"，当下吃力但记得牢） | 练习集**混搭不同题源**（如吃/看/画/喝交错出题），防"O槽 保底"思维定式                                  |
| 4 | 变式练习 / Variation（Bjork & Bjork 2020）                      | 同一知识点多维变式（正向/逆向/特殊/综合），防思维僵化            | 同一 (S,V) 出 **正向**（该配 O）/ **逆向**（不该配 O）/ **临界**（类别泛化二跳）三型题                 |
| 5 | 即时 + 精细化反馈 / Elaborative Feedback（Butler & Roediger 2008） | 反馈在检索活跃时立即给；**解释"为什么对/错" > 只给对错信号**     | LLM 教师批改 = 判断 + **讲评原因**（elaborative），每次自判后立即给（v11 已具备）                   |
| 6 | 认知负荷管理 / CLT（Sweller）                                     | 先工作样例 → 撤脚手架 → 独立练习；一次一个概念              | ⑤a 工作样例（教师示范判断过程）→ ⑤b 独立练习；一次判一个组合                                        |
| 7 | 编码特异性 / Encoding Specificity（Tulving & Thomson 1973）      | 练习线索 = 应用线索（水下记词在水下回忆更好）                | 练习用**真实槽位/走链路径**（与 ②③ 同机制），判断线索 = 应用线索                                    |
| 8 | 分层练习（"学练展评"模式：基础过关 → 变式迁移）                                | 基础（自动化）/ 巩固 / 拓展（迁移），保底不封顶              | 基础题（训练组合）/ 巩固题（新组合）/ 拓展题（类别泛化二跳）                                          |

**通用题集结构（⑤b 每单元统一）**：**三档 × 变式 × 穿插**——每次练习 = 混搭题集；批改口径 = 网络自判 vs LLM 教师一致率，**分档统计**（基础档应 ≈1.0、巩固档 ≥0.9、拓展档允许 <1.0 留泛化边界，阈值随单元定）。分档意义 = 自判置信分层（§4.8"该不该开口"的输入：拓展档低置信 → 判"不知道"更诚实）。

#### 1. 词单元（Stage 0 → 2.5：词形 → 词义）

| 步      | 内容                           | 数据                  | 机制               | 状态               |
| ------ | ---------------------------- | ------------------- | ---------------- | ---------------- |
| ① 是什么  | 词复述（输入 w → 回响 w）             | 字表（3500）/ 词表（10000） | 模式固化 + 整块唤起      | ✅ v1.0/v2.0      |
| ② 怎么造  | 新词入网可唤起 + 词义走链（同类词唤起类别）      | OpenHowNet 词↔义原边    | 类别 hub + 举一反三    | ✅ v8.5           |
| ③ 造对的  | 词义判断题对/错（苹果是水果？对；苹果是咸的？错）    | HowNet 判断题 + 人工负例   | 验证门：对→固化、错→拒绝    | ✅ v8.5（拒绝 54/54） |
| ④ 为什么错 | 错误词义联想归因（苹果→咸 靠哪条突触）+ LLM 解释 | 错误判断题 + 归因记录        | 连接级归因处罚 + LLM 教师 | ⏳ 未开发（词单元 v13）   |
| ⑤ 自己判断 | 凭词↔义原边结构自判"w 属于 c 吗"（路径存在性）  | 同 ③ 判断题集            | 结构检查自判（v12 机制复用） | ⏳ 未开发            |

关键设计：④ 的归因对象 = "错误联想边"（词→错误义原/标签边，如苹果→咸）；⑤ 的自判结构 = 词→类别标签的可达性（§4.8 信号）。

**⑤b 课后练习规划（词单元）**：

* **基础档**：训练词类别归属复测（苹果→水果？对）——自判 = 词→标签直连可达性

* **巩固档**：hold-out 词自判（凭与类别成员共享的义原边判"属于哪类"）——二跳泛化

* **拓展档**：无义原词（数字/专名/新词）→ 自判"不知道"（诚实留白，衔接 §4.8）

* **变式**：正向（是水果？）/ 逆向（是咸的？）/ 临界（共享多类义原的词，如"瓜"跨食物/植物）

* **穿插**：五类交错出题（食物/动物/颜色/时间/地点混出），防"见果即食"定式

* **批改**：LLM 教师讲评错因（"苹果不是咸的：无此义原边，错误联想来自…"）

#### 2. 词组单元（Stage 1：主谓/动宾/修饰组合）

| 步      | 内容                       | 数据          | 机制                        | 状态                             |
| ------ | ------------------------ | ----------- | ------------------------- | ------------------------------ |
| ① 是什么  | 词组复述（苹果好吃/妈妈抱/很快）        | 专门词组集       | 模式固化 + 词组内转移定式            | 🔶 26 字母 combo 口径验证过原子保留，词组集未建 |
| ② 怎么造  | w1→w2 组合（先学 a 再学组合，原子保留） | 词组对         | bigram 定式转移               | ⏳ 待开发                          |
| ③ 造对的  | 词组搭配约束（吃+苹果 对、吃+石头 错）    | 例句共现对       | 类别约束 + 验证门拒绝（复用 2.6 动宾机制） | ⏳ 待开发                          |
| ④ 为什么错 | 搭配错误归因 + LLM 原因          | 错误词组 + 归因记录 | 连接级归因处罚 + LLM 教师（通用）      | ⏳ 待开发                          |
| ⑤ 自己判断 | 凭 V↔O 搭配边结构自判"能配吗"       | 同 ③         | 结构检查自判（通用）                | ⏳ 待开发                          |

关键设计：词组单元是句子单元的**前置微缩版**（动宾约束机制先在词组级验证，再上句级），④⑤ 直接复用 v11/v12 通用件。

**⑤b 课后练习规划（词组单元）**：

* **基础档**：训练词组复测（吃+苹果 能配？）——自判 = V↔O 直连可达性

* **巩固档**：新词组对（训练过的词、没配过的组合）——直连/二跳

* **拓展档**：类别泛化二跳（吃+香蕉：吃→苹果→食物→香蕉 链可达？）

* **变式**：正向（能配）/ 逆向（不能配：吃+石头）/ 临界（跨类词，如"看+学校"——类别不匹配但语义可接受，LLM 教师裁决）

* **穿插**：主谓/动宾/修饰三类词组混出

* **批改**：LLM 教师讲评（临界题是"看"类宽松约束的教学落点）

#### 3. 句子单元（Stage 2 → 2.6）——**五步已完整规划与落地**（见 §2.6 上方表格）

① 什么是句子 ✅ v7.0（整句涟漪复述）→ ② 怎么造 ✅ v9.0（槽位绑定 + 定式走链）→ ③ 造对的 ✅ v10.0（动宾搭配约束 + 病句拒绝）→ ④ 为什么错 ✅ v11.0/11.1（连接级归因处罚 + LLM 教师判断+原因）→ ⑤ 自己判断 ✅ **v12.0/v13.0**（结构检查自判 + 复杂句式练习题，全量验收通过，§2.6 详）。

#### 4. 复杂句单元（Stage 3：因果/转折/顺序关系）

| 步      | 内容                      | 数据                                 | 机制                                | 状态      |
| ------ | ----------------------- | ---------------------------------- | --------------------------------- | ------- |
| ① 是什么  | 关系句复述（因为…所以…）           | 专门关系句 11 条（数据专门化，stage3\_rel.json） | 模式固化 + 关系词转移定式                    | ✅ v14.0 |
| ② 怎么造  | 关系词补全（因为→? 答所以）+ 半句接续   | 关系句对（3 组 × 变体）                     | 关系词对跟读 ×3 建直连（因为→所以 边权 24.0）      | ✅ v14.0 |
| ③ 造对的  | 关系搭配正确（因为…所以 对、因为…但是 错） | 关系判断题 + 负例（错配 6 对防御性拒边）            | 验证门：对→固化、错→拒绝（错配题 3/3 拒绝）         | ✅ v14.0 |
| ④ 为什么错 | 关系错配归因 + LLM 原因/讲评      | 错误关系句 + 归因记录                       | 连接级处罚 + LLM 讲评（通用）                | ✅ v14.0 |
| ⑤ 自己判断 | 凭关系词传出突触结构自判"这组关系词配吗"   | 题集 15 道（基础 5 + 巩固 4 + 拓展 6）        | 结构检查自判：配对直连 + 内容链（基础/巩固一致率 1.000） | ✅ v14.0 |

关键设计：④ 的归因对象 = 错误关系词边（因为→但是 共现误建）；⑤ 的自判结构 = 关系词→关系词传出突触可达性。复杂句的"为什么"首次接触语义因果（"因为下雨所以带伞"），LLM 教师的解释价值在此放大（规则验证器对语义因果完全无力）。**v14.0 实证**：语义因果题 LLM 裁决 3/3 与人工标注一致；网络语义盲区如实报告——2 道语义不合理题（下雨→吃饭 / 饿→看医生）网络结构全链通判"可造"、教师 LLM 判"不可造"，**保守诚实不动**（结构合法 ≠ 语义合理，删边会破坏合法结构），判定由 LLM 讲评补位。

**⑤b 课后练习规划（复杂句单元）**（✅ 已实现 v14.0）：

* **基础档**：训练关系句复测（因为…所以 配吗？）——自判 = 关系词直连可达性（5 题全对）

* **巩固档**：新关系句（换内容，如 虽然…但是）——关系词传出突触存在性（4 题全对，bigram 链复用）

* **拓展档**：语义因果题（"因为下雨所以带伞" 对吗？）——规则验证器完全无力，**LLM 教师语义判断的价值放大点**；网络自判 = 关系词 + 内容词连词可达性（诚实边界：内容级因果网络无边 → 判"不知道"，结构链通但语义被否 → 保守诚实 + LLM 讲评）

* **变式**：正向（配）/ 逆向（错配：因为…但是）/ 临界（"虽然…但是" vs "虽然…所以" 语义边界）——错配 3 题全部凭"配对无直连"拒绝

* **穿插**：因果/转折/顺序三类关系混出，逼辨"这组关系词配不配"

* **批改**：LLM 教师讲评语义因果错因（"因为下雨所以带伞合理；因为下雨所以吃饭不合理——前后内容无因果联系"）

**⑥ 对话/回答读取机制（✅ 已实现 v15.0，铁律 6"对话即学习"读取侧落地）**：

* **核心矛盾**：网络"记住了但不会说话"——续推侧（全局回响/涟漪/WTA）被虚词枢纽连接数量爆炸淹没（前缀续推 0%），而**直接传出突触读取预验 9/10**（答案就在边里，读出路径不对）；

* **direct\_next 直接传出突触读取**：读词 `W_out` 稀疏边汇聚 top-k，不走全局回响 + **域内过滤**（对话域词表 61 词，排除虚词枢纽）→ 问答 10/10；

* **prefix\_next 前缀接话**：最后词传出突触 + **关系词配对直读**（关系词位于倒数第二位 → 直读配对词，规避内容词双传出突触对称竞争）→ 前缀接话 10/10；

* **训练平衡（方案 D）**：真实语料边碾压专门定式边（在→你 206 vs 在→看 8）→ 加权跟读 + B 型自适应加权（26 轮反超 208.0 vs 206.4）；

* **对话练习**：教师说 → 网络接 → 判定（top-1 ∈ 预期候选）→ 修正（保守版**不删边只固化 ×3**）→ 13 轮 × 5 话题全一致；

* **链式接话**：逐词生成整句，每步 ∈ 合法后继集 → 3/3；

* **关键洞察**：读取机制 ≠ 记忆机制；对话是"选合法后续"不是"背课文"（验收口径 = 合法后继集）；删边惩罚破坏合法对称边 → 修正只固化不删边。

**⑦ 句式颗粒度升级（✅ 已实现 v16.0，parent=15.0）——句式的复杂度"没有这么低"（用户 2026-08-10）**：

* **问题**：现 s3 是 4 词定式 `[关系词1, X, 关系词2, Y]`，真实关系句远更复杂。前置探测（probe\_s3adv）：真实语料关系句均值 15.3 词、关系词位置多样（句首/句中/句尾）、配对/单用并存；v15.0 上升级句式读取仅 10/29=34.5%——**三类断点**：① 配对直读过度触发（因为今天→所以，跳过内容小句"下雨"）② 后半关系词无上下文（所以→吃饭，期待主语"我"）③ 主语结尾断读（所以我→你，真实语料边 201.6 碾压）；

* **数据（质量优先，用户 2026-08-10："用质量更好的来训练，主要是对话和短文的"）**：`stage3_rel_v2.json` **108 条** = 短文·真实 47 条（**toutiao 新闻标题精筛**：去标点/去专名堆砌/词表外词≤4）+ 对话·构造 38 条（日常口语关系句）+ 短文·构造 23 条（叙述关系句）；四维全覆盖（嵌套复合 / 内容小句化 / 关系词位置多样 / 句内修饰加长：今天/助词了/动宾/去地点）；**不用商品评价语料（stage3\_sents.json 弃用于 v16）**；

* **读取 = 句式状态机** **`clause_next`**（废除 v15 REL\_NEXT 配对直读）：`NONE→PRE→POST` 状态驱动——PRE 阶段读内容词传出突触 top-1（**排除人称**：内容小句尾部再读人称 = 开新小句，超出"单一配对句"边界，且真实边 了→你 碾压专门边），top-1 是关系词2 才触发**配对约束直读**（因为→所以，打破 下雨→所以/但是 对称竞争）；POST 阶段关系词2 后**排除 V/O 读结果主语**、主语后**排除人称读结果谓语**（避开 我→你 201.6）；

* **验收口径 = 分句接话**（对话语义：教师说前半 → 网络逐词接后半；按**角色类型**判定 REL2/SUBJ/PRED 命中，尊重内容多样性——主语位任何人称、谓语位任何动作/宾语都算句式接对）；6 句代表覆盖三类关系 + 四维升级；

* **训练平衡**：108 条 ×2 轮 + 教师批改校准（读错 → 固化期望边 ×3，全量仅 **1 处**（他→上课 ×6，真实边 他→生病 碾压））；修正前 17/19=0.895 → 校准后 **19/19=1.000**；

* **继承 v15 零遗忘**：字/词 1.0000、句 0.9255、2.5 类别 1.0000、hold 15/15；

* **关键洞察**：句式复杂度上不去的**根因不是句子少，而是"网络见过的句式太少"**——读不出来 = 读取机制（状态机）+ 数据质量（对话/短文真实句式）双缺；**纯注入开源语料无效**（真实边碾压专门边，v15 已验证），正确路径 = 受控小句化专门化 + 真实短文精筛补充；状态机是读取约束（跟踪"我在原因段还是结果段"），不是新语言知识——读出的仍是已学边。

**⑧ 先见后教 + 数据扩量 + 并发训练（✅ 已实现 v17.0，parent=16.0）——"不是教，是先让网络见过散文"（用户 2026-08-10）+ "怎么会花这么多时间？并发呢？能不能切块？"**：

* **问题**：v16 全量散文先见探测——先见不碾压专门边（修正前 0.947 ≥ 基线 0.895），但 **108 条×2 轮后教拉不回配对直读断点**（"虽然→但是"被散文全量竞争边碾压，0.947 未回基线），校准 1 处且修复错边（PRE 位 top-1 是"生病"不是"了"，固化 了→但是 无效）；

* **先见后教顺序**：鲁迅 5 本全量 9310 句（词表内过滤 ≥3 词）先喂进网络成经验 → 再喂教学数据把目标边压回；铁律 1 边界：散文只做"见过"背景，**不扩词表不学新词**（新词归 Stage 4 / v14 字级方案）；

* **数据扩量**：`stage3_rel_v3.json` **166 条** = 108 现有 + **58 新模板**（全部词表内 miss=\[]：因果 +26 / 转折 +17 / 顺序 +15；新词元 她/你们/他们/热/渴/忙/疼/难过/看书/上班/回家/做饭/吃药/洗澡/坚持/医院/图书馆；候选词已对 v16.0 词表查证，仅 喝水/起床/手帕 不可用已避开）；

* **并发训练（rng 切块）**：学习路径含回响噪声发放（每句 \~3600 随机边）→ **纯统计批量不等价**（对拍 1846 万边差异 340 万）；成立方案 = 主进程预生成每块边界 rng state → 各进程恢复喂入 → 数组级增量合并（**≡ 串行，100 句 2 块对拍 0 差异**）；dict 全量快照 30GB+ OOM → 数组级（src\_i/slot\_k/dst\_j/vals 四数组零复制）worker 峰值 \~2GB；**并发上限 = 4**（内存墙 \~600MB/进程 + 4 核物理墙 + 合并端墙）；

* **训练平衡**：`_grow_v16.py` PRE 位排除"在"（全量散文把 了→在 堆到 w\_max=16 封顶与 了→但是 并列，PRE top-1 排序靠前永远胜出 → 配对直读断点 + 校准固化无效）；

* **v17 实测（全量 9310 句先见 + 166×2 后教）**：散文后修正前 1.000（无碾压）→ 后教后修正前 **1.000**（目标 ≥0.95 ✅）→ 校准 **0 处**（目标 ≤1 ✅）→ 校准后 1.000；总耗时 1422s（4 块并行 vs 串行 \~60 分钟）；

* **关键洞察**：先见后教安全可用（散文 = 纯增益，不碾压专门边反而抬升绝对强度）；v16 的校准负担（他→上课 1 处）根因 = **教学量不足 + PRE 位"在"竞争**两因叠加，翻倍教学量 + exclude 修复后归零；字级 OOV（v14 方案）是 v18 方向，与 v17 正交。

**⑨ s3 幼小衔接颗粒度细化（⏳ 设计 v2.17，用户 2026-08-10："s3 颗粒度还不够，从幼儿教育下手，现在是幼小衔接阶段"）**：

**定位**：s3 对标 4-6 岁复杂句（§三 对照表）→ 现升级为**幼小衔接阶段（5-6 岁大班→小学）收口**——用户原则：**s3 至少能说一句"像模像样的话"才能进 s4**（否则 s4 效果大打折扣）。依据（2026-08-10 查证）：
- **《3-6岁儿童学习与发展指南》（教育部）5-6 岁语言目标**：听不懂或有疑问时能**主动提问**；能理解**因果、假设**等相对复杂句——网络"问为什么/什么是"有官方幼教依据
- **VB-MAPP**：问答应答 = **内语言 Intraverbal（四操作项最后缺口，v2.7 标注 ❌）**；L2 里程碑 9 = 回答"What"问题（25 项目、3 种语言线索）；前提 = 命名 200+ 词 + 回答 25+ "什么"问题
- **李宇明/陈前瑞《语言的理解与发生》**：因果认知顺序——**"据因推果"（因为→所以）比"以果溯因"（为什么→因为）更容易**（166 条已教容易方向，"为什么"是更难方向 = 合理进阶）；儿童问句 3 岁后完善、4 岁后"为什么"大量使用
- **诚实标注**：反问/设问是**小学中年级句式概念**（幼儿园《指南》不要求）——定位为"小学衔接预备"（幼儿口语中已出现"你不是吃饭了吗"类反问、叙述中已出现设问自答）

**子阶段（颗粒度细化）**：

| 子阶段 | 内容 | 数据/知识源 | 机制 | 状态 |
|---|---|---|---|---|
| s3.1 定式关系句 | 配对关系句 + 内容小句（因为/虽然/先） | stage3_rel_v3.json 166 条 | 状态机读取 + 先见后教 | ✅ v14-v17 |
| s3.2 句内修饰（定状补） | 状语（今天/已经/正在/马上/常常）+ 定语（大/红/漂亮+名词）+ 补语（完/到/起来/干净）——**现状：166 条只覆盖 4 状语（今天/了/不/仍然），定语补语 0 教学；词表全部有料** | 定状补模板（词表内，不扩词表） | 角色词集加 DET/ADV/CMP 位 | ⏳ 数据扩量 |
| s3.3 问答应答（Intraverbal） | "什么是X"（苹果→可以吃/是食物）+ "为什么X"（为什么带伞→因为下雨）+ "X会怎样"（下雨→要带伞） | stage25 义原类别 hub（食物/动物/颜色/时间/地点已建）+ 166 条正反向 | 问答教学 RL（判定链/奖励/示范/渐隐/streak） | ⏳ |
| s3.4 主动提问（Mand 扩展） | 网络问"为什么X/什么是X/怎么办"——读到未知/异常（低置信/无路径）时发起提问 | 提问模板 | 低置信触发提问（衔接 §4.8 元认知"该不该开口"） | ⏳ |
| s3.5 反问/设问（小学衔接预备） | 口语反问（"你不是吃饭了吗"）+ 设问（自问自答"什么是苹果？苹果可以吃"） | 反问/设问模板 | 虚词层（难道/吗）+ 自问自答链 | ⏳ |
| s3.6 对话能力验收 | **"能说一句像模像样的话"** = 自然语句输出（虚词层 NAT_FILL + naturalize_s3 已实现）+ 三层问答 streak 15 通过 + 主动提问触发 + EVAL 回归 1.000 | — | 达标才进 s4 | ⏳ |

**教学法（幼儿教育对齐）**：
- **情景还原法**：教师示范完整句（"小熊因为饿了所以要吃饭"）→ 网络跟读 → **渐隐独立**（fade 已有）→ 独立奖励 ×2——幼儿式"示范→模仿→独立"
- **提问引导 + 5W1H 扩充**：避免刻板问答（VB-MAPP 评估指南：强化正确反应、分散测试、不纠正错误——错题由下一轮示范补位）
- **因果教学顺序**：先"据因推果"（已有）→ 后"以果溯因"（新教，难）
- **技术要点**：网络只有 W_out（无逆向边）——"为什么"靠**教学固化逆向锚点边**（"带伞→因为"×N，示范+重复，幼儿也是教出来的），不加 W_in 大改
- **与现有机制衔接**：对话教学 RL 框架（_probe_dialog_v17.py：判定链/奖励/处罚/话题块/渐隐/重复/streak 15/逐句留档/快照）直接复用；"什么是X"用 stage25 义原类别 hub（Stage 2.5 产物）；"为什么X"用 166 条因果反向 + 逆向锚点固化

#### 5. 文章单元（Stage 4 → 4.5：句间逻辑 → 文章类型）

| 步      | 内容                                          | 数据                    | 机制                   | 状态    |
| ------ | ------------------------------------------- | --------------------- | -------------------- | ----- |
| ① 是什么  | 句接句转移 + 整篇模式（句 A → 句 B）                     | 句对 + 文章定式             | 句级转移定式（CLS 慢系统）      | ⏳ 未开发 |
| ② 怎么造  | 接续句 B（输入句 A 涟漪 → 唤起句 B）+ 定式组合               | 句对 + 主题句集             | 句接句转移 + 句子角色 hub     | ⏳ 未开发 |
| ③ 造对的  | 接续不跑题（句间逻辑链）+ 文体识别正确                        | 类型标注文章（toutiao 15 类等） | 句子角色约束 + 类型 hub 举一反三 | ⏳ 未开发 |
| ④ 为什么错 | 接续错误/文体误判归因 + LLM 原因                        | 错误接续 + 归因记录           | 连接级归因处罚 + LLM 教师（通用） | ⏳ 未开发 |
| ⑤ 自己判断 | 自判"这句接得上吗/这篇是什么文体"——**衔接 §4.8：答得出就说、答不出不说** | 同 ③                   | 结构检查自判 + 阈值标定        | ⏳ 未开发 |

关键设计：④ 的归因对象 = 错误句间转移边/文体误判边；⑤ 的自判是 §4.8 元认知预备的最佳落点（QA 语料子集"答对固化/答错拒绝"已在 Stage 4 设计，见下）。文章单元是"复刻闭环"的完整考验（单元最大、关系最复杂）。

**⑤b 课后练习规划（文章单元）**：

* **基础档**：训练句对复测（句 A → 句 B 接得上吗？）——自判 = 句间转移直连可达性

* **巩固档**：新接续（训练过的句 A + 没配过的句 B）——主题句集二跳

* **拓展档**：文体识别（这篇是什么文体？）——类型 hub 举一反三（复用类别机制）；跨主题接续

* **变式**：正向（接得上）/ 逆向（接不上：跑题）/ 临界（模糊主题——句 A 可接多条句 B，判"哪个最相关"，低置信判"不知道"）

* **穿插**：多主题交错（足球/电商/天气混出），防"见句就续"定式

* **批改**：LLM 教师讲评（"这句接续跑题：它和上句没有共享主题词，转移边弱…"）——接续"为什么对/错"= 句间逻辑链的解释

#### 6. 推理单元（Stage 5：数学推理 + 逻辑推理）

**两条线（2026-08-10 用户决策"调整数学内容放到推理单元"：原 Stage 2.7 数字结构 + 2.8 数字运算 重定位至此，见下 §Stage 5 详解）**——**数学推理线**（数字结构 → 口诀/竖式 → 数值意义，先形式后意义）逐层走完五步闭环；**逻辑推理线**（类比/三段论/条件推理）走链补全。

| 步      | 内容                        | 数据          | 机制                   | 状态    |
| ------ | ------------------------- | ----------- | -------------------- | ----- |
| ① 是什么  | 逻辑链复述（前提→结论 模式）           | 推理语料        | 概念涟漪 + 涟漪转移          | ⏳ 未开发 |
| ② 怎么造  | 多步逻辑链补全（A→B→C）            | 三段论/类比样例    | 概念涟漪 ↔ 涟漪转移 + 泛化     | ⏳ 未开发 |
| ③ 造对的  | 逻辑合法（前提→结论 推理有效）          | 逻辑判断题 + 负例  | 验证门：对→固化、错→拒绝        | ⏳ 未开发 |
| ④ 为什么错 | 逻辑错误归因（无效推理靠哪条突触）+ LLM 原因 | 错误推理 + 归因记录 | 连接级归因处罚 + LLM 教师（通用） | ⏳ 未开发 |
| ⑤ 自己判断 | 自判"这个推理成立吗"（最高阶自判）        | 同 ③         | 结构检查自判 + 阈值标定        | ⏳ 未开发 |

关键设计：④ 是五步闭环的终点形态——推理错误的归因 + 解释 = 元认知（§4.8 预备的完整体）；⑤ 的自判直接支撑"AI 慢下来、公众可理解"愿景（网络能解释自己的推理为什么成立/不成立）。

**⑤b 课后练习规划（推理单元）**：

* **基础档**：训练逻辑链复测（A→B→C 成立吗？）；口诀复测（3+4=7 对吗？）——自判 = 链路/查表可达性

* **巩固档**：新前提组合（换概念，同构推理）；新口诀题（训练过的数、没查过的表）——涟漪转移泛化 / 查表泛化

* **拓展档**：类比迁移（换领域同构，如 苹果:水果 = 狗:?）——类别 hub 二跳；竖式进位（23+18=41）——多步序列（诚实难点，§12.5 过程执行器）

* **变式**：正向（成立/对）/ 逆向（不成立：无效推理；口诀误查）/ 临界（看似成立实则无效——偷换概念；进位临界"99→100"——最高阶自判，LLM 教师裁决）

* **穿插**：三段论/类比/条件推理/口诀/竖式混出，逼辨"用哪种推理"

* **批改**：LLM 教师讲评错因（"大前提不成立：所有鸟都会飞 是假，因为企鹅不会飞…""进位漏了：个位满 10 要向十位进 1"）——自判 + 解释 = 网络元认知的完整体

***

### Stage 0 词级（复述/命名）——数据：词表

* **数据**：专门词表（高频词 + 类别标签，如 {苹果, 香蕉, 汽车, 奔跑}）

* **训练**：每词反复经历（Hebbian 模式固化），输入词 → 回响词

* **验收**：词复述率（输入 w 判读 w）≥ 0.95；**零遗忘**（全部词学完，前面的还在）

* **n**：起于小规模（26 字母 n 级，如 256-512）

### Stage 1 词组级（组合）——数据：词对/词组

* **数据**：专门词组（主谓/动宾/修饰：苹果好吃 / 妈妈抱 / 很快）

* **训练**：词组反复经历，沉淀词组内转移（w1→w2 定式）+ 词组模式

* **验收**：词组内续词 top-1；"先学 a 再学组合，原子保留"（26 字母 combo 实验口径）

* **n**：首次扩容（旧规划 512 → 1024 为早期"1 词 1 神经元"口径，已废弃；实际按 §六分配制 K=4 + 按需扩容）

### Stage 2 短句级（句内结构）——数据：规范短句

**状态：✅ 已完成（2026-08-10，快照 v7.0，报告见 §12 后附）**——五步闭环进度（词/句单元通用）：① 是什么 ✅ v7.0（整句复述）

* **数据**：专门短句（≤8 词主谓宾，无噪声：我要苹果 / 他喜欢吃西瓜）

* **训练**：短句反复经历（**跟读 + 复读**），整句涟漪 = 句模式（记忆单元）

* **验收**：句复述率（输入整句→唤起整句）≥ 0.95；整句涟漪匹配（主题唤起正确率）

* **实测（v7.0）**：句复述率 **0.9845** ✅；前缀唤起 0.2277（309/499 句）✅；字/词复述 1.0000 零遗忘 ✅

* **n**：扩容（实测 n 58096 → 148140；旧规划 1024 → 2048-4096 为早期"1 词 1 神经元"口径，已废弃）

### Stage 2 自我表达（表达侧补课，⏳ v2.18 设计，2026-08-10 用户："早教也没做好，怎么教网络自我表达" + "从网络的角度出发，网络只能听和说，类似自闭症阶段"）

**定位**：早教阶段"表达侧"补课——网络从"应答机"（被问才说）变成"会表达"（主动发起）。**网络 = 自闭症类比**（v2.7 盲人类比 + v13 述情障碍查证：50% 自闭症人群有信号说不出）：只能听说、无视觉、有内感受边（X→痛）但说不出、刻板语言（echolalia 反复说"但是我想了"）、社交发起缺陷（只会应答从不主动说）、需求表达缺失（FCT 干预对象）。教学对齐 ABA/DTT/NDBI + FCT + 内感受→情绪词绑定（v2.8/v13 已落地机制复用：fade 渐隐/强化尝试/中性纠错/偶发教学/拒绝=最高奖励）。

**教学链**：感知（已有：X→痛 体验边）→ 行为（已有 v13：疼→不要）→ **表达（新：疼→"我疼"）→ 社交（新："我疼，帮帮我"）**

**五层教学（每层 LLM 教师 + streak 15 通过）**：

| 层 | 内容 | 示例 | LLM 教师角色 |
|---|---|---|---|
| 层1 命名（Tact） | 感受词复述 | "疼"→复述"疼" | 示范 + 肯定 |
| 层2 内感受→表达绑定 | 情境→状态表达 | "你碰了石头"→"我疼" | **情境创设**（8 状态词 × 情境池）+ 示范 |
| 层3 FCT 扩展 | 拒绝（已有）/求助/需求 | "我疼，帮帮我""我要喝水" | 情境引发 + 示范重铸 |
| 层4 自发发起（Mand 完整版） | 无提示主动表达 | 读到"渴"→主动"我要喝水" | 撤情境（渐隐）+ 独立奖励 ×2 |
| 层5 感受+原因 | 感受 × s3 因果 | "我难过，因为下雨不能去公园" | 示范完整句 + 讲评 |

**LLM 教师一次调用（对齐 _speak 六节压缩模式）**：【情境】【表达判定】【质量原因】【教师反馈】【示范句】【下个情境】——四角色一体（情境创设者/示范者/讲评者/接纳者）；无 key/失败回退规则（模板情境 + 期望句示范）。

**每轮流程（结合 LLM 教师）**：
1. LLM 教师给情境（文本，如"你碰了石头，很疼"）→ 网络读到情境词 → 表达
2. 判定分层：规则层（表达含状态词/需求词/拒绝词？对应情境类型）+ LLM 层（表达与情境贴切度："碰石头很疼"→"我疼"✓、"我开心"✗）
3. 奖励/处罚（RL 复用）：贴切 → 学期望句 ×1（独立 ×2）；不贴切/说不出 → LLM 示范"来，跟老师说：我疼，帮帮我"→ 跟读 → 渐隐；拒绝坏东西 → 100 分最高奖励（v13 延续）；重复 → 减半（echolalia 治理）
4. streak 15 通过（每层独立，错一次清零）

**数据**：情境池（LLM 生成/模板）：疼/饿/渴/累/冷/热/难过/开心/害怕/生气 × 每状态 5-10 情境；期望表达模板（全词表内，已查证：帮/帮忙/疼/痛/饿/渴/觉得/喜欢 全在表）；**零词表扩展**。

**诚实边界**：网络"我疼"= v13 体验边（X→痛）驱动的文本表达——比 v2.7 Verbalism（纯文本关联）更进一步（有体验基础），但仍是文本输出，感知缺口照旧标注（v2.7 已立）。

**验收**：各层 streak 15 通过（层4 自发：无情境提示主动表达）+ 自然语句输出（naturalize 扩展"我疼，帮帮我"）+ EVAL 回归（v17 成果不破坏）+ 逐句留档（talk_log/dialog.md）+ 教学后快照。

**脚本**：`_grow_self_express.py`（复用 `_probe_dialog_v17.py` 框架：判定链/奖励/处罚/话题块/渐隐/重复/streak/逐句留档/快照 + LLM 教师）

### Stage 2.5 词语类别（v2.4 改造：词典学词义）——数据：OpenHowNet 义原判断题

**状态：✅ 已完成（2026-08-10，快照 v8.5，实测见本段末，报告见 docs/reports/\[REPORT]-定式网络Stage2.5词典学词义实验报告）**——五步闭环进度（词单元）：① 是什么 ✅ v1.0/v2.0 → ② 怎么造 ✅ v8.5 → ③ 造对的 ✅ v8.5 → ④⑤ ⏳ 词单元 v13（详见 §四"学习单元闭环明细"表 1）

**定位**：补词级（Stage 0-1）与句级（Stage 2）之间缺失的"语义角色层"。v2.4 改造源于用户诊断——**"词有了、词义没有"**：词形复述 1.0（模式已固化），但同类词在 stage2\_sents 中几乎不同句共现（探测：食物仅 {蔬菜,猪肉} 一个 2 词块），词义（传出突触结构）孤立 → 举一反三无路可走。跟读（监督）只能焊"清单直连"，学不会词义；改用**词典（OpenHowNet 义原）+ RL 验证门**。

* **词义 = 词↔义原关联集合（传出突触结构）**：定式网络里"词义"不是额外标签，就是这个词能唤起什么、被什么唤起。词典直接给出词↔义原连接，绕开"语料无共现"的根因。

* **数据源：OpenHowNet（知网，清华 THUNLP 开源，MIT）**——约 2000 义原标注 20 万+ 中英文概念。实测 v7.0 词表 31819 词中 16781 词（**52.7%**）可查，5 万+ 词↔义原边。义原天然分两层：**类别义原**（fruit|水果、animal|动物、InstitutePlace|场所 → 类别 hub，供 Stage 2.6 槽位约束）+ **属性/功能义原**（edible|可食、study|学习 → 感知/行为通道）。

* **机制 = RL 验证门（§12.6 三因子规则）**：每条词↔义原关系 = 一道判断题——

  * "苹果是水果？对" → 验证门固化（苹果→水果 边）

  * "苹果是咸的？错" → 验证门拒绝固化（错误联想不焊死）

  * 词义 = **经验证门确认**的传出突触集合：可查（传出突触直读）、可扩展（新判断题）、可拒错（防认知偏差固化）

  * 多义原词取并集 = 多通道（类别 + 功能 + 来源），与 §12.7 语义链（苹果→水果→食物）同机制

* **类别举一反三**：与标签共享义原的未训练词（hold-out），凭其与类别成员共享的义原传出突触走链唤起标签——**与验证门走链同一检索机制**；未覆盖词（无义原）自然作"知道/不知道"对照（衔接 §4.8）

* **诚实边界**：① OpenHowNet 义原以**类别/功能/来源**为主，**不标注**"甜/圆/红"这类感知属性（视觉/味觉/触觉通道数据缺失，留待 Stage 4 用自然语言定义句补充，如 Wiki 定义句）；② 覆盖率 52.7%：数字/专名/新词无词义边，正好是"真不知道"的自然样本；③ 语法词性（名词/动词/形容词）仍需外部标注，作后续进阶（动作类≈动词类先近似）

* **验收**：① 类别归属（训练词→标签义原直连 ≥0.9）；② 词义举一反三（hold-out 词→本类信号占优 ≥50%）；③ 替换造句（"我要"→食物类词唤起，类别约束的槽位填充雏形）；④ 字/词/句全链零遗忘

* **实测（v8.5，全量 5 类：食物/动物/颜色/时间/地点；动作类跳过见诚实边界）**：

  * ① 类别归属 **1.0000** ✅（训练成员→本类标签直连，5 类全过）

  * ② 词义举一反三 **1.0000** ✅（hold-out 3 例/类：坐果/挂果/瓜子→食物、刺猬/北极熊/四不像→动物、红线/循环/染红→颜色、一世/一个半月/一代→时间、全日空/双人间/巷道→地点，本类信号占优 8-48 权重）

  * ③ 替换造句食物信号 **28.8 ≥ 他类 38.4×0.5** ✅（直接命中 土豆/饼干/苹果）

  * ④ 零遗忘：字 1.0000 / 词 1.0000 / 句 0.9324（base 0.9355）✅

  * 验证门拒绝 **54/54** 负例边未增长 ✅（"错"判断题不焊死）

* **实测暴露（数据驱动修正，2026-08-10）**：① HowNet 类别义原（动物/颜色/场所）覆盖的是"下巴/底色/一世"这类词，与日常类别词错位 → 标签组按人工词表词的义原**扩组**（食物+食品/蔬菜、动物+牲畜/走兽/虫、颜色+单字色名、地点+设施/房间）；② **动作类（吃/跑/跳…）HowNet 覆盖 0** → 本阶段诚实跳过（词性标注后补）；③ 成员筛选须"义原含本类标签"（"后来/最近"义原为将来/新近，无"时间"标签，排除）；④ 大网络虚词劫持（"我"→的 256）使单链走链失效 → 验收③改用传出突触直读聚合（与验收②同构）

* **脚本**：`_grow_cat.py`（load v7.0 → 义原分配 → 验证门判断题 → 四类验收 → save\_snapshot）

### Stage 2.6 主谓宾句式（✅ 已完成 2026-08-10，快照 v10.0（v9.0 动宾迭代，病句拒绝），脚本 `_grow_svo.py`；报告见 docs/reports/\[REPORT]-定式网络Stage2.6主谓宾句式实验报告-SVO槽位与造句.md；**v11 迭代（RL 颗粒度升级 + LLM 教师）✅ 已完成 2026-08-10，快照 v11.0（规则版）/ v11.1（LLM 教师首版）/ v11.2（LLM 教师纯语义版），脚本** **`_grow_v11.py`，报告见 docs/reports/\[REPORT]-定式网络Stage2.6v11迭代实验报告-RL颗粒度升级连接级归因处罚与LLM教师.md；LLM 教师接入实测：判断 242 次、病句 LLM 原因 174/177、prompt 锚定修复（"看石头"验证），铁律 5 五步闭环详见表下**）——数据：S/V/O 槽位化短句

**定位**：从"这句话"（Stage 2 记忆）到"这类句子"（句法规则泛化）的跨越——**造句 = 按定式组合没学过的句子**。人掌握"主谓宾"不是记句子，是掌握"S 位放主体、V 位放动作、O 位放对象"的抽象定式。

**词序编码形态（v2.5 决定：角色槽位 hub 绑定）**：

* 3 个槽位模式（S 槽/V 槽/O 槽，各 K 神经元），词↔槽位**共发放绑定**（"我"↔S 槽、"吃"↔V 槽、"苹果"↔O 槽）——复用 §12.6 已验证的"角色槽位表征"机制（传递模板实验 \[主语位 S+流动] 同构）

* **SVO 固定语序下角色绑定 ≡ 位置编码**：S 槽恒第 1 位、V 恒第 2、O 恒第 3，词绑定到哪个槽位即词序，无需额外位置签名

* 整句槽位化跟读 \[S槽+我]\[V槽+吃]\[O槽+苹果] 反复共发放 → 沉淀**槽位间连接（定式：S槽→V槽→O槽）**+ 词间连接

* **弃 STDP 方向连接作主机制**：① 大网络虚词劫持已证单链走链失效（2.5 实测"我"→的 256）② 方向连接要求注入顺序而跟读需清 trace（2.5 已验证）；方向留作可选强化，单独验证防共享字污染

* **诚实边界**：本阶段只学"SVO 固定序"（规范主谓宾）；把字句/被字句等变序、宾语前置后置 → 后续阶段

**机制三层（全部复用既有原语，无新训练机制）**：

| 层      | 学什么              | 机制                    | 复用            |
| ------ | ---------------- | --------------------- | ------------- |
| 词↔槽位绑定 | 我→S槽、吃→V槽、苹果→O槽  | 判断题"吃 是 动词位？对"（judge） | §2.5 judge    |
| 类别约束   | 吃→动作、苹果→食物（沿用四类） | 判断题 + 类别 hub          | §2.5 judge    |
| 定式     | S槽→V槽→O槽 + 词间    | 整句槽位化跟读（一次共发放）        | §2.5 judge 同构 |

**V 位动作类（v2.5 数据驱动，jieba 补 2.5 缺口）**：`_probe_svo.py` 探测 v8.5 词表——jieba.posseg 动词覆盖 **6049 词（19.3%）**；人工动作种子词 25/30 直接命中（吃/喝/买/看/学/要/拿/做/写/读/玩/去/来/打/跑/走/坐/站/听/说/踢/跳/洗/戴/养/修/借 全在网络且标 v）。**训练成员 = 人工动作词表（30 种子 + 双字动词抽查）∩ v8.5 网络**，标签"动作"（新类别 hub）。诚实边界：① jieba 有噪声（学/画 标 n、喝 vg/穿 zg/种 m）——人工词表兜底，jieba 只作候选发现；② 动词的语义义原（HowNet 覆盖 0）仍缺，本阶段只有"类别归属"（V 位约束原料），动词完整词义后补；③ 能愿/否定/判断词（不会/不能/没有/应该）不选入动作训练成员（造句 V 位用真动作动词 + 轻动词"要"）

**S 位人称类（v2.5 探测发现）**：jieba 把 我/他/她 标 r（代词）不进名词类 → S 位 = **人称类（人工词表：我/你/他/她/我们/你们/他们…）+ 名词类成员**（2.5 四类里动物类可当 S：猫吃鱼）。O 位 = 2.5 四类成员 + 泛名词（书/球/水…）

**动宾搭配约束（v2.5 增补 2026-08-10 用户追问"正常造句教学/举一反三训练"，v9.0 后迭代）**：v9.0 造句候选不随 S/V 变化（缺动宾语义）——V 词只学"是动作类"，没学"吃配食物、看配地点"。教学参照（查证：小学造句教学把"我吃了一个书"列为词语搭配不当的病句；搭配能力来自**生活场景例句**反复接触 + 仿写，非背搭配表）。**探测**：① OpenHowNet V 词义原覆盖全 0；② raw 语料（外卖/头条/网购）语域不匹配（"吃"后接时候/味道，学/画/踢/洗/读 共现 0）；③ stage2\_sents 是 QA 句 → **搭配数据 = 教材式例句构造 + jieba 提取共现对**（教学同款"例句驱动"；未来有真实儿童语料可直接替换源）：

* **训练（共现，非类别表）**：教材式例句（"我吃苹果""爸爸喝牛奶""妈妈买西瓜""我们去公园"…）→ jieba 切分提取 (V, O名词) 共现对 → judge 固化 **V↔宾语词**。V 词传出突触 = 搭配结构（吃→苹果/米饭/西瓜…全食物）

* **举一反三（类别泛化，2.5 同款机制）**：宾语词↔类别标签边（2.5 已有）→ "吃"连过的食物词经"食物"标签唤起**未共现的同类词**（吃+香蕉：香蕉没跟吃训过，凭 吃→苹果→食物→香蕉 链成立）——从典型例句学、往同类泛化

* **病句拒绝（不固化即拒绝）**：语料无"吃+学校"共现 → 吃 传出突触无学校 → 学校进不了候选（负例不进正例，RL 验证门哲学："验证门管该不该记"）

* **聚合机制（v9.0 大改）**：候选主源 = **V 词传出突触聚合**（直接搭配词 + 2 跳类别泛化），**O槽 只做定式路径确认**（不再提供词权重）——"吃什么"由"吃"的搭配决定，候选随动词变化；无搭配词可学的动词（学/踢/读/听：学书/踢球/读报/听歌的宾语不在 O 集）→ 回退 O槽 保底泛名词池

* 诚实边界：例句为构造（教材式，无真实日常语料——评论/新闻语域不匹配已探测）；搭配表非穷尽（"买"还可接非食物商品）；"看"接地点是 O 集限制下的妥协（看风景/看家更自然）；动词完整词义仍缺，搭配是"共现结构 + 类别泛化"级

**学习理论对标（RL ↔ 人类教学法，用户 2026-08-10 要求显式对齐）**——Stage 2.6 每个机制都有强化学习对应物 + 真实语文教学对应物（教学法查证：小学造句教学"谁+干什么+什么"句型操练、句型替换、仿写、扩句/缩句、病句=词语搭配不当、生活场景例句驱动、填空/看图说话）：

| Stage 2.6 机制                  | 强化学习对标                                                       | 人类实际教学对标                          |
| ----------------------------- | ------------------------------------------------------------ | --------------------------------- |
| 槽位绑定判断（词↔槽位 共发放）              | 状态表征：词 → 句法角色（state encoding，为策略提供可泛化的状态空间）                  | 句子成分教学：给词定性（谁是主语/谓语/宾语）           |
| 定式共发放（S槽→V槽→O槽）               | 动作序列策略：合法转移（S 后接 V、V 后接 O 的转移结构）                             | 句型教学：主谓宾固定顺序——先学定式再填词             |
| 类别判断题（w↔标签：对→固化）              | Reward Model（奖励模型）：对"w 属于 c"给出正奖励，驱动 policy 更新               | 教师提问确认：苹果是食物吗？→ 答对获表扬（肯定反馈）       |
| 负例拒绝（错→不固化）                   | RLHF 负反馈/惩罚项：错误样本**不产生奖励、不更新 policy**（2.5 已实现 neg\_rejected） | 病句纠错：我吃了一个书 → 教师否定，学生不再使用         |
| 语料共现搭配（V↔宾语词 judge）           | 环境奖励估计：从交互样本统计 reward 信号（无监督 reward，非人工标注）                   | 例句模仿积累：反复接触"我吃苹果/我想买一个苹果"，高频搭配成习惯 |
| 举一反三（吃→苹果→食物→香蕉）              | 奖励泛化/策略迁移：相似状态共享策略（state abstraction 归纳偏置）                   | 迁移教学：学会"吃苹果"自然会说"吃香蕉"（同语义类迁移）     |
| 验证门 gate（§12，该不该记）            | Critic / reward 阈值：验证通过才放样本进学习（决定 sample 是否用于更新）             | 教师把关：先判断对错，再决定教不教/记不记             |
| 槽位↔类别约束（V槽↔动作、O槽↔食物/地点）       | 动作合法性约束：当前状态下可用 action 集合（可用集过滤）                             | 搭配教学：动词后接什么词类、宾语位装什么（"吃"后不接地名）    |
| 造句引擎（S+V → O 候选走链读出）          | 策略执行：给定 state 选合法 action（含可行性检查/路径确认）                        | 句型操练：给"谁+干什么+\_\_\_\_"框架填空、仿写     |
| 零遗忘验收（字/词/句 + 2.5 + hold-out） | 持续学习/防灾难遗忘（catastrophic forgetting 防护，old 知识不回退）             | 复习巩固：学新不忘旧（语文课的复习机制）              |
| 验收阈值（≥0.9/≥0.5）               | reward threshold / 收敛判据（达到熟练度标准才推进）                          | 及格线：达标才进入下一教学单元                   |

**验收数据（训练/测试组合分离——造句 ≠ 背句）**：

* S 集：人称 5-8 + 动物 5（猫/狗/鸟/鱼/兔子）≈ 10；V 集：动作 10（吃/喝/买/看/学/画/踢/洗/读/听…）；O 集：食物 8 + 地点 4 ≈ 12

* 全组合 ≈ 1200 条 → **训练 800 / 测试 400**（测试三元组 (S2,V2,O2) 三词各已单独训练槽位绑定与类别，但组合从未共同出现）

* 词↔槽位绑定与类别判断**全体词单独训**（不随组合切分）；hold-out：每类留 1-2 词不训类别判断（§2.5 机制，凭共享信号唤起）

**验收（四道，沿用 2.5 传出突触直读聚合防虚词劫持）**：

1. **槽位归属**：训练词→S/V/O 槽位直连 ≥0.9（我→S槽、吃→V槽、苹果→O槽）
2. **类别约束**：动作类成员→"动作"标签 ≥0.9；S/O 位沿用 2.5 四类归属不回退
3. **造句泛化（核心，两级）**：

   * ③a 定式唤起：输入 S2 → 传出突触聚合唤起 V 槽词（动作类信号）；输入 \[S2+V2] → 唤起 O 槽词（宾语类信号）——定式成立

   * ③b 槽位填充（新组合 = 造句）：输入 \[S2+V2] → 宾语位唤起**未组合过的** O 类词（凭"O 类标签"边被宾语位约束唤起，如"我要水"的"水"从未在"我要"组合训过）——信号占优 ≥50%（2.5 hold-out 口径）。**v2.5 增补（动宾约束后）**：配了搭配的动词 → top-8 中**该动词搭配类别占优 ≥0.5**（候选随动词变化）；**病句拒绝**：异类组合（吃+学校、喝+公园）→ 该异类词**不得进入 top-8**（语料无共现 → 无边）；未配搭配的动词（学/踢/读/听）保持宾语位名词占比 ≥0.5（诚实留白）
4. **全链零遗忘**：字/词/句复述 + 2.5 四类归属 + hold-out 全不回退

**n 增量（估算）**：3 槽位 + 动作标签 + 人称标签 + 动作成员 \~30 + 人称成员 \~7 ≈ 42 模式 × K=4 ≈ 170 神经元（v8.5 n=148604，游标续用一次扩容几百）

**实测结果（2026-08-10 全量，详见报告）**：槽位归属 1.0 / 类别约束 1.0 / 定式成立（S槽→V槽 4.8、V槽→O槽 4.8、槽位↔类别 8.0）/ 造句填充 **400 测试组合 100%** 宾语位名词 top-8 占比 1.0 / 零遗忘（字词句 + 2.5 四类 + hold-out 15/15）。冒烟→全量修正：定式 6 模式全连接稀释 O 位约束（0.38<0.5）→ **分离注入**（定式只训三槽，绑定/约束用判断句单独强训）；标签传出突触 1:1 聚合被 HowNet 噪声劫持 → O槽 传出突触为主源 + 标签 ×0.3 折扣。

**v11 迭代设计（2026-08-10，RL 颗粒度升级 + 处罚原因；✅ 已完成，v11.0 规则版 / v11.1 LLM 教师首版 / v11.2 LLM 教师纯语义版，实测数据见设计末）**——用户追问"造句只知可不可以、不知为什么"+"现在 RL 颗粒度够吗"：

* **问题定性（统计 vs 价值 + 颗粒度缺口）**：v10 的动宾搭配是**统计共现**（吃→苹果因例句共现、吃↛学校因无共现），网络只有关联强度、没有后果；生物是**价值学习**（吃→食物因后果好、吃↛石头因痛觉预期）。**颗粒度缺口**：现有 RL 奖励是**样本级二元标量**（对/错打回整次共发放），而"为什么错"是**连接级**（哪条突触把错词带进候选）——处罚原因必须落到具体连接，现有验证门做不到。

* **文献对标（查证 2026-08-10）**：RLBFF（ICLR 2026，NVIDIA）——奖励是**原则粒度**（每条原则一个二元判断："石头是食物吗？否"），奖励自带"为什么"（哪条原则不满足）；RLVR（DeepSeek R1）——**规则验证器自动判对错**，零人工标注；Explainable RLHF（NeurIPS 2025）——**归因粒度**：定位导致错误响应的训练数据 → 修正；RL with Human Advice（Frontiers 2021）——区分评价性/纠正性反馈，explanations 是建议的一种形式。

* **设计（核心 = RL 颗粒度升级）**：

  1. **RL 颗粒度升级：连接级归因处罚（核心设计点，v2.6 修订）**——"错"从样本级升级到连接级：

     * **归因**：错误发生时（如"吃+石头"进了候选），反向追踪候选词**靠哪条突触进入 top**（石头→O槽？食物标签→石头？O槽 保底？）→ 定位具体连接 = 处罚落点（对齐 Explainable RLHF 归因→修正；RL 信用分配 credit assignment）

     * **处罚执行（连接级）**：落点边**显式减权 → 删除**（排斥 = 边不存在；稀疏网络 W 无负权重，删除即"惩罚结果"）；不是整次打回，是纠正具体连接

     * **原则级奖励**：验证器输出"每条原则的二元判断"（石头是食物吗？否）而非整句一个对/错（对齐 RLBFF 原则粒度）
  2. **LLM 教师（判断 + 原因一体，2026-08-10 用户决策）**——用户定：**强化学习时的判断由 LLM 负责**（不止处罚原因）：

     * **判断**："吃石头对吗？" → LLM 判"错"（LLM-as-Judge / RLAIF：用 LLM 反馈代替人工/规则验证）；"看石头对吗？" → LLM 判"对"——**解决规则验证器的类别级误判**（"看石头"类别不匹配但语义合法，规则比对会误判负例，LLM 语义判断能识别）

     * **解释**：LLM 同时给自然语言处罚原因（"因为石头不是食物"）——判断与原因一体输出

     * **架构划分（教学对标）**：**LLM = 教师（判断 + 解释），网络 = 学生（归因处罚 + 记忆）**——LLM 不碰网络边；处罚仍由网络执行（归因 → 连接减权删除）；判断信号注入网络 = RL 奖励（教师认可固化 / 教师否定 → 网络归因处罚）

     * **双轨实现**：LLM 判断依赖 API（DeepSeek，环境变量 DEEPSEEK\_API\_KEY）；**无 key 时回退规则验证器**（类别比对，v10 已验）——规则验证 = 保底，LLM 验证 = 升级，冒烟/全量都可在无 key 下跑通，LLM 接入为配置项
  3. **LLM 受限解释器（自然语言原因）**：**归因记录**（哪条突触、权重、判定、处罚动作）→ 结构化诊断 → LLM 翻译为自然语言（"不能吃石头：因为石头不属于食物类（判定边），而吃通常搭配食物（共现边），候选由 O槽 传出突触带入 → 已删除该边"）。**诚实底线**：输入限死（只喂网络真实学到的边）+ 输出越界拦截（LLM 生成网络边里没有的内容 → 拦截/标注"非网络知识"）——LLM 是**翻译器（代嘴）不是知识源**，解释仍是网络自己认知的语言化

  * **否定知识表达**：`~食物` 否定 hub 降级为实现细节（**可不建**）——排斥用"边删除"表达（稀疏网络正权重减到 0 即不存在）；是否额外建否定 hub 由冒烟实测决定（负例频次高、需累积记忆时再建）

* **RL 对标（补 §2.6 表）**：连接级归因处罚 = 信用分配（credit assignment，奖励分配到具体连接）+ Explainable RLHF 归因修正；原则级二元奖励 = RLBFF；**LLM 教师（判断+原因） = RLAIF / LLM-as-Judge**（LLM 反馈代替人工/规则验证）；无 key 回退规则验证器 = 可验证奖励（RLVR）；约束强度差异（吃严格、看宽松）= 不同 (V,类别) 对的 Q 值分布；LLM 解释器 = 可解释 RL 的自然语言归因层

* **诚实边界**：① 网络"知道"错误原因的**原材料（边）与行为（拒绝），但说不出**（无语言通道），LLM 只补通道不补知识；② 归因粒度受"候选唤起路径可追踪"限制——多源聚合（主源+泛化+保底）时归因到主要贡献边，突触强度是正实数、无负向排斥权（删除 = 惩罚上限，无"反向惩罚边"累积）；③ **验证双轨边界**——LLM 教师可识别"看石头"合法（语义判断），但判断用**外部知识**（教师知识，非网络边）；无 key 回退规则验证器仍存类别级误判（"看石头"被误判负例）；④ 语义级（苹果皮不宜吃）判不了，留缺口；⑤ 价值是**次级价值**（教师信号），非生物第一性体感；⑥ "食物为什么能吃"（生理因果）仍缺（HowNet 动作义原覆盖 0）；⑦ **外部知识注入需标注**——LLM 教师的判断结论进入网络学习信号，但网络学到的仍是自己的边（归因处罚），"为什么"的解释来源（教师知识 vs 网络边）在每次诊断中显式区分

**v11 实测（2026-08-10 全量三版，详见报告）**：

* **v11.0（规则版，无 LLM）**：`runs/v11_0_20260810_043622/`，llm\_enabled=false——连接级归因处罚全量验证：归因 149/400、处罚动作 14 次、删边 224、病句拒绝 177 全过（模板原因 `[模板占位]`）

* **v11.1（LLM 教师首版）**：`runs/v11_1_20260810_045348/`，接入 DeepSeek API——LLM 判断 256 次、病句 LLM 原因 173/173；**实测暴露 prompt 上下文锚定**：喂"搭配类别是\[地点]"→ LLM 附和类别表，把"看石头"误判为错（"「看」搭配地点，石头非地点"）

* **v11.2（LLM 教师纯语义版，修复后全量）**：`runs/v11_2_20260810_050156/`——prompt 改"不要参考任何给定搭配表，仅凭语义常识判断"，allow 只留作越界检查实体 → **LLM 判断 242 次、与规则一致 142 次（一致率 0.587）、病句 LLM 原因 174/177**、重测 400 全过、全验收通过

* **分歧定性（一致率 0.587 非失败，是设计点）**：41% 分歧全是 **LLM 语义判断超越规则验证器**——"鸟+吃+牛奶"类别对但语义不自然（规则 ok、LLM 错）、"狗+要+西瓜"（规则 ok、LLM 错"狗不会主动要西瓜"）、"看石头/画石头"（规则误判负例、LLM 判对）

* **LLM 教师边界演示（`_grow_v11.py`** **演示块，不改网络）**：我吃苹果→对 / 我吃石头→错 / 我看石头→对（锚定修复后） / 我喝学校→错 / 我画石头→对 / 我买学校→错——教师超越规则验证器设计点落地

**学句子单元的完整闭环（五步，2026-08-10 用户定稿）**——"这是句子这个单元的学习过程，到后面也要复刻同样的过程"（铁律 5）：

| 步骤     | 内容                                                 | 教学阶段      | 版本          | 机制                           |
| ------ | -------------------------------------------------- | --------- | ----------- | ---------------------------- |
| ① 是什么  | 什么是句子（整句记忆单元）                                      | 识别        | v7.0        | 整句涟漪复述                       |
| ② 怎么造  | 按 SVO 定式组合**没学过**的句子                               | 生成        | v9.0        | 槽位绑定 + 定式走链                  |
| ③ 造对的  | 动宾搭配约束，病句拒绝                                        | 约束        | v10.0       | 例句共现 + 类别泛化                  |
| ④ 为什么错 | 错误归因到连接 + 自然语言原因                                   | 解释        | v11.0/11.1  | 连接级归因处罚 + LLM 教师             |
| ⑤ 自己判断 | 网络凭**自己结构**判断对错（**⑤a 先学怎么做判断 → ⑤b 课后练习 + 教师批改讲评**） | 内化 + 课后练习 | **v12.0 ✅** | 判断方法教学（示范）→ 结构检查自判 + 验证门阈值标定 |

**教学主客体阶段（对齐真实教学）**：教师主导（①②③：示范 + 外部验证）→ 教师 + 原因（④：判错并解释"为什么"）→ **学生内化 + 课后练习**（⑤：先学怎么做判断 → 自己练习判断 → 教师批改讲评）。对标人类：家长教说话 → 孩子学造句 → 老师说清病句原因 → 孩子学"怎么判断"、做练习 → 老师批改讲评。

**v12 迭代设计（网络自判，2026-08-10 用户决策，已实施 ✅ 快照 v12.0 parent=11.2，全量验收 260s 通过）**：

* **实测（v12.0 全量，2026-08-10）**：`_grow_v12.py` 在 v11.2 上迭代（不重训），LLM 教师启用（llm\_enabled=true）；⑤a 判断方法教学 4 示范（1 次误拒绝「我看石头」→ 固化正确路径）；⑤b 课后练习题集 28 道（三档×变式×穿插）→ 阶段1 一致率 基础 12/12=1.000 / 巩固 6/6=1.000 / 拓展 8/10=0.800 → 修正 1 次（误拒绝固化）→ 阶段4 复测 拓展 9/10=0.900（「他吃石头」自判"不知道"vs 教师"不可造"，保守诚实不修正）；错误收敛 1/1=1.000；继承 ✅（字 1.0000 / 词 1.0000 / 句 0.9232 / 2.5 类别 1.0000 / hold 15/15）；留档 runs/v12\_0\_20260810\_105435/result.json；报告见 docs/reports/。**smoke 实测修正**：教师基准基础/巩固 = 规则验证器（LLM 判"看公园"错是真实语义偏好）、拓展 = LLM 裁决；修正原则"一致→不动作、误放行→删来源边、误拒绝→固化、保守诚实→不动"；四阶段流程（全题自判→统一修正→错题复测→全题复测）。

* **v13 迭代（复杂句式练习题，2026-08-10，已实施 ✅ 快照 v13.0 parent=12.0，全量验收 220s 通过）**：用户需求"句子变复杂（我要苹果→我想要苹果、他看石头→他在看石头）+ 用复杂句式做练习题 + 答错/答不出由大模型告诉正确答案和解析 + **判定用代码不用 LLM**（2026-08-10 用户定：对错判断用代码规则，LLM 只负责正确答案和解析）"。A 型（S+想要族+O，想要/需要/想）+ B 型（S+在+V+O）12 道三档题（人工语义标注）；**教师判定 = 代码规则**——宽松动词 {看/想要/需要/买} 任意宾语可造（价值判断与句子合法性分开：想要石头能说）+ 严格动词类别比对（O∈V 搭配类别可造 / O 有类别但不在→不可造）；**网络自判 = 核心三元走 v12 self\_judge**（修饰词在/想 结构无信号 → 诚实标注"未判"）；修正 = v12 四原则（误拒绝 → 固化整句含修饰词，建立转移定式）；**LLM 讲评只对答错/答不出**（含保守诚实不动网络的不一致题）输出「正确答案 + 解析」。实测：阶段1 基础 2/4 / 巩固 2/4 / 拓展 2/4 → 5 次误拒绝固化全部收敛（我想要苹果/我需要牛奶/我想要香蕉/他想要饼干/我想要石头——含价值临界句按"语义能说"判可造）→ 阶段4 基础 4/4=1.000 / 巩固 4/4=1.000 / 拓展 3/4=0.750（「他在吃石头」自判不知道 vs 教师不可造，保守诚实不动网络 + LLM 讲评"石头不能吃"）；错误收敛 5/5=1.000；继承 ✅（字 1.0000 / 词 1.0000 / 句 0.9357 反升 / 类别 1.0000 / hold 15/15）；留档 runs/v13\_0\_20260810\_111247/result.json + runs/\_speak\_logs/{ts}\_complex\_exercise/result.json；报告见 docs/reports/。

* **目标**：判断内化——训练后网络凭自己的边结构**自己判断**"(S,V,O) 可造/不可造"，外部教师（规则验证器 / LLM）从"判断者"转岗为"批改讲评者"（课后练习的批改者，不是退场）

* **⑤a 学怎么做判断（判断方法教学）**：先教判断方法/标准——结构检查怎么查（O 在不在 V 的搭配传出突触可达集）、阈值怎么定（唤起强度多高算"可造"）→ 教师示范判断过程（给诊断样本，教师演示"怎么判"并讲方法）→ 网络把判断方法沉淀为**判断路径模板**（先查直连、再查二跳泛化、再判强度）

* **机制（复用 §4.8 信号，无新训练机制）**：

  * 自判 = **结构检查**：O 在不在 V 的搭配传出突触可达集（V直连 / 类别泛化二跳可达）+ **唤起强度**（候选 top 排名/强度、走链衰减）→ 二元输出"可造/不可造" + 强度置信

  * **"该不该开口"阈值标定**：验证门教——自判对 → 标"强判断"；自判错 → 教师纠正、标"该说不知道"（§4.8 阈值标定机制）

* **⑤b 课后练习 + 教师批改讲评（内化路径）**：v11 处罚沉淀即自判基础（删边后结构上"不可造"、固化后"可造"）——**LLM 教师判断 → 网络归因处罚/固化 → 边结构状态 = 自判依据**；网络做练习（自判判定）→ **教师批改**（对照 LLM 教师判断：一致率）+ **讲评错因**（自判错误样本 → 教师解释 → 网络归因修正）→ 收敛后进入下一单元（持续学习闭环）

* **⑤b 课后练习规划（句子单元，练习设计原则见 §四"课后练习设计原则"）**：

  * **基础档**：训练组合复测（我要苹果 可造？）——自判 = O 在 V 搭配传出突触直连可达

  * **巩固档**：新组合（三词都单训过、组合没学过，如 我们要水）——直连 + 类别泛化二跳

  * **拓展档**：类别泛化二跳（吃+香蕉：吃→苹果→食物→香蕉 链可达？）+ 跨类临界（看+石头：类别不匹配但语义合法——**LLM 教师裁决**，网络自判凭 看→地点类 边 + 泛化可达，诚实边界：语义合法性来自教师，网络只判结构）

  * **变式**：正向（可造）/ 逆向（不可造：吃+学校）/ 临界（画+家：v11.2 实测被噪声污染又治愈的样本，作临界题）

  * **穿插**：不同 V 的搭配交错出题（吃/看/画/喝混出），防"O槽 保底"定式（v11.2 一致率 0.587 的分歧样本正是穿插素材）

  * **批改**：LLM 教师讲评错因（"动宾搭配不当，公园是地点非食物"）——批改即 v11 原因的正式化

* **验收**：① 网络自判 vs LLM 教师判断**一致率**（训练后 self-judge 对照 teacher-judge，报告里显式并列两者判定）；② 自判错误样本的教师纠错收敛；③ 继承 v11 全验收零遗忘

* **诚实边界**：自判 = 图可达性检查（路径存在 + 强度），**不是"理解为什么"**（"为什么"仍靠 LLM 解释通道，见近期未来路线）；自判上限 = 网络已学边结构，未见组合凭类别泛化可达，泛化之外判"不知道"（衔接 §4.8）；判断置信 ≠ 语义理解

**未来路线（v11/v12 之后，跨 Stage 演进）**：

```
近期  解释通道内化：LLM 生成的解释句作为教学句跟读记忆
      → 下次"吃+石头"触发拒绝 → 网络自己唤起解释句（检索非生成）
      （教学对标：孩子先是家长教，背多了自己会说）
中期  价值预期：从"类别规则"升到"后果预测"（Q 值语义）
      → 看到 吃+石头 预测负后果而拒绝；回答"为什么"才是"会痛"而非"不在类"
      （生物对标：生物不靠规则避石头，靠痛觉预期）
远期  元认知：网络能解释自己的行为（自解释 = 元认知第一步）
      → 呼应 §4.8 预备 + 用户愿景"AI 慢下来、公众可理解"
```

**脚本与留档**：`_grow_svo.py`（load v8.5 → 槽位/动作类/人称类分配 → 判断题 + 槽位化跟读 → 四道验收 → save\_snapshot parent=8.5 → v9.x → v10.0 动宾迭代；v11 三件套在 v10.0 上迭代）；数据 = `stage26_pos.json`（jieba 标注，已留档）+ 脚本内构造的 S/V/O 槽位句集（runs 落盘）；指标 + 快照 + 报告三件套（成长协议）

### Stage 3 复杂句级（关系）——数据：因果/转折句

* **数据**：含关系标记的专门句（因为…所以… / 虽然…但是… / 先…然后…）

* **训练**：复杂句反复经历，沉淀**关系词转移定式**（因为→所以 的强关联）

* **验收**：关系词对补全（因为→? 答所以）；句复述不退化（Stage 2 零遗忘）

* **n**：扩容（以当前快照 n=148776 为基准，按 §6.3 分配制按需追加——复杂句关系词 + 句模式落位空间；旧规划 4096→8192 为早期"1 词 1 神经元"口径，已废弃）

### Stage 4 文章级（句间逻辑）——数据：句对 + 句间关系 + 文章定式

**定位**：Stage 2 学"句内词间结构"，Stage 4 学"文内句间结构"——文章结构 = 句接句转移 + 句间关系标记 + 文章定式：

1. **句接句转移**：句 A 涟漪 → 句 B 转移定式（跟读机制从词层搬上句层，CLS 慢系统的句子级转移）
2. **句间关系标记**：Stage 3 的关系词（因为/所以/虽然/但是）从"句内标记"延伸到"跨句标记"（"下雨了。所以带伞出门。"）——关系词是句接句的"方向标签"
3. **文章定式**：句式定式（主谓宾之于词 = 总分总/承接/转折布局之于句）同构；主题句/支撑句/总结句的"句子角色"可复用词语类别的 hub 机制（句子角色标签 + 句子成员）

* **验收**：句 B 唤起率（输入句 A 涟漪 → 唤起句 B）；接续不跑题（句间逻辑链）；词/句级不退化

* **QA 语料子集**：问句↔答句对是句接句的特殊形式（问句作句 A、答句作句 B），Stage 4 数据专门化时构造；配合验证门"答对固化/答错拒绝"——**答得出就说，答不出不说（衔接元认知预备）**

* **n**：扩容（以实际 n 为基准按需追加——句级模式 + 句间关系空间；旧规划 8192→16384 为早期"1 词 1 神经元"口径，已废弃）

### Stage 4.5 文章类型（v2.3 新增，2026-08-10 用户追问）——数据：带类型标注的文章

**定位**：文章结构（句接句/定式）是"文内怎么排"，**文章类型（文体/类别）是"这整篇属于哪一类"**——与词语类别（Stage 2.5）同机制、同哲学，只是单元从"词"换成"文章"：

| 层级          | 单元     | 类别机制                   | 类型信号          |
| ----------- | ------ | ---------------------- | ------------- |
| 词层（2.5）     | 词      | 词语类别（食物/动作）            | 成员↔标签共现       |
| 句层（4）       | 句      | 句子角色（主题/支撑/总结）         | 句子↔角色标签       |
| **文层（4.5）** | **文章** | **文章类型（记叙/说明/议论/新闻…）** | **文章模式↔类型标签** |

* **机制**：文章（整篇涟漪/模式）与类型标签反复共发放 → 类型 = 文章模式的传出突触 hub；新文章（没学过类型）输入 → 走链唤起类型标签 = **文体举一反三**（与"菠萝→食物类"同一检索机制）

* **识别信号**：文体标记词（说明文"首先/其次/最后"、议论文"我认为/因此/然而"、新闻"本报讯/导语"）+ 句式特征 + 内容主题——信号在定式网络里就是"文章↔类型"连接结构

* **数据（现成标注）**：语料库 raw 已备——`toutiao_cat_data.txt`（头条新闻 15 类：体育/财经/娱乐/健康…）、`online_shopping_10_cats`（电商评论 10 类）、`ChnSentiCorp`（情感分类）。数据专门化：学文章类型就用带类型标注的文章

* **顺序**：排在 Stage 4 之后（先有"整篇文章模式"，才能做"文章模式↔类型标签"绑定，同词模式先于词类别）

* **验收**：训练文章唤起类型标签 ≥0.9；hold-out 新文章唤起类型 ≥50%（文体识别举一反三）；词/句/文章级零遗忘

### Stage 4.8 元认知预备（v2.3 新增，Stage 5 前）——知道/不知道

**问题**：网络检索是确定性的，唤不起就是空，**它自己不会报告"我不知道"**——没有这个机制，QA 只能"背到就答、背不到就编"。

**机制（定式网络独有：知识 = W 图结构本身，可检查）**：

| 信号    | 是什么                                                 | 对应认知                          |
| ----- | --------------------------------------------------- | ----------------------------- |
| 唤起强度  | fire\_ratio 高低、走链强度衰减终止（§12.7 已验证：下一步 < 上一步×0.4 停止） | "知道感"（检索激活量）                  |
| 路径存在性 | "苹果→颜色"之间 W 里有没有边（图可达性，可查）                          | 区分"知道但没想起来"（有边弱激活）与"真不知道"（无边） |

* **阈值标定**：fire\_ratio 多少算"知道"由**验证门教**——答对 → 该检索标"强知道"；答错 → 标"该说不知道"。三因子规则调制的不只是"固化不固化"，还有"**该不该开口**"

* **诚实边界**：网络做到的"不知道"是**低唤起触发"不知道"响应模式**，不是自我意识（人也是靠"检索失败感"做代理，非绝对自知）

* **定位**：原始信号（强度 + 可达性）现有机制即备，阈值标定在 Stage 5 前接入；排在 Stage 2.5/2.6/3/4 之后自然衔接 QA（见 Stage 4 的 QA 语料子集）

### Stage 5 推理级（概念 + 数学推理，v2.11：数字内容重定位至此）——数据：逻辑推理文本 + 数字/运算语料

**定位**：Stage 5 = 抽象推理层，**两条线并行**（用户 2026-08-10 决策"调整数学内容放到推理单元"——原 Stage 2.7 数字结构 + 2.8 数字运算 整体移入本单元，作**数学推理线**；原有类比/三段论/条件推理作**逻辑推理线**）。两条线共用验证门 / 连接级归因处罚 / LLM 教师等通用件（§12）。

**数学推理线（结构 → 运算 → 意义三层，先形式后意义）**：

* **结构层（原 Stage 2.7 数字结构）**——现代汉语数字**双系统并存**，两者都要学：

  * **中文数字（单位词系统）**：个/十/百/千/万/十万/百万/千万/亿 = **单位 hub 层级**（乘法单位词："三百四十五" = 3×100+4×10+5；"三万/三亿" = 更高进位 hub），不是西方纯位值

  * **阿拉伯数字（纯位值系统）**：0-9 十个数码 + **位置即权值**（"123" = \[百位槽+1]\[十位槽+2]\[个位槽+3]）——复用 Stage 2.6 槽位位置编码（数码绑定到槽位即位值）

  * **混合表达（语料常态）**："8000 万""3.5 万亿" = 阿拉伯位值 + 中文单位 hub 进位节点——双系统接缝，天然验证单位 hub 机制

  * **机制**：数码（0-9 与 零-九）= 数字类别（复用 2.5 类别机制）；中文数位词 = 单位 hub、万/亿 = 四位一节进位 hub 层级；**数数到大数靠结构生成、不靠背长链**（走链强度衰减问题在此解决：数到 1000 = 单位 hub / 位值槽位组合，不是背 1000 跳）

  * **顺序（人类对照）**：数字词/数码识别（记忆）→ 组合规则（槽位绑定）→ 万/亿大数感（hub 层级）→ **双系统互译**（"123"↔"一百二十三"，位值↔单位词映射）

  * **数据**：数字类别（0-9 + 零至九）+ 中文数位词跟读；语料中新闻/财经数字（"8000 万""3.5 万亿""2026 年"）为天然跟读材料

  * **验收**：数字类别归属；1-100 数数链（走链续数）；进位走链（"3百4十5"→"3百4十6"、"99"→"100"）；双系统互译（仅结构映射，不要求数值大小）；词/句级零遗忘

* **运算层（原 Stage 2.8 数字运算）**——分三层，开始时间不同：

  | 层次  | 内容                          | 机制                | 时机               |
  | --- | --------------------------- | ----------------- | ---------------- |
  | 记忆层 | 一位数四则口诀（3+4=7、7×8=56）       | QA 查表 + 验证门判对错    | Stage 4 后        |
  | 过程层 | 多位数竖式（23+18=41：位对位 + 进位/借位） | 位值槽位 + 逐位查表 + 进位链 | 需序列控制/工作记忆（诚实难点） |
  | 意义层 | 为什么 3+4=7（数量化表征）            | 本单元推理             | 最晚、最硬            |

  * **运算符词（加/减/乘/除）= 指令 hub**：决定"3,4"查哪张口诀表——"加"= 加法表开关、"乘"= 乘法表开关，即 §12.2"指令跟随"的天然雏形（运算符词 = 行为模式选择的指令，复用类别 hub 机制）

  * **一位数四则无进位/借位** = 纯查表（四张表记忆），无过程复杂度

  * **多位数竖式** = 位对位调一位数口诀 + 进位传播（个位满 10 → 十位 +1，与数数进位同一机制）；**诚实标注**：竖式是"先个位后十位"的多步过程，需要工作记忆 + 序列控制（§12.5 多步/复合模式，单步 RL 需升级层次控制）——不是缺知识，是缺过程执行器

  * **验收（记忆层）**：口诀随机抽查 top-1 正确率；错题验证门拒绝固化；词/句级零遗忘

  * **诚实边界**：先能"背对"（查表），后能"算对"（过程），最后才"懂对"（意义）——数值意义（"345 > 344"）需数量化表征，最晚最硬

**逻辑推理线（原 Stage 5 内容）**：

* **数据**：专门推理语料（类比/三段论/条件推理）

* **训练**：概念涟漪 ↔ 概念涟漪转移 + 涟漪泛化（半句唤起类似句）

* **验收**：多步逻辑链补全；举一反三（同主题变体预测一致）

**n**：按需扩容（以实际 n 为基准，推理概念涟漪 + 数字/运算模式落位；旧规划 16384→… 已废弃）

### 每阶段必做（成长协议四件套）

1. 前级零遗忘验收（旧评估集 top-1/复述率不退化）
2. 本阶段专门数据验收（位置分层 / 复述率 / 唤起率）
3. 生成质量对比（本阶段数据上，是否"会说本阶段的话"）
4. **留档（缺一不可，2026-08-10 用户决策"每次实验必须留存实验数据"）**：result.json（指标 metrics：验收数字 + 参数 + 判据）+ **net.npz（模型快照：W + 构造参数 + 模式字典 pats + 分配游标 cursor）** + 数据/分级语料（stage25\_sememes.json / stage26\_pos.json 等离线抽取数据留 `data/curriculum/`）+ 训练产物（判断题/槽位句集随 runs 落盘）+ 报告（docs/reports/）——**报告给结论、数据给证据，缺一不可**。**统一走** **`snapshot.py`** **语义版本链**（2026-08-09 落地，MAJOR.MINOR 类比软件版本）：每次训练产生新版本 `runs/v{M}_{N}_{时间戳}/`（旧版本永不删除），index.jsonl 记录 version/parent 父子链；正常续训 major+1（a→a+1→a+2），**回退训练同代变体**（a+2 出问题 → `load_version("2.0")` 回退 a+1 再训 = **a+2.1（3.1）**，与 a+2 平级直接对比）——增量成长硬前提

## 五、数据专门化设计

### 5.1 原则

* 每阶段数据**纯净**（只含该阶段结构），禁止混合跳级

* 数据量按阶段递增（词 < 词组 < 短句 < 复杂句 < 文章 < 推理）

* **每阶段内部有重复**（跟读性质：同一内容反复出现，满足"印象越来越足"）

* 全部生成/抽取到 `data/`，可复现、可留档

### 5.2 数据来源（先抽取，后构造）

现有 corpus\_open20w\.json 可作为**原料库**，按阶段抽取纯净子集：

| Stage    | 抽取规则                                                           | 预估量                          |
| -------- | -------------------------------------------------------------- | ---------------------------- |
| 0 词表     | 词频 top-N（去 UNK、去停用词）                                           | 1000-3000 词                  |
| 1 词组     | 高频相邻词对（bigram 专有数据）                                            | 5000-2 万对                    |
| 2 短句     | 语料中 ≤8 词、无 UNK 的句子                                             | 2 万-5 万句                     |
| 2.5 词义   | **OpenHowNet 义原**（词↔义原判断题，验证门固化/拒绝）                            | 5 万+ 边（实测覆盖 v7.0 词表 52.7%）   |
| 2.6 词性   | **jieba.posseg 词性标注**（v8.5 词表 → 动词 19.3%/6049 词，动作类/人称类成员离线抽取） | 3.1 万词（留档 stage26\_pos.json） |
| 3 复杂句    | 含 因为/所以/虽然/但是/如果 的句子                                           | 1 万-3 万句                     |
| 4 句对/文章  | 语料中相邻句子对（同段落内）；**Wiki 百科条目**（导语→正文→分节，句接句 + 文章定式 + 句间逻辑）       | 2 万-5 万对                     |
| 4.5 文章类型 | **Wiki 条目分类标注**（人物/地理/生物/事件…，自带类型标签，省标注成本）                     | 数千-数万篇                       |
| 5 推理     | 需专门构造（条件/类比模板）                                                 | 数千-数万                        |

**Wiki 定位（v2.4 明确）**：**Stage 4/4.5 的现成语料，不是现阶段（词义/句式）语料**——Wiki 长文粒度太粗，词义阶段喂长文会重演"20 万篇混合文章让他悟"的失败；其价值在**文章级结构**（句接句、定式、句间逻辑）与**自带分类标注**（省 4.5 的标注成本）。原始 dump 需清洗（模板/参考文献/格式标记），到 Stage 4 前按 §5.1"先抽取后构造"管线处理即可。

**关键**：每阶段数据 = "反复读的三字经"——同一批句子多次出现（重复率 ≥ 10 次），不是"每句一次"（v1.1 的错误）。重复率是跟读的本质。

## 六、神经元逐步扩容方案与评估

### 6.1 结论：**可以逐步增加神经元数量，且支持任意粒度（含 n+1）**

v2.1 修订（2026-08-09 用户确认）：用户问题——"n=8032 训练好后，下一次训练能不能 n+1？"答案是**能**。`expand(n_new)` 是纯追加，n\_new 可以等于 n+1，也可以一次扩几百（机制等价）。"知识的增量 = 神经元的增量"，新增一个概念（如第 27 个字母）就 +1。

### 6.2 技术路径（已验证代码结构）

```python
def expand(self, n_new):            # SparseSchemaNet 新增方法
    assert n_new > self.n
    pad = n_new - self.n
    self.v            = np.vstack([self.v, np.zeros((pad, self.slots))])
    self.spikes       = np.pad(self.spikes, (0, pad))
    self.pre_trace    = np.pad(self.pre_trace, (0, pad))
    self.last_k_star  = np.pad(self.last_k_star, (0, pad))
    self.refractory_left = np.pad(self.refractory_left, (0, pad))
    self.slot_freq    = np.pad(self.slot_freq, ((0, pad), (0, 0)))
    self.W_out       += [[{} for _ in range(self.slots)] for _ in range(pad)]
    self._edge_cache += [[None] * self.slots for _ in range(pad)]
    self._edge_dirty += [[True] * self.slots for _ in range(pad)]
    self.n = n_new
```

* **旧知识 100% 保留**：W\_out 前 n\_old 行原样复制（连接结构不动），扩容是纯追加

* 新神经元空白 → 供新阶段学习（新词/新词组/新句模式落位）

* **粒度任意**：`n_new = n + 1`（单个新概念按需落位）或成批扩容（几十上百个），机制等价

### 6.3 关键风险与对策：分配制模式字典（v2.1 修订，取代分区冻结）

旧 `_word_pattern(n, k, word)` 用 `crc32(word)` 做种子、在 `[0, n)` 选神经元——**哈希结果依赖 n**。n 一变，旧词模式重算 → 旧 W 连接失配（灾难），n+1 直接被否。

**对策（分配制模式字典，用户已批准）**：

* **模式字典** `pats: {word: [k 个神经元索引]}` 是唯一事实源：词→神经元落位**生成一次、永久冻结、随网络一起持久化**（npz 附加保存），任何扩容都不重算旧词

* **新词落位 = 分配制**：从当前**空白神经元**按游标顺序取 k 个（分配唯一 → 新词之间零冲突），写入模式字典 +1 条

* **n 只是"可用空间上界"**：旧词条目不动，新词在 \[0, n) 的空白区分配；n+1 只是把可用空间扩大 1，完全安全

* 与哈希对比：分配制牺牲"确定性跨语料复用"，换来**任意粒度扩容 + 新词零冲突**，更贴近"新概念要新神经元"的认知模型

* 验收：扩容前后，旧评估集 top-1/复述率**逐值一致**（零遗忘硬指标）；新增词复述可学（增益为正）

### 6.4 扩容触发条件（何时扩）

三种触发，全部走同一 `expand`：

* **按需 +1**：新增单个词/概念，模式字典分配游标到达 n 边界 → `expand(n+1)`（第 27 个字母场景）

* **区分度不足**：词模式冲突率（分配后仍重叠）或"排列一致性"跌破阈值 → 成批扩容（空间换区分度）

* **阶段升级**：词→词组→句→文自带扩容计划（§四 n 表），一次扩一档

* 扩容验证：扩容后新阶段学习增益为正 + 旧阶段零遗忘 → 通过

### 6.5 为什么必须扩容（不固定 n）

26 字母实验实测：n=256 时排列一致性跌到 0.10（模式空间不足，词分不开）。语言复杂度随阶段递增（词 3000 → 词组 2 万 → 句 5 万），固定 n=8192 会重演"区分度塌缩"（对应 20 万语料 w\_max 饱和后的分数齐平）。**n 随阶段增大，是语言容量与模式区分度的必然需求**，与大脑随年龄增大同理。

### 6.6 续训闭环（v2.2 已验证 ✅ 2026-08-10）

需求（用户 2026-08-10）："模型是逐步迭代的，不是每次从 0 开始学习；以后扩大神经元规模和调整每次调用神经元的数量时，原来之前的记忆不丢。"

**闭环**（脚本 `_grow_continue.py`，报告见 §12.7 后附）：

```
load_version("2.0")   # 加载旧快照（词级，n=54000/13500 模式）
  → baseline 评估        # 零遗忘基准
  → expand(＋4096)       # 纯追加扩容
  → 复测                 # 扩容零遗忘硬指标：旧评估集逐值一致
  → 调 wta_k（2~48）     # "调整每次调用神经元的数量"
  → 分配新词 500 + 跟读   # 游标续用自动扩容
  → save_snapshot        # v6.0，版本链继续
```

**验收结果（全过）**：

* 扩容零遗忘：n 54000→58096，字/词复述 1.0000 逐值一致

* 调参零遗忘：wta\_k 2\~48 遍历，旧复述率不回退（wta\_k=24 时字复述 0.9950 为竞争召回边界现象，非遗忘）

* 续训零遗忘：新增 500 词训练后旧字/词复述不回退，新词复述 1.0000

**关键设计**：续训起点 = `load_version()`（权重 + pats + cursor 全恢复），游标从旧 cursor 继续分配 → 新词自动落在空白神经元，旧模式一条不动。

## 七、双系统读出架构（CLS 计算化）

本轮实验确立：词级（wsum）与句级（涟漪）是两个互补系统，单一系统都不够：

| <br /> | 位置层（皮层对应物）   | 语义层（海马对应物）            |
| ------ | ------------ | --------------------- |
| 记忆     | W 转移定式（慢统计）  | 整句涟漪（快情景）             |
| 精度     | 词级位置（续写）     | 句级主题（联想）              |
| 实测     | wsum 泛化 0.11 | 复述 1.0、主题唤起强、泛化 0.024 |
| 局限     | 只会词接词（跑偏）    | 无位置精度（对不上）            |

**协同方案（待 Phase 实现）**：

1. 涟漪唤起主题句集（候选池，语义过滤）
2. wsum 在候选池约束下做位置续写（词级精度）
3. 融合输出：涟漪提供"语义方向"，wsum 提供"位置落点"

引擎定位修订：wsum = 位置层主力（无参直读最快）；涟漪 = 语义层（整句单元）；grad(train\_ctx) 降级为可选项（20 万干净 W 实测不敌 wsum，z=-2.91）；trace/pahe/candB/C 归档。

## 八、分阶段实施计划

### Phase A：数据专门化基建

* 写 `_data_curriculum.py`：从 corpus\_open20w\.json 抽取 §5.2 分级纯净数据 + 控制重复率 → `data/curriculum/`

* 验收：每级数据纯净性（词表检查、句长过滤、关系标记覆盖）

### Phase B：神经元扩容实现

* `SparseSchemaNet.expand(n_new)` + `_word_pattern_new(n_old, n_new, k, word)`（模式分区冻结）

* 扩容前后旧评估逐值一致验收（零遗忘硬指标）

### Phase C：Stage 0-1 重建（词→词组，专门数据）

* 小 n（256→1024）起步，词表 → 词组，跟读重复

* 验收：词复述率 ≥0.95、词组续词、原子保留、扩容零遗忘

### Phase D：Stage 2-3（短句→复杂句，句级复述）

* 短句反复经历（跟读）→ 整句涟漪固化（网络内，非外挂）

* 验收：句复述率 ≥0.95、关系词补全、零遗忘

### Phase E：Stage 4-5 + 双系统协同

* 句接句转移（文章级）+ 涟漪+wsum 协同读出

* 验收：句 B 唤起率、推理链补全、举一反三

## 九、风险评估

| 风险                        | 等级 | 应对                                             |
| ------------------------- | -- | ---------------------------------------------- |
| 模式冻结实现出错（旧词模式被改）          | 高  | 扩容前后逐值对拍（旧评估集 top-1 完全一致）为硬验收                  |
| 分级数据量不足（抽取太少）             | 中  | 语料不够则构造模板数据（生成器）+ 用户提供专门数据                     |
| 每阶段反复经历导致 W 饱和（w\_max 顶满） | 中  | 沿用 w\_max 64 + 扩容分担（新阶段用新神经元，旧槽不受压）            |
| 句级复述在真实句上不收敛（句模式冲突）       | 中  | 控制句长/词汇量；复述率 0.95 为阶段门槛，不过则缩规模                 |
| 增量成长中灾难性遗忘（旧定式被覆盖）        | 中  | learn\_gate 冻结 + sleep 整理 + 每阶段零遗忘验收；ART 防遗忘原则 |
| 双系统协同融合引入复杂度              | 低  | 先独立验证两系统，再融合（Phase E）                          |

## 十、测试与验收

1. **零遗忘验收**（每阶段）：旧评估集 top-1 / 复述率 / 唤起率不退化（逐值对拍）
2. **本阶段验收**：§四各 Stage 指标（词复述 ≥0.95、词组续词、句复述 ≥0.95、关系补全、句 B 唤起、推理链）
3. **扩容验收**：expand 前后旧评估逐值一致 + 新阶段学习有增益
4. **生成质量**：每阶段数据上"会说本阶段的话"（前缀一致性 + 人工判断）
5. **回归**：abc / 26 字母 / 频率门控 sleep 不退化
6. 每阶段留档 `runs/时间戳/` + 数据留档 `data/curriculum/`

## 十一、影响范围

* **核心网络**：`SparseSchemaNet.expand(n_new)`（新增）；`_word_pattern_new`（新增，模式分区）；learn\_gate/sleep 已有

* **新增**：`_data_curriculum.py`（分级数据）、`data/curriculum/`（分级语料）、`_grow_stage.py`（阶段训练+验收脚本）

* **废弃/归档**：v1.1 的 grad 主引擎定案、trace 优化、20 万混合语料直训路径

* **文档**：主方案 v1.4 同步成长协议；技术档案更新（本实验链 ①-⑧）

***

## 十二、行为模式选择与验证门（v2.2 新增，RL 强化学习）

### 12.1 问题定义：网络不知道"用哪种模式"

Stage 0-1 已实现两种能力：

* **复读**（Hopfield 自联想）：输入完整模式 → 回响自身（"输入 a 输出 a"）

* **联想**（模式补全）：输入前缀 → 补全后续（"输入人之初输出性本善"）

**最大问题（用户 2026-08-09 提出）**：这两种是**解决问题的模式**（未来还有：先复读再联想、举一反三、总结规律/利用规律……）。一个刺激源会**同时激发多个模式的候选**（输入"人之初"：复读候选="人之初"，联想候选="性本善"），但网络**没有机制决定现在用哪个模式**。

### 12.2 认知启发：模式选择信号来自"教学指令"，不在刺激里

* 老师教："**和我一起读**" → 复读；"**人之初下一句是什么**" → 联想

* 刺激本身不携带该用哪个模式的信息，**意图/指令携带**（认知科学"指令跟随" instruction following）

* 指令本身也是词/句，在网络里有自己的神经表征（**指令模式**）——"和我一起读"唤起"复读指令"模式，调制后续行为模式选择

### 12.3 RL 的定位：不是外挂黑盒策略，而是"验证门"（三因子学习规则）

定式网络动力学是确定性的（Hebbian 无监督），RL 不训练动力学，只做一件事：

| RL 要素 | 在定式网络里的落地                        |
| ----- | -------------------------------- |
| 状态    | 网络当前激活（走链到了哪里、唤起了什么候选）           |
| 动作    | 检索/传播产生联想结果                      |
| 奖励    | 外部/内部验证（"对"/"错"）                 |
| 策略更新  | **验证通过 → 该次共发放被固化；失败 → 不固化甚至弱化** |

这正是神经科学中的**三因子学习规则**（Hebbian × 奖励因子/多巴胺）：光共发放不学，共发放 + 奖励到来才强化。现有 `learn_gate`（全局布尔开关）升级为**逐次调制门**（已验证的链首尾 +强，未验证 +0，失败 -弱化）。

> **v2.6 增补（2026-08-10 用户决策，项目级硬约束）**：**所有 RL 环节的负反馈必须附 LLM 生成的自然语言原因**——"错"不能只是标量惩罚：负例 = 拒绝 + 处罚原因（自然语言），原因由 LLM 从网络结构化诊断翻译而来（LLM 是"代嘴"不是知识源；输入限死只喂网络真实边、输出越界拦截）。对齐 RLBFF（原则式奖励）/ Explainable RLHF（归因→修正）。本约束适用于后续所有 Stage（数字运算错题、推理错误、QA 回答错误等全部 RL 场景）。

### 12.4 举一反三的最小机制：联想负责"想"，复读负责"记"，验证门负责"该不该记"

以"流动的是水 / 水是液体 → 流动的是液体"为例：

| 步骤    | 机制                                      | 用的能力     |
| ----- | --------------------------------------- | -------- |
| 学两条前提 | 复读"流动的是水"（流动↔水）、复读"水是液体"（水↔液体）          | 现有跟读     |
| 推理出结论 | 输入"流动" → 传播走链 → 经中间项"水"唤起"液体"           | 现有传播（联想） |
| 压缩成直连 | 验证通过 → 把 \[流动, 液体] **复读一次** → 首尾共发放固化直连 | 复读一对新词   |

关键洞察：

* 纯两句训练下，"流动"和"液体"**从未共发放，物理上断开**——首尾直连不可能无中生有，必须靠"走链成功后把首尾复读一次"来**主动建立**

* **验证门决定该不该压缩**：未经证实的链（如"流动→水→能淹死人"）走通了也只当临时检索结果，**不固化**——这是防止错误联想/认知偏差永久焊死的闸门

* 一旦直连建立，中间项"水"不再是必经之路（组块化/知识编译，ACT-R 产生式编译对应物）——"把中间步骤去掉"的机制

### 12.4b 目标修正（用户 2026-08-10）：学"传递模式"，不是学"这句话"

**关键区分**：

* ❌ 学会"流动的是液体"= 记住了这条**实例**（换"苹果是红的、红是颜色"就失效）→ 死记硬背

* ✅ 学会"X是Y、Y是Z → X是Z"= 抽出**传递律模板**（抽象、可复用）→ 举一反三

这对应"总结规律/利用规律"的实质：**规律是结构，不是内容**。机制 = **角色槽位表征**：

```
"流动的是水" = [主语位 S + 流动] + [系词位 是] + [宾语位 O + 水]
"水是液体"   = [主语位 S + 水]   + [系词位 是] + [宾语位 O + 液体]
                  ↑ "水"同时出现在 O 位和 S 位 → 桥 → 传递链
```

* 跟读注入**角色绑定**（S↔词、O↔词），不止内容共发放

* 多条"X是Y"反复后，共性沉淀的是**槽位变换结构**（S 位条目可经 O 位衔接下一条 S→O 链）→ 传递律模板

* **利用规律 = 新实例填入槽位走链**（即使新组合从未训练过）——真泛化

| 层       | 学的是什么           | 验证方式               |
| ------- | --------------- | ------------------ |
| 实例层     | 流动→液体 首尾直连（组块化） | 同组词反复可用            |
| **模式层** | S-O 槽位变换 = 传递模板 | **换未训练过的新词组合**仍能推出 |

**验收硬指标**：训练 {流动是水, 水是液体, 苹果是水果, 水果是食物, 雪是白的, 白是颜色…}，测试**从未共现**的新组合（如"鸟是动物、动物是生物 → 鸟是生物"）——模板成立才算学会模式，只回吐训练句不算。

### 12.5 与 RL 的能力边界（诚实标注）

| 目标                | RL 能否做到    | 说明                                |
| ----------------- | ---------- | --------------------------------- |
| 验证门：奖励调制固化/弱化     | ✅ 现在能做     | 改动最小，learn\_gate 升级为调制门           |
| 指令→模式映射（"跟我读"→复读） | ✅ 能做       | 需要先把复读/联想抽象为接口化行为模式模块             |
| 行为模式仲裁（多候选选一）     | ✅ 能做       | 需"候选报告接口"（观测通道）+ 明确奖励             |
| 加法举一反三（5+3=8）     | ⚠️ 只做验证门部分 | 泛化靠**共享表征**（数字需数量化表征），RL 不产生泛化    |
| 多步/复合模式（先复读再联想）   | ⚠️ 暂不做     | 需工作记忆 + 序列控制，RL 要从单步升级到 MDP/层次 RL |

### 12.6 最小验证实验（已实施 ✅ 2026-08-10，脚本 `_rl_gate.py`）

**语料（带角色槽位）**：

* 训练组（多实例，学传递模板）：{流动是水, 水是液体, 苹果是水果, 水果是食物, 雪是白的, 白是颜色}

* 陷阱前提（先建立真实错误链，供验证门拒绝固化）：{水能淹死人}

* 验证门拒绝对象：{流动的能淹死人}（共享中间项"水"的陷阱结论）

* 测试组（**训练中从未共现的新组合**）：{鸟是动物, 动物是生物} → 应推出"鸟是生物"

**流程**：

1. 槽位化跟读：注入 \[S+X] + \[是] + \[O+Y] 角色绑定，多实例反复 → 沉淀 S-O 槽位变换模板
2. 走链：输入"流动" → 传播经"水"唤起"液体"
3. 验证门：老师说"流动的是液体→对"，"流动的能淹死人→错"；对链首尾复读固化，错链不固化
4. 验收（分两层）：

   * **实例层**：验证固化后"流动"能否直接唤起"液体"

   * **模式层**：测试组新组合（鸟/动物/生物从未共现训练）能否推出"鸟是生物"——**只回吐训练句不算学会模式**

**实验结果（全部 PASS ✅）**：

```
[1] 训练组 6 句 + 陷阱前提 1 句 × 3 轮 跟读完成
[2] 实例层走链（输入 流动）: 流动 → 水 → 液体 ✅ 联想链成立
[3] 验证门：流动的是液体→固化 (0.0→1.6)；流动的能淹死人→不固化 (0.0→0.0)
[验收①] 错误链前提「水能淹死人」已建立（直连 4.8）✅
         正确链固化 + 错误结论拒绝固化 → ✅
[5] 模式层泛化: 鸟 → 动物 → 生物 ✅ 结论句「鸟是生物」从未跟读 → 真举一反三
[验收②] 实例层直连 ✅
[验收③] 模式层模板 ✅
═══ 总验收: PASS ✅ ═══
```

**关键结论**：

1. **复读+验证门确实能长出直连**（实例层组块化成立：流动→液体 0.0→1.6）
2. **验证门能挡住错误联想固化**（认知偏差防护：错误链「水→淹死人」存在但错误结论不被加固，这正是不让负面联想焊死成规律的闸门）
3. **模式层真泛化成立**（传递模板套用新词组合：鸟/动物/生物从未训练共现，仍推出"鸟是生物"——学的是"X是Y、Y是Z→X是Z"模式，不是"流动的是液体"这句话）

**过程中发现并修复**：`SparseSchemaNet._edge_row` 空行返回 `(None, None)` 而非 `None`，传播时 `drive[None] += None` 报 dtype 错（此前 `_grow_zh` 碰巧未触发）；`_rl_gate.run_train` 需清 `refractory_left`（跟读独立事件，防上句发放的词在本句被不应期挡下——"流动是水"→"水是液体"中"水"连续出现，不清则水↔液体共发放学不到）。

### 12.6b 泛化压力测试（已实施 ✅ 2026-08-10，脚本 `_rl_gate_stress.py`）

五级递进，验证"彻底泛化"，全部 PASS：

| 级别       | 内容                                     | 结果                    |
| -------- | -------------------------------------- | --------------------- |
| L1 规模泛化  | 40 词 / 32 实例（8 独立链×5 词），全链走通率          | **100%（8/8）**         |
| L2 链长泛化  | 4 跳传递链（A→B→C→D→E）                      | 含于 L1，全通              |
| L3 新词外推  | 全新词（麒麟/瑞兽/神兽，训练零共现）只学前提 → 推出结论         | ✅                     |
| L4 中间项断开 | 固化直连后摘除中间项 B（清传入突触+传出突触）：对照组断开、实验组直连仍在 | ✅（组块化彻底）              |
| L5 随机性稳定 | 10 个随机种子重复 L1                          | **均值 100% / 最低 100%** |

**L4 关键对照**：未固化就摘除 B → 走链 A 断开（证明必须靠直连）；验证门固化 A→C（0.0→1.6）后摘除 B → A→C→D 仍通（组块化彻底，不再借道中间项）。

**结论**：举一反三能力在 40 词/32 实例规模下彻底泛化——传递模板（学模式）与内容解耦，任意新词组合套模板即可推理，且不受中间项存废影响。

### 12.6c 规模压力测试与缺口修复（已实施 ✅ 2026-08-10）

**规模压力测试**（脚本 `_rl_gate_scale.py`，数据留档 `runs/`）：S1-S3 词表 100→300→1000 词，推理走通 **100%**、模板零稀释（规模本身撑得住）；噪声下推理也不受影响。但暴露**两个真实机制缺口**：

| 缺口           | 症状                 | 根因                   |
| ------------ | ------------------ | -------------------- |
| 终止缺口（S4）     | 57/60 条联想沿噪声连接无限蔓延 | 贪心走链无停止判据            |
| 上下文缺口（S5/S6） | 共享 hub 分支选错、被强分支吸走 | 每跳只看当前词传出突触，不知道"从哪来" |

**修复实验**（脚本 `_rl_gate_fix.py`，报告 \[REPORT]-补缺口实验）：三补丁机制——

```
1. 整链跟读：训练时整条链作为一句复读 → 链内非相邻词建立弱直连
   （为路径聚合提供信号；认知对应"整句固化句内连接"）
2. 路径聚合走链：每步候选得分 = 路径上所有已见词传出突触聚合
   （认知对应"工作记忆"：记住起点调制分支选择）
3. 强度衰减终止：下一步强度 < 上一步 × 0.4 → 停止
   （认知对应"联想收敛/AI 慢下来"）
```

**验收（同一网络，只切换检索方式 → 提升归因于检索机制）**：

* F1 终止：过冲 **57 → 0 条**，推理走通保持 100%

* F2 上下文（等权分支）：A1→B1、A2→B2 旧走链错 1 条 → 新走链全对

* F3 上下文（不对称分支）：弱分支不再被强分支吸走，全对

**结论**：举一反三的联想层已具备"收敛"（终止）与"定向"（上下文）两能力，与验证门（该不该记）构成完整三角。

### 12.7 影响范围（v2.2）

* **核心网络**：`learn_gate` 从全局布尔升级为逐次调制门（带强度）；走链检索已有

* **新增**：行为模式接口化模块（复读/联想各一）、验证门训练脚本 `_rl_gate.py`

* **不改变**：Hebbian 动力学、分配制、扩容、快照版本链

### 12.7b 大网络机制验证（已实施 ✅ 2026-08-10，脚本 `_rl_gate_big.py`）

机制从构造词表（w0\~w59）搬迁到 v2.0 真实网络（n=54000，13502 模式），验证真实竞争下成立。

**关键发现（第一版失败）**：贪心走链在真实网络被**字形分解带偏**——"苹果→果→苹→小…"，因为字模式与词强连接（word\_to\_char 0.997，跟读 8 轮）远强于槽位化语义连接（3 轮）。语义传递与字形分解共用同一 W 传出突触竞争，这是构造网络发现不了的矛盾。

**修复：词级走链** **`walk_chain_lex`**（候选排除单字模式 + 上下文聚合 + 终止衰减）——语义传递（苹果→水果→食物）发生在词层面，字层面是构成关系，检索时分层。认知对应：人的"苹果→水果"语义联想与"苹果→苹"字形分解是不同通路。

**验收（全部通过）**：

* 词级走链：苹果→水果→食物 ✅、动物→生物 ✅（结论句从未跟读）

* 终止收敛 ✅、验证门（固化 0→1.6 / 拒绝 0→0）✅

**结论**：验证门/走链/终止/上下文全套机制在真实规模成立；接大网络的必要修正是**检索分层**（词级 vs 字级）。
